# FranSCORE 기술설명서

**프랜차이즈 브랜드 여신 포트폴리오 집중·상관 리스크 관리**
KB 제8회 AI Challenge 출품작 · 전 과정 기술서

> 가동 중인 서비스 — <https://franscore.streamlit.app>
> 저장소 — 비공개 (제출 시 심사용 접근 제공)
> 문서 작성 기준일 — 2026-08-03 / 커밋 `5c66d93` 시점

---

## 이 문서를 읽는 법

이 문서는 FranSCORE 의 **개발 전 과정**을 기술한다. 무엇을 만들었는지뿐 아니라
**무엇을 시도했다 버렸는지**, **어디서 틀렸고 어떻게 알았는지**를 함께 적는다.
후자를 뺀 기술서는 결과 홍보문이지 기술서가 아니라고 판단했다.

문서에 나오는 모든 수치는 이 저장소의 산출물에서 **직접 계산된 값**이다.
그 정합성은 사람이 아니라 도구가 검사한다 — `tools/check_doc_numbers.py` 가
문서에 적힌 **142건**의 숫자를 산출물에서 다시 계산해 대조하고, 하나라도 어긋나면
종료코드 1 로 실패한다. 폐기된 표기(옛 모델명·옛 컬럼명 등)가 남아 있어도 실패한다.

```bash
python tools/check_doc_numbers.py
```

### 문서 구성

| 부 | 내용 | 심사 관점 |
|---|---|---|
| 제1부 | 문제 정의 · 시장 조사 · 규제 조사 · 문제의 정식화 | 문제 정의 및 필요성 |
| 제2부 | 데이터 — 원천 9종 · 엔티티 정합 · 패널 · 커버리지 | 기술 적정성 |
| 제3부 | 모형 — 라벨 · 피처 · 학습 · 보정 · 등급 · 검증 | 기술 적정성 / 실현 가능성 |
| 제4부 | AI 활용 — 무엇에 쓰고 무엇에 안 쓰는가 · 정량 평가 | 아이디어의 창의성 및 효과 |
| 제5부 | 제품 — 화면 5종 · 디자인 시스템 · 접근성 | 활용 가능성 |
| 제6부 | 운영 — 파이프라인 · 배치 · 배포 · 폐쇄망 · 사용 명세 | 개발 계획의 구체성 |
| 제7부 | 품질 체계 — 우리가 스스로 잡은 결함 전수 기록 | 개발 과정의 신뢰성 |
| 제8부 | 한계 · 로드맵 | 정직성 |
| 부록 | 산출물 색인 · 재현 절차 · 개발 이력 | 재현 가능성 |

### 규모 요약

| 항목 | 값 |
|---|---|
| 파이썬 소스 | 57개 파일 · 20,420줄 (`src/` 30 · `tools/` 13 · `tests/` 8 · 진입 6) |
| 문서 | 12종 (본 문서 포함) |
| 커밋 | 54개 (2026-07-30 ~ 2026-08-03) |
| 패널 | 60,478행 / 16,406 브랜드 / 2016~2024 (9개 연도) |
| 피처 | 49개 (죽은 피처 0개) |
| 학습·검증 라벨 | 3,635행 · 양성률 9.2% |
| 배포 코호트 | 1,442 브랜드 (2024년 공시 기준) |
| 자동 검사 | 테스트 30건 (pytest) + 문서 수치 142건 + 린트 |

---
---

# 제1부 · 문제

## 1. 한 문장으로

> **은행은 프랜차이즈 가맹점에 돈을 빌려주면서, 그 점포가 어느 브랜드에 묶여 있는지를
> 위험 축으로 보지 않는다. 그런데 그 브랜드가 위험의 실제 축이다.**

FranSCORE 는 이 빠진 축 하나를 만든다. 차주 심사를 대체하지 않는다. 차주 심사가
**구조적으로 볼 수 없는 축**을 얹는다.

---

## 2. 시장 — 얼마나 큰 문제인가

### 2-1. 자영업자 여신의 규모와 질

| 지표 | 값 | 출처 |
|---|---|---|
| 자영업자 대출 잔액 | **1,095.5조원** (전 금융권 가계·기업대출의 **28.5%**) | 한국은행 『금융안정보고서』 2025.12 |
| 숙박·음식업 사업자대출 연체율 | **3.02%** | 〃 |
| — 비교: 도소매 | 1.77% | 〃 |
| — 비교: 제조 | 2.37% | 〃 |
| — 비교: 부동산 | 1.10% | 〃 |
| 자영업자 차주 | **307.8만명** | 〃 |
| 그중 취약차주 | **43.7만명** (전년 38.9만 → 증가) | 〃 |

숙박·음식업은 주요 업종 중 연체율이 가장 높고, 부동산업의 약 3배다. 그리고
**차주 총수는 줄어드는 동안 취약차주만 늘었다** — 남은 차주의 질이 나빠지고 있다는 뜻이다.

### 2-2. 그 안에서 프랜차이즈의 비중

| 지표 | 값 | 전년 대비 | 출처 |
|---|---|---|---|
| 가맹본부 | 9,960개 | — | 공정거래위원회 「2025년 가맹사업 현황 통계」 |
| 브랜드 | **13,725개** | **+10.9%** | 〃 |
| 가맹점 | **379,739개** | **+4.0%** | 〃 |

브랜드가 가맹점보다 2.7배 빠르게 늘고 있다. 브랜드당 점포 수가 줄어드는 구조 —
**소형·신생 브랜드가 계속 생겨나는 시장**이다. 뒤에서 다시 다루지만, 이것이
"점포 30개 이상" 같은 자격 요건이 커버리지를 크게 깎는 이유이기도 하다.

**출처 URL**
- 한국은행 『금융안정보고서』 2025년 12월 — <https://www.bok.or.kr/portal/bbs/P0000593/view.do?menuNo=200068&nttId=10095308>
- 공정거래위원회 「2025년 가맹사업 현황 통계 발표」 — <https://www.ftc.go.kr/www/selectBbsNttView.do?key=12&bordCd=3&nttSn=47332>

### 2-3. KB 고유 수치를 쓰지 않은 이유

이 문서에는 KB국민은행의 SOHO 여신 잔액·가맹점 결제 점유 같은 **내부 지표가 한 줄도
없다.** 그 값을 대입하면 뒤에 나오는 배수(비예상손실 7.30배)가 곧바로 원화 자본 소요로
환산되어 훨씬 인상적인 문장을 쓸 수 있다. 그러나 공개 통계로 확인할 수 없는 값이므로
**추정치를 지어내지 않는다.**

대신 **은행 실여신 CSV 한 장을 붙이면 그 자리가 채워지도록** 인터페이스를 계약 수준으로
정의해 두었다 (→ 제6부 §33, `docs/OPERATIONS.md` §8). 이것이 이 프로젝트의 일관된
태도다: 없는 숫자는 만들지 않고, 그 숫자가 들어올 자리를 만든다.

---

## 3. 왜 이 위험이 은행 것인가

### 3-1. 법이 만든 구조 — 담보가 채무불이행과 동시에 증발한다

가맹사업법상 **가맹점사업자의 파산·부도·영업정지는 즉시해지 사유**다.
차주가 부도나는 순간 가맹계약이 끊기고 영업권이 소멸한다.

이것이 일반 소상공인 여신과 결정적으로 다른 점이다. 일반 점포는 부도가 나도
설비·상권·임차권이 남아 회수 재원이 된다. 가맹점은 **간판을 내리는 순간 그 점포가
가졌던 가치의 상당 부분이 브랜드에 귀속돼 사라진다.**

즉 **회수율(LGD)이 브랜드 상태에 직접 묶여 있다.** 브랜드가 흔들리면 부도확률(PD)과
회수율이 **같은 방향으로 동시에** 나빠진다. 이 동시성은 차주 단위 심사로는 잡히지 않는다.

### 3-2. 그런데 시장에 그 축이 없다

개인사업자 CB·카드사·빅테크가 쓰는 대안정보를 조사했다.

| 대안정보 | 단위 |
|---|---|
| 가맹점 매출 | 차주(점포) |
| 상권 정보 | 차주(점포) |
| 업력 | 차주(점포) |
| 결제 정보 | 차주(점포) |
| 단골 고객 | 차주(점포) |
| 부동산 | 차주(점포) |

**전부 차주 단위다.** 그 목록 어디에도 '브랜드 단위 위험도'가 없다.
차주 심사를 아무리 정교하게 해도, 같은 브랜드 300개 점포에 나간 여신이 **함께**
움직인다는 사실은 그 심사에 들어오지 않는다.

---

## 4. 전제를 주장하지 않고 측정했다

이 도구 전체가 하나의 전제 위에 서 있다.

> **같은 브랜드에 묶인 여신은 '브랜드'라는 공통 요인으로 상관된다.**

전제가 거짓이면 브랜드 단위 관리는 불필요하고 이 프로젝트는 존재 이유가 없다.
그래서 **가장 먼저 이 전제를 측정했다** (`src/correlation.py`).

### 4-1. 식별 전략

한 브랜드의 점포는 여러 시도(市道)에 흩어져 있고, 시도마다 상권·지역경기·임대료가 다르다.

- 점포 감소가 **지역별로 독립**이면 → 입지 요인
- 여러 지역에서 **동시에** 일어나면 → 브랜드 요인

독립 가정을 넘어서는 동시성의 초과분이 곧 브랜드 공통요인이다. 브랜드×연도×지역
패널 144만 행에서 **지역쌍 349,089개**의 동시감소를 실측했다.

### 4-2. 결과 — 층화로 거시를 걷어내도 남는다

자산상관은 바젤 IRB/ASRF 와 **같은 정의**로 역산한다:
p₁₁ = Φ₂(Φ⁻¹(p), Φ⁻¹(p); ρ) 를 수치 역산.

| 통제 수준 | 자산상관 ρ | 무엇을 걷어냈나 |
|---|---|---|
| 통제 없음 | 0.418 | (거시+업종+브랜드 혼재) |
| 연도 통제 | 0.417 | 코로나 등 **거시충격** 제거 |
| **연도+업종 통제** | **0.416** | **브랜드 고유 상관** |

거의 줄지 않는다. **"그냥 코로나 아니냐"를 수치로 기각**한 것이다.

| 측정값 | 결과 |
|---|---|
| 브랜드 **내부** 자산상관 ρ_W | **0.416** [0.382, 0.438] |
| — 바젤 기업여신 가정(12~24%) 대비 | **1.7~3.5배** |
| 브랜드 **간** 자산상관 ρ_B | **0.005** [0.000, 0.009] |
| 한 브랜드 전 지역 동시감소 실측 빈도 | **4.31%** |
| — 지역 독립 가정 시 기대 빈도 | 0.200% (**21.6배**) |

### 4-3. 이 결과가 뜻하는 것

**브랜드 내부는 강하게 묶여 있고, 브랜드끼리는 거의 무관하다.**

ρ_B ≈ 0 은 처음엔 실망스러운 결과처럼 보였다. 거시 공통요인이 검출되지 않았다는
뜻이기 때문이다. 그런데 이 결과는 오히려 **처방을 강화한다**:

> 위험이 경기(거시)에 있지 않고 **브랜드에** 있다면, 관리 단위도 브랜드여야 한다.
> 거시 시나리오 스트레스로는 이 위험이 잡히지 않는다.

그리고 브랜드 간 상관이 낮다는 것은 **브랜드 분산이 실제로 효과가 있다**는 뜻이기도
하다 — 이 도구가 제안하는 처방(브랜드 한도 관리)이 작동할 여지가 있다는 근거다.

### 4-4. 손실로 환산

차주(가맹점) 단위 2단계 요인 모형:

```
X_ij = √ρ_B · Z  +  √(ρ_W − ρ_B) · Y_i  +  √(1 − ρ_W) · ε_ij
       (거시)        (브랜드 i)            (개별 점포 j)
```

은행 여신은 브랜드 법인이 아니라 **개별 가맹점주**에게 나가므로 이 층위가 맞다.
요인 조건부 독립을 이용해 브랜드별 이항분포로 정확히·가볍게 계산한다(차주 9,180명,
시뮬레이션 200,000회).

| 지표 | 독립 가정 | 브랜드 상관 반영 | 차이 |
|---|---|---|---|
| 기대손실 (EL) | 15.3억 | 15.3억 | 변화 없음 |
| 99% 손실 | 17.0억 | **28.0억** | **+11.0억 (+64.3%)** |
| 99.9% 손실 | 17.6억 | 36.9억 | +19.3억 (+109.4%) |
| **비예상손실 (UL₉₉)** | 1.74억 | **12.71억** | **7.30배** |

**상관은 평균을 바꾸지 않고 꼬리만 두껍게 한다.** 이것이 정확히 자본 소요가 사는 자리다.

---

## 5. 규제·업계 조사

이 도구가 은행에서 실제로 받아들여지려면 무엇을 충족해야 하는지 조사했다.

### 5-1. 모형 리스크 관리 — SR 11-7 은 이미 대체되었다

미 연준의 모형 리스크 관리 지침 **SR 11-7 은 2026년 4월 17일 SR 26-2 로 대체**되었다.
비중요(non-material) 모형의 최소 요건은 세 가지다:

1. 모형 인벤토리 등재
2. 성과 모니터링
3. 중요도 전환 조건 정의

그리고 조사에서 얻은 가장 중요한 시사점은 이것이었다:

> **1순위 산출물은 성능표가 아니라 「사용 범위·금지 용도 명세」다.**
> 참고 도구가 은행에서 거부되는 실제 사유는 AUC 가 낮아서가 아니라
> **"어디까지 쓸 수 있는지 모르겠다"** 이기 때문이다.

그래서 `docs/MODEL_USE_SPEC.md` 를 만들었다 — 허용 용도 4개, **금지 용도 8개**,
중요도 전환 조건 4개, 알려진 한계 10개를 못박았다 (→ 제6부 §35).

### 5-2. 신용평가사는 등급을 어떻게 다루는가

| 관행 | 우리의 적용 |
|---|---|
| **부여척도와 공표척도의 분리** — 18~20 notch 로 부여하지만 부도율·이행행렬은 6개 알파벳 버킷으로만 공표 | 3등급(FS1/FS2/FS3)으로 공표. 통계적으로 지지되지 않는 세분화는 검증표에서 접는 것이 업계 표준이다 |
| **정의를 외부 규범에서 인용** — 부도를 금융투자업규정 조문 번호까지 달아 인용 | 우리 '악화'도 공정위 공시 항목 관측값으로만 구성하고 정의를 문서·산출물에 고정 |
| **절대 기준은 규칙이 아니라 등급별 실현율 실적표가 만든다** | 등급 컷을 순위 백분위에서 절대 확률 컷으로 전환하고 실현율표를 공표 |
| **이행행렬에 이탈(WR) 열을 반드시 둔다** | 이탈률 11.0% 를 별도 열로 공표 — 없으면 "다들 등급을 유지했다"는 거짓 인상을 준다 |

**3등급인 이유**를 정직하게 밝힌다. 4등급 이상은 보정 연도를 뺀 진짜 OOS 에서
인접 구간의 신뢰구간이 겹치고, 연도를 빼면 최상위가 붕괴한다. 우리가 가진 표본으로
지지되는 세분화가 3단계까지라는 뜻이다.

### 5-3. 바젤 IRB — 개념은 빌리되 이름은 빌리지 않는다

- **자산상관 정의**를 IRB/ASRF 와 동일하게 써서 우리 ρ 를 업계 벤치마크(12~24%)와
  비교 가능하게 했다.
- **확률 하한 관행**(0.03%)을 준용했다.

그러나 **우리 산출물을 PD 라고 부르지 않는다.** 라벨이 부도가 아니기 때문이다
(→ §7-2, 제3부 §11). 이 구분은 코드 주석·문서·산출물·테스트 네 곳에 못박혀 있다.

---

## 6. 기존 수단으로 왜 안 되는가

| 기존 수단 | 무엇을 보는가 | 왜 부족한가 |
|---|---|---|
| 개인사업자 CB | 차주 신용·매출·상권 | 브랜드 축 없음. 같은 브랜드 300개 점포가 함께 움직이는 것을 모른다 |
| 본부 재무제표 심사 | 가맹본부 재무 | 외부감사 대상만 공시. 브랜드 기준 커버리지 **11.4%** (실측). 그리고 본부가 멀쩡해도 특정 브랜드만 무너질 수 있다 |
| 뉴스 모니터링 | 사건 발생 후 | **사후**다. 집단폐점이 기사화될 때는 이미 여신이 물려 있다 |
| 거시 스트레스 테스트 | 경기 시나리오 | ρ_B = 0.005 — **거시 공통요인이 검출되지 않는다.** 이 위험은 거시 시나리오로 잡히지 않는다 |

마지막 줄이 특히 중요하다. 우리가 측정한 결과는 "브랜드 위험은 거시 스트레스로
대체될 수 없다"는 것이다. 이 축이 없으면 위험은 **어떤 기존 도구에도 안 잡힌다.**

---

## 7. 문제의 정식화 — 우리가 정확히 무엇을 푸는가

### 7-1. 입력과 출력

```
입력   공정거래위원회 가맹사업 공시 (연 1회) + 금융감독원 전자공시(DART) 감사보고서
       + 네이버 검색어트렌드 + 뉴스
       [+ 은행 실여신 CSV — 선택, 붙이면 절대 금액이 실측이 된다]

출력   ① 브랜드별 1년 내 구조악화 전환 확률 + 3등급 + 근거 소견
       ② 브랜드별 여신 집중도 · 상관 반영 손실 분포 · 꼬리 기여 배분
       ③ 점검 우선순위 큐
```

### 7-2. '악화'의 정확한 정의 — 그리고 왜 '부도'가 아닌가

우리가 예측하는 사건은 **부도가 아니다.** 실제 여신 부도·연체 라벨을 **한 건도 보지
못했기 때문이다.** 그것을 부도확률이라 부르는 것은 우리가 가지지 않은 근거를
주장하는 일이다.

정의는 이렇다:

> **악화** = 공정거래위원회 가맹사업 공시 지표(가맹점수·계약종료·평균매출·면적당매출)가
> **업종×연도 하위 구간에 진입**하는 사건

- 사건 3종: 점포 순감소(하위 20%) / 실질매출 감소(하위 20%) / 계약종료율 급등(상위 20%)
- **2개 이상 동시 발동** 또는 **1개가 극단(5%)** 이면 양성
- 노이즈 게이트: |Δ점포| ≥ 3, 점포 10개 이상
- 업종 중앙값으로 디플레이트 (업종 전체가 나빠진 것과 이 브랜드가 나빠진 것을 구분)

이 정의는 **학습 이전에 git 커밋 `cfcf164` 로 동결**했다. 성능이 안 나온다고 임계값을
조정한 뒤 재측정하는 것을 구조적으로 막기 위해서다. 이력으로 검증 가능하다.

### 7-3. 우리가 풀지 않는 문제 — 명시적 범위 밖

- 차주(가맹점주) 개인 신용평가 → 기존 CB 의 영역
- 자동 여신 승인·거절 → **금지 용도**로 명세에 못박음
- 부도확률 추정 → 라벨이 부도가 아님
- 실시간 위험 감지 → 공시가 연 1회이므로 확률은 연 1회 움직인다 (뉴스·검색수요가 그 사이를 보조)

---
---

# 제2부 · 데이터

## 8. 원천 — 무엇을 어디서 가져오는가

### 8-1. 공정거래위원회 가맹사업 오픈API (7종, 전량 수집)

| 데이터셋 ID | 이름 | 그레인 | 우리가 쓰는 것 |
|---|---|---|---|
| **15110241** | 가맹점 현황 | 브랜드×연도 | **패널 본체** — 가맹점수·신규등록·계약종료·계약해지·명의변경·평균매출·면적당매출 |
| **15109828** | 브랜드 개요 | 브랜드 | 가맹사업개시일(업력)·임원수·직원수 |
| **15157660** | 업종 개폐점률 | 업종×연도 | 업종 기준선 전용 |
| **15125467** | 브랜드 등록 정보 | 브랜드 | **브랜드관리번호**·가맹본부관리번호·사업자등록번호 — 정합의 핵심 키 |
| **15125490** | 가맹점·직영점 정보 | 브랜드×연도×**지역** | 직영점수·지역분산 원천 (144만 행) |
| **15110265** | 브랜드별 창업 금액 | 브랜드×연도 | 가맹금·교육비·보증금·기타 — **여신 익스포저 실측 원천** |
| **15125518** | 브랜드 등록취소 | 사건 | 자진/직권 취소 — 하드 실패 신호 |

**수집 원칙:** 원본 JSON 을 `data/raw/` 에 **그대로 보존**한다. 이미 존재하면 재호출하지
않는다. 새 클론에서 API 키 없이 전 지표를 재현할 수 있는 이유다(실측 bit-identical 확인).

### 8-2. 조사했으나 쓰지 않은 것 — 이유와 함께

| 데이터셋 | 판정 | 사유 |
|---|---|---|
| 15110402 업종 개폐점률 | **폐기 확인** | data.go.kr 404. 후속 데이터셋 15157660 으로 대체 |
| 15143710 브랜드 평균매출·점포수 | **제외** | fairdata.go.kr **가명처리·구간값** (브랜드ID 가명, 매출 하/상한 구간). 15110241 이 비구간 실값 제공 → 상위 대체 |
| 15143709 지역별 | **대체** | 동일 fairdata 계열. 15125490(비가명·실값)으로 대체 달성 |
| 15125569 정보공개서 목록 | **부분** | 공정위 별도 키 체계 — data.go.kr 키는 `INVALID_KEY`. 공개 데모키로 목록만 취득 |

가명·구간값 데이터를 쓰지 않은 것은 중요한 선택이다. 구간값을 쓰면 커버리지가 늘지만
**변화율을 계산할 수 없다.** 이 모형의 핵심 신호는 수준이 아니라 변화이므로, 커버리지를
포기하고 실값을 택했다.

### 8-3. 금융감독원 전자공시 (DART)

**"DART 는 상장사만"은 사실이 아니다.** 외부감사 대상이면 비상장이어도 감사보고서를
제출한다. 메가엠지씨커피·컴포즈커피·이디야·BHC·배스킨라빈스 등 주요 비상장 본부가
모두 제출 중이다.

| 커버리지 기준 | 브랜드 수 | 점포 가중 |
|---|---|---|
| 상장사만 | — | 4.7% |
| **감사보고서 포함** | — | **39.7%** |

수집 후 **자기 검산**을 붙였다:

| 검산 | 대상 | 통과율 |
|---|---|---|
| 자산 = 부채 + 자본 | 2,729행 | **99.63%** |
| 영업이익 ≤ 매출액 | 2,678행 | **98.28%** |

감사의견 분포도 함께 기록한다: 적정 2,053 · 한정 254 · 의견거절 55 · 부적정 3 ·
미기재 394. 계속기업 불확실성 언급 **97건**.

### 8-4. 공정거래위원회 정보공개서 (가맹사업정보제공시스템)

정보공개서에는 **외감 여부와 무관하게 전 등록 브랜드의 3개년 재무**가 있다.
본부 재무 커버리지 11.4% 를 근본적으로 해소할 수 있는 유일한 경로다.

- 정식 API(`IFRMP_SERVICE_KEY`)는 **별도 키 체계** — data.go.kr 키로는 접근 불가
- 파서는 실제 문서로 검증 완료 (110개 표준 섹션, 재무 3년·점포 3년 정확 추출)
- HTTP 406 은 키 문제가 아니라 **User-Agent 없는 요청을 막는 WAF** 였다
- 공개 데모키는 본문을 **고정 1건**으로만 반환한다 — 서로 다른 `sn` 5건의 SHA-256 이
  동일함을 확인하고, 응답 해시 대조로 **스스로 탐지**해 브랜드별 산출물을 만들지 않는다

**웹 열람 경로와 캡차** — 같은 시스템의 일반 열람 화면(`franchise.ftc.go.kr`)은
공개돼 있다. 실측으로 `list.do` 약 11,751건 열거, `view.do` 로 재무·가맹점변동·
지역매출·법위반 표 획득까지 확인했다. 그러나:

> 🛑 세션당 **5건 열람 직후 캡차**가 걸린다("사람 인증이 필요합니다"). 목록 재방문·
> 10초 대기 모두 무효 — rate limit 이 아니라 **자동화 차단 장치**다.
> **우회하지 않았다.** 기술적 가능 여부와 무관하게, 명시적 자동화 차단을 뚫는 것은
> 이 프로젝트가 지킬 선 밖이다.

그래서 전량 수집기가 아니라 **주문형 조회 도구**(`--brand`)로 남겼고, 자동 배치에
연결하지 않았다. 본부 재무 공백의 구조적 해소는 여전히 정식 키가 유일한 길이다.

실측 성과 (한 브랜드 예시 — 우리 모형이 재무를 전혀 못 보던 브랜드):

| 국물에빠진두루치기 | 2023 | 2024 | 2025 |
|---|---|---|---|
| 자본 | 140억 | 337억 | **−67억 (자본잠식)** |
| 영업이익 | −203억 | +178억 | **−411억** |
| 매출 | 5,861억 | 5,725억 | 5,152억 |

자산 = 부채 + 자본 검산 3개년 전부 일치.

### 8-5. 네이버 (검색어트렌드 · 검색 · 웹문서)

| API | 용도 | 한도 |
|---|---|---|
| 검색어트렌드 (`/search-trend/v1/search`) | 브랜드 수요 추이 | 50,000/일 |
| 뉴스 검색 (`/search/v1/news`) | 뉴스 본문 요약 → LLM 입력 | 25,000/일 |
| 웹문서 검색 (`/search/v1/webkr`) | 공식 도메인 식별 (로고) | 〃 |
| 이미지 검색 (`/search/v1/image`) | 로고 후보 수집 | 〃 |

**핵심 설계:** 브랜드와 카테고리를 **한 요청**에 담는다. 따로 부르면 각자 최대 100으로
재정규화되어 비교가 불가능해진다. 계절성 상쇄를 위해 최근 12개월 평균 vs 직전 12개월
평균으로 증감을 잡는다.

실측 (수요 292개 브랜드 수집):

```
인생냉면   검색량 −31.8%  /  냉면 카테고리 −20.7%   → 업종보다 11.1%p 더 빠짐
달콤왕가탕후루  −67.2%
```

이 "업종 대비 초과 하락" 구분이 중요하다. **냉면 수요가 준 것인가, 이 브랜드만
밀리는가** — 여신 판단에서 완전히 다른 이야기다.

---

## 9. 엔티티 정합 — 이름으로 매칭하면 안 되는 이유

### 9-1. 브랜드 정합

공정위 실적 API(15110241)에는 **브랜드관리번호가 없다.** 명칭만 있다. 그런데 명칭은
리브랜딩·표기변경·동명이인으로 흔들린다.

해법: 15125467(등록 정보)에서 관리번호를 가져와 **관리번호 우선** 정합.

| 지표 | 값 |
|---|---|
| 원천 행수 | 76,846 |
| 유효 행수 | 76,843 (정규화 불가 3건 제거) |
| **관리번호 확보율** | **97.77%** |
| 고유 브랜드 | 20,626 |
| — 관리번호 기반 | 19,698 |
| — 명칭 기반 (관리번호 없음) | 928 |
| 모호 관리번호 무효화 | 1,039 |

**교차검증:** 독립 API 간 가맹점수 일치율 **98.9%**.

### 9-2. 가맹본부 ↔ DART 법인 정합 — 이름을 믿지 않는다

법인명 매칭은 **후보 생성에만** 쓰고, **사업자등록번호·법인등록번호 대조로 확정한
것만** 채택한다.

| 판정 | 건수 |
|---|---|
| 확정 | **700** |
| 불일치 (이름만 같은 다른 법인) | **812** |

**이름만 믿었으면 54%가 오매칭이었다.** 실제로 "우리개발"이라는 이름의 법인이 DART 에
7개 있고 사업자번호가 맞는 건 하나뿐이다.

이 단계에서 UI 가 브랜드명 문자열로 본부를 찾아 `dart.py` 가 막은 오배정을 되살리고
있던 결함도 발견해 수정했다(→ 제7부 §38).

---

## 10. 패널 구축과 생존성 진단

### 10-1. 단계별 감소 — 숨기지 않고 표로 공개

`outputs/survival_report.csv`

| 단계 | 행수 | 브랜드 수 | 연도 범위 | 매출 보유율 |
|---|---|---|---|---|
| ① 전체 (정합 후) | 76,717 | 20,626 | 2016~2024 | 39.8% |
| ② 업종 필터 (외식) | 60,478 | 16,406 | 2016~2024 | 39.1% |
| ③ + 실시간 자격 (점포 30+ 누적 & 3년 연속) | **7,544** | **1,809** | 2018~2024 | **92.7%** |

자격 필터가 행의 87.5% 를 떨어뜨린다. 이것은 이 프로젝트가 가장 많이 지적받는
한계이며, 뒤에서 **정면으로 다룬다**(→ 제3부 §16, 제8부 §41).

동시에 주목할 것: 매출 보유율이 39.1% → **92.7%** 로 뛴다. 자격 표본은 데이터가
실제로 있는 구간이다. 커버리지와 데이터 품질이 정면으로 상충한다.

### 10-2. 실시간 자격 — 룩어헤드 선별을 막는다

초기 구현에는 **표본 선별 룩어헤드**가 있었다. "최종적으로 점포 30개 이상이었던
브랜드"를 고르면 미래를 보고 표본을 만든 셈이다.

수정: **누적 최대(cummax)** 와 **연속 구간**으로 시점 t 에서 판정 가능한 자격만 쓴다.

```
eligible_t = (t 시점까지의 누적 최대 점포수 ≥ 30) AND (t 시점까지 연속 관측 ≥ 3년)
```

### 10-3. 표본 이탈 회계 — 생존편의의 크기를 잰다

모형은 t+1 행이 없는 브랜드를 조용히 뺀다(inner merge). 그런데 **t+1 이 없다는 것
자체가 나쁜 소식**일 수 있다. 그래서 세어서 공개하되 **라벨은 바꾸지 않는다**
(`tools/attrition_report.py`).

| 지표 | 값 |
|---|---|
| 패널 브랜드 | 16,406 |
| 소멸 브랜드 | **7,073** |
| — 그중 등록취소 공식 기록 보유 | **5,620 (79.5%)** |
| 등록취소 사건 총계 | 10,229 (자진 7,759 · 직권 2,470) |
| 연도별 소멸률 | 6.9~19.4% |

**패널 이탈이 실제 등록말소와 잘 대응한다**(79.5%)는 것은 데이터 품질의 방증이다.

**라벨에 넣지 않는 이유**도 기록했다:
1. 우측절단으로 추가 양성률이 train 0.76% vs test 1.86% 로 **2.4배 갈려** 백테스트가 무효화된다
2. 자진취소가 **76%** 라 부실과 동일시할 수 없다
3. 마지막 관측 연도 직전 해는 갭복귀를 볼 수 없어 소멸 수가 상한임을 `censored` 열로 표기

**등록취소를 패널에 연도 플래그로 붙이지 않은 이유**도 실측 근거가 있다. 접수연도에
그 브랜드의 패널 행이 이미 없어 대다수가 안 붙고(원본 10,441건 중 317행만 매칭),
연도키를 빼고 붙이면 **2023년 취소가 2019년 행에 얹혀 미래 정보가 과거로 샌다.**

---

## 11. 커버리지 — 세 기준으로 병기

한동안 우리는 **브랜드 수 15.5%** 하나만 적었다. 그것은 **스스로를 실제보다 낮게
보고하는 것**이었다.

| 기준 | 값 | 계산 |
|---|---|---|
| 브랜드 수 (외식) | 15.5% | 1,442 / 9,333 |
| **점포 가중 (외식 내)** | **73.6%** | 135,035 / 183,399 |
| 점포 가중 (전 업종) | 35.6% | 135,035 / 379,430 |

**외식 가맹점 4곳 중 3곳이 우리가 점수를 가진 브랜드에 속한다.** 여신 관점에서
의미 있는 것은 브랜드 개수가 아니라 점포(=차주) 수이므로, 점포 가중이 더 적절한
지표다. 세 기준을 모두 적어 어느 쪽으로도 오해가 없게 했다.

**덤으로 나온 교차검증:** 우리 패널의 전 업종 점포 합계 **379,430** 이 공정위 공표
**379,739** 와 **99.92% 일치**한다. 수집 누락이 사실상 없다는 뜻이다.

### 11-1. 층별 커버리지 — 무엇이 얼마나 비어 있는가

| 층 | 브랜드 기준 | 점포 가중 |
|---|---|---|
| 공시 패널 (핵심 지표) | 100% | 100% |
| 본부 재무 (DART) | **11.4%** | **39.7%** |
| 검색 수요 (네이버) | 20.8% (292개) | — |
| 뉴스 | 9개 브랜드 | — |
| 브랜드 로고 | 42.4% (611개) | 67.7% |

본부 재무 11.4% 가 최대 공백이다. 이것 때문에 **본부재무를 독립 부문으로 세우지
않았다** — 자료 없는 브랜드가 좋아 보이거나 나빠 보이는 왜곡이 생기기 때문이다
(→ 제3부 §22).

---

## 12. 개인정보 — "0" 이라고 적었던 것이 틀렸다

데이터 출처 절의 제목이 오랫동안 **'개인정보 0'** 이었다. 사실이 아니었다.

공정위 15125467 응답의 `jnghdqrtrsRprsvNm`(가맹본부 대표자 성명)이
`data/raw/brand_master_*.json` **84,368행**에 그대로 들어 있다.

| 항목 | 실태 |
|---|---|
| 원본 스냅샷에 포함 | ✅ 대표자 성명 84,368행 |
| 코드에서 참조 | **0건** |
| 파생 산출물로 전파 | **0건** |
| 화면 노출 | **0건** |

결과에는 영향이 없지만, **"없다"고 적은 것은 틀렸다.** 실태 표로 대체하고 틀렸던
사실을 그대로 남겼다. 대응은 **저장소 비공개 유지**다 (이력 재작성은 하지 않는다).

---
---

# 제3부 · 모형

## 13. 라벨 설계

### 13-1. 사건 정의 (학습 전 동결)

`config.yaml` 에 고정, 커밋 `cfcf164` 로 학습 이전에 동결.

| 사건 | 지표 | 분위 | 방향 |
|---|---|---|---|
| `ev_store` 점포 순감소 | `store_growth_rate` | 하위 20% | lower |
| `ev_sales` 실질매출 감소 | `real_sales_growth` | 하위 20% | lower |
| `ev_contract` 계약종료 급등 | `contract_end_rate` | 상위 20% | upper |

**양성 조건:** 2개 이상 동시 발동 **또는** 1개가 극단(5% 분위) 진입

**게이트:**

| 게이트 | 값 | 왜 |
|---|---|---|
| `min_abs_store_change` | 3 | 점포 12개 브랜드에서 1개 감소가 −8.3% 로 잡히는 노이즈 차단 |
| `min_stores_at_t` | 10 | 소표본 비율의 분산 폭발 차단 |
| `healthy_gate_at_t` | true | **t 시점에 이미 악화 중인 브랜드는 학습 표본에서 뺀다** |
| `deflate_by` | 업종 중앙값 | 업종 전체가 나빠진 것과 이 브랜드가 나빠진 것을 구분 |

`healthy_gate_at_t` 가 이 프로젝트에서 가장 큰 파장을 일으킨 설정이다 (→ §17).

### 13-2. 결과 구성 (`outputs/label_composition.csv`)

**학습·검증 표본 (실시간 자격 통과):**

| 항목 | 값 |
|---|---|
| 표본 | **3,635행** (브랜드 1,250개) |
| 양성률 | **9.22%** (335건) |
| 연도별 양성률 | 7.9~11.8% |

| 사건별 발동률 | 값 |
|---|---|
| `ev_store` | 10.59% (385) |
| `ev_sales` | 12.19% (443) |
| `ev_contract` | 8.45% (307) |

| 중복 분포 | 값 |
|---|---|
| 0건 | 76.5% (2,782) |
| 1건 | 16.9% (616) |
| 2건 | 5.3% (192) |
| 3건 | 1.2% (45) |

> **자격 필터 이전 프레임**(7,163행 · 양성률 11.4%)은 `label_composition_pregate.csv`
> 에 따로 기록한다. 한동안 이 둘이 한 파일에 뒤섞여 두 문서가 서로 다른 숫자를
> 인용하는 모순이 있었다 (→ 제7부 §38-24).

### 13-3. 어느 사건이 잡히는가 (`label_decomposition.csv`)

상위 10% 컷에서 유형별 포착률:

| 사건 유형 | 양성 | 포착 | 재현율@10% |
|---|---|---|---|
| 전체 | 60 | 16 | 26.7% |
| `ev_store` | 41 | 13 | 31.7% |
| `ev_sales` | 30 | 6 | **20.0%** |
| `ev_contract` | 43 | 14 | 32.6% |

**매출 감소가 가장 안 잡힌다.** 숨기지 않고 기록한다 — 매출 결측률이 높은 것과
관련이 있을 가능성이 크다(피처 결측 §14-2).

---

## 14. 피처 49개

### 14-1. 구성

| 접두 | 그룹 | 개수 | 예시 |
|---|---|---|---|
| `f_lvl_` | 수준 | 7 | 점포수, 평균매출(log), 직영비중, 업력, 면적당매출(log), 사업연차, 직원수(log) |
| `f_chg_` | 변화 | 6 | 점포 성장률, 매출 성장률, 계약종료율, 신규개점률, 명의변경률, 면적당매출 증가율 |
| `f_trd_` | 추세 | 9 | 3년 평균·표준편차·기울기 × (점포성장 / 매출성장 / 면적당매출) |
| `f_ind_` | 업종상대 | 3 | 점포성장·실질매출성장·계약종료 백분위 |
| `f_struct_` | 구조 | 10 | 직영비중 변화, 개폐점 갭, 진출지역수, 지역 HHI, 최다지역 비중, 지역당 점포수 |
| `f_hq_` | 본부재무 | 14 | 자기자본비율, 부채비율, 영업이익률, 순이익률, ROA, 매출(log), 매출성장, 적자연속, 영업적자, 자본잠식, 자본잠식(완전), 계속기업, 재무보유여부, 자료 노후도 |

**죽은 피처 0개** — 전 피처가 산출물에서 실제로 값을 갖는다.

### 14-2. 결측을 숨기지 않는다

| 피처 | 결측률 |
|---|---|
| `f_lvl_n_stores` | 0.7% |
| `f_lvl_brand_age` | 0.0% |
| `f_lvl_avg_sales_log` | **60.9%** |
| `f_lvl_sales_per_area_log` | **61.3%** |
| `f_chg_sales_growth` | **73.7%** |
| `f_trd_sales_growth_mean` | **81.6%** |

매출 계열의 결측이 심하다. 이것은 공시 특성이며(소규모 브랜드가 매출을 안 낸다),
그래서 §13-3 에서 매출 사건 포착률이 낮게 나온다. LightGBM 은 결측을 분기에서
직접 처리하므로 대체값을 만들어 넣지 않는다 — **없는 값을 지어내면 그 자체가 신호가
된다.**

### 14-3. 윈저라이즈 — 이진 플래그를 제외한 이유

연도별 상위·하위 1% 절단을 적용한다. 그런데 초기 구현이 **이진 플래그에도** 적용해,
발생률 0.1% 인 `f_hq_going_concern`(계속기업 불확실성)이 상위 1% 절단에 걸려
**전부 0으로 지워질 상태**였다. 이진 컬럼을 제외하도록 수정했다.

---

## 15. 시점 누출 방지 — 주장이 아니라 검사

이 프로젝트에서 가장 공을 들인 부분이다. 백테스트 성능은 누출 하나로 통째로 거짓이 된다.

### 15-1. bit-exact 불변성 검사

`tests/test_sanity.py::time_leakage` 는 이렇게 동작한다:

1. 합성 패널을 만든다
2. **t+1 이후의 모든 수치 컬럼을 교란**한다
3. (brand, t ≤ 2022) 피처 행이 **bit-exact 동일**한지 assert

하나라도 달라지면 그 피처는 미래를 보고 있다. 검증 컬럼 수는 사고를 겪으며
**27 → 40 → 49** 로 늘었다.

### 15-2. 커버리지 가드 — 테스트가 검사하지 않는 것을 검사한다

누출 테스트에 사각지대가 두 번 있었다.

**사고 ①** 합성 패널이 신규 9개 컬럼을 만들지 않아 해당 피처 분기가 **검증 범위 밖**
이었다. → `panel.PANEL_VALUE_COLS` 단일 정의 + 커버리지 가드 2종 신설
(합성 패널 컬럼 누락 검사, 교란 대상 누락 검사).

**사고 ②** 그 커버리지 가드가 **항상 실패**해 52개 전 피처의 누출 검증이 통째로
미실행 상태였다. 합성 픽스처가 커버리지를 모사하도록 고쳐 복구.

두 번째가 특히 뼈아프다 — **가드를 넣었는데 그 가드가 고장 나서 아무것도 검증되지
않고 있었다.** 이후 창업비용 5종을 `PANEL_VALUE_COLS` 에 추가했을 때 이 가드가
**즉시 교란 대상 누락을 검출**했다. 설계대로 작동한 것이다.

### 15-3. 피처 스키마가 미래에 의존하던 결함

분산 0 컬럼을 **사후에** 버리는 방식이라 t+1 값에 따라 컬럼 집합이 달라졌다.
누출 테스트가 이것을 검출했다. 업종 더미를 범주 2개 이상일 때만 생성하도록
구조를 바꾸고, 상수 컬럼은 제거가 아니라 **경고로 보고**하도록 고쳤다.

### 15-4. 누출 점검 체크리스트

`docs/LEAKAGE_CHECKLIST.md` 에 항목별로 "무엇을 어떻게 확인했는가"를 대응 테스트와
함께 표로 정리했다. 검증자가 개발자의 주장을 믿지 않고 직접 돌릴 수 있다.

---

## 16. 학습과 기준모형

### 16-1. 시간 분할

```
train  2018~2021 (2,206행)   valid  2022 (689행)   test  2023 (740행)
base rate:  8.79%                   11.76%              8.11%
```

연도 교집합 없음, test = 최대 연도, valid = test−1. 테스트로 검사한다.

### 16-2. 복잡도 선택은 valid 로만

사전 후보 3개를 valid AUC 로 선택한다. **test 는 보지 않는다.**

| 후보 | valid AUC | best iter |
|---|---|---|
| num_leaves 7, min_child 40 | 0.7771 | 62 |
| num_leaves 15, min_child 30 | 0.7723 | 30 |
| **num_leaves 31, min_child 20** | **0.7791** | **25** |

조기종료 지표를 logloss → **valid AUC** 로 바꿨다(`first_metric_only`). 불균형
데이터에서 logloss 조기종료는 순위 성능과 어긋난다.

### 16-3. 기준모형 3종 — 이기기 어려운 상대를 고른다

| 기준모형 | 무엇 |
|---|---|
| `persistence` | 전년 상태 유지 — "작년에 나빴으면 올해도 나쁘다" |
| `single_var_contract_end` | 계약종료율 **단일 변수** |
| `logistic` | 로지스틱 회귀 (class_weight=balanced) |

**약한 기준모형을 세워 이기는 것은 의미가 없다.** `persistence` 는 이 문제에서
실제로 강력한 함정이고, 단일변수 계약종료율은 도메인 상식에 해당한다.

### 16-4. 단일분할 성능 (test 2023, n=740, base 8.11%)

| 모형 | Lift@10% | Precision@10% | PR-AUC | ROC-AUC | Brier |
|---|---|---|---|---|---|
| persistence | 1.500 | 0.122 | 0.116 | 0.545 | 0.096 |
| single | 2.167 | 0.176 | 0.141 | 0.637 | 0.232 |
| logistic | 1.833 | 0.149 | 0.162 | 0.726 | 0.195 |
| **lgbm** | **2.667** | **0.216** | 0.171 | 0.723 | 0.081 |
| lgbm (보정) | 2.667 | 0.216 | 0.171 | 0.723 | **0.075** |

**train Lift 9.25 → test 2.67.** 과적합 폭을 그대로 공개한다.

### 16-5. 워크포워드 확장창 3-fold (OOS n=2,063)

| fold | test | persistence | single | logistic | **lgbm** |
|---|---|---|---|---|---|
| 1 | 2021 | 1.811 | 2.415 | 2.617 | **2.818** |
| 2 | 2022 | 1.751 | 2.627 | **3.127** | 2.752 |
| 3 | 2023 | 1.500 | 2.167 | 1.833 | **2.667** |
| **macro 평균** | — | 1.688 | 2.403 | 2.472 | **2.456** |

### 16-6. 우위 주장 — 하나만 한다

| 비교 | 평균 격차 | 95% CI (연도블록 부트스트랩) | 판정 |
|---|---|---|---|
| lgbm − persistence | **+1.059** | **[1.003, 1.115]** | **유의** |
| lgbm − single | +0.348 | [0.218, 0.468] | (아래 주의) |
| lgbm − logistic | +0.233 | [−0.183, 0.628] | **동등** |

**우리는 persistence 대비 우위 하나만 주장한다.**

`lgbm − single` 의 부트스트랩 CI 는 0 을 넘지만, **블록 3개짜리 백분위 부트스트랩의
산물**이다. fold 값에서 직접 낸 t-구간은 **[−0.141, +0.826]** 로 0 을 포함하고,
같은 저장소의 `lift_delta_bootstrap.csv` 도 [−0.893, +1.589] 비유의다.

> **두 산출물이 모순될 때 좁은 쪽을 싣는 것은 정직하지 않다.**
> 그래서 "단일변수 대비 유의하게 우위" 주장을 **철회**하고 fold 값을 공개했다.
> LightGBM 을 유지하는 근거는 성능 우위가 아니라 **SHAP 설명력**이다.

### 16-7. 그 밖의 정직성 보고

**보정 공정 비교** — isotonic 을 LightGBM 에만 적용해 놓고 "Brier 최우수"라고 하던
것은 불공정 비교였다. 기준모형 3종에도 동일 보정을 적용해 공개:

| 모형 (보정 후) | Brier |
|---|---|
| lgbm | 0.075 |
| logistic | 0.080 |
| persistence | 0.078 |
| single | 0.078 |

차이가 미미하므로 **"Brier 최우수"를 다변량 모형의 주된 실익으로 내세우던 문구를
철회**했다.

**부분표본 (`newbrand_split.csv`)** — 신생/기존 브랜드로 나누면 LightGBM 이
**양쪽 모두에서 1위가 아니다.** 명시했다.

| 부분표본 | 1위 |
|---|---|
| 기존 브랜드 (n=529) | logistic (2.637) |
| 신생 브랜드 (n=211) | persistence (2.131) |

**절제 실험 (`ablation.csv`)**

| 단계 | 피처 수 | Lift@10% |
|---|---|---|
| 수준만 | 7 | 2.000 |
| +변화 | 13 | 2.500 |
| +추세 | 22 | 2.000 |
| +업종상대+구조 | 35 | 2.167 |
| **전체 (+본부재무)** | **49** | **2.667** |

**수준+변화 13개만으로 2.500 이 나온다.** 전체 49개의 2.667 과 크게 다르지 않다.
이 사실을 해석과 함께 명시했다 — 피처를 늘린 것의 한계효용이 작다는 뜻이다.

### 16-8. 워크포워드 집계 편향 — 실측으로 발견해 고친 것

풀링 Lift 가 **모든 개별 fold 보다 낮은** 모순으로 검출했다.

원인: fold 마다 재학습하므로 출력 확률 스케일이 다르다(LGBM 최대확률
0.80 / 0.84 / 0.57). 원점수를 전역 정렬하면 2023 fold 가 행의 35.9% 인데 상위 10%
의 **4.4%** 만 차지한다(편향배수 0.12). **재학습 모형에 불리하고 고정공식 기준모형에
유리한 인위적 편향**이다.

수정: 상위 k% 지표는 **fold 안에서** 계산. 주 지표를 `macro_avg_folds` 로 바꾸고,
rank 정규화 풀링·원점수 풀링·**편향 진단표**(`walkforward_pool_bias.csv`)를 모두
함께 공개. 부트스트랩도 연도블록 재표집 후 fold 내 계산·평균 방식으로 수정.

---

## 17. 개발표본 ≠ 적용모집단 — 가장 아팠던 발견

### 17-1. 문제

라벨은 `healthy_gate_at_t=true` 로 만들어져, `labels.parquet` 3,635행이 전부
**'그 해 악화사건 0건'** 브랜드다. 그런데 점수 산출은 `eligible_t` 만 걸고 게이트를
걸지 않는다.

실측 (2024 코호트 1,442개):

| 항목 | 값 |
|---|---|
| 모델이 본 적 없는 모집단 | **550개 (38.1%)** |
| 주의(FS3) 367개 중 요주의 출신 | **271개 (73.8%)** |

**이들에 대한 성능 근거는 원리적으로 만들 수 없다** — `labels.parquet` 에 행이 없어
어떤 백테스트도 불가능하다. 보고된 Lift@10·AUC 는 전부 게이트 통과분 측정값이다.

### 17-2. 1차 대응 — 숨기지 않는다

`scores_latest.csv` 에 상태 컬럼을 추가하고 화면에 구분 배지를 노출했다.
숨기면 심사역이 두 트랙을 **같은 근거의 '주의 등급'** 으로 읽는다.

브랜드 상태 3분류 (`labels.brand_state`):

| 상태 | 정의 | 2024 코호트 |
|---|---|---|
| 건전 | 그 해 악화사건 0건 | 892 |
| 요주의 | 그 해 악화사건 1건 이상 | 496 |
| 평가불가 | 판정 지표 관측 불가 | 54 |

초기 프록시(`in_model_population`)는 **직전 연도 라벨 표본**을 봐서 t−1 상태를 재고
있었다(50.9% 로 과소 보고). 산출연도 상태를 직접 판정하도록 고쳤다.

### 17-3. 2차 대응 — "원리적으로 불가능"은 과장이었다

요주의 구간은 AUC·Lift 를 잴 수 없다. 그러나 **상태 전이는 패널에 그대로 있어
실현율은 셀 수 있다.** 실제로 셌다 (`tools/watch_base_rates.py`).

| 상태 | t 시점 사건수 | n | 재발동 | **실현율** | Wilson 95% CI |
|---|---|---|---|---|---|
| 건전 | 0 | 3,657 | 342 | **9.4%** | [8.5, 10.3] |
| 요주의 | 1 | 1,218 | 293 | **24.1%** | [21.7, 26.5] |
| 요주의 | 2 | 536 | 246 | **45.9%** | [41.7, 50.1] |
| 요주의 | 3 | 125 | 81 | **64.8%** | [56.1, 72.6] |

한 줄로: **9.4% / 24.1% / 45.9% / 64.8%** — 건전 대비 **2.6배 / 4.9배 / 6.9배**.

방법론상 주의점을 함께 못박았다:

- 점수 산출과 **동일한** `eligible_t` 게이트 적용
- t+1 판정불가 17건은 0 으로 세지 않고 **제외**
- Wald 가 아닌 **Wilson** 구간 (소표본에서 Wald 는 구간이 잘못된다)
- **이 표는 순위를 매기지 않는다** — 같은 사건수 안에서는 모두 같은 확률이며,
  그것이 이 구간에 대해 아는 전부다

### 17-4. 요주의 전용 모형을 만들어 보고 — 기각했다

요주의 구간 전용 LightGBM 을 실제로 학습했다. 결과:

```
요주의 전용 LightGBM   워크포워드 macro AUC  0.6937
계약종료율 단일변수                          0.7066
```

**변수 14개가 1개보다 못하다.** 그래서 모형을 기각하고 실현율 공표로 대체했다.
만들 수 있다고 해서 만드는 것이 아니라, **더 나은 것이 있으면 그것을 쓴다.**

---

## 18. 확률 보정 — 세 번 고쳐 만든 것

### 18-1. 1차 문제: PD 포화

isotonic 보정이 정확히 **1.000(3행)** 과 **1.4e-08(830행)** 까지 포화해, 대시보드에
"1년 악화확률 100.0%" 가 표시되고 EL 계산까지 과신했다.

→ 여신 실무 관행대로 하한 **0.03%**(바젤 IRB 준용)·상한 **99%** 클립.
단조 변환이라 순위 지표(Lift@k·PR-AUC·ROC-AUC)는 불변, Brier·EL 만 보수화.

### 18-2. 2차 문제: 확률 계단

isotonic 산출은 계단 함수라 2,510개 중 **216개가 42.86% 로 같은 확률**을 달았다.
심사역이 순서를 못 매긴다.

→ `smooth_calibrated`: 계단 내부를 원점수 순위로 **대칭 분산**한다. 계단 평균을
정확히 보존하므로 보정 품질이 유지된다.

| 지표 | 변화 |
|---|---|
| 평균확률 | 0.10071 → 0.10071 (동일) |
| ECE | 0.03612 → 0.03612 (동일) |
| Lift@10 | 2.667 → 2.667 (동일) |
| 순위상관 | 1.000000 |
| 서로 다른 값 | 17 → **511개** |

**단, 계단 내부 순서에는 판별력이 없다** — 이 저장소가 직접 측정했다
(AUC 0.521, p=0.69). 그래서 표시값은 보조로만 쓰고 **등급은 계단값으로 매긴다**
(→ §19-3).

### 18-3. 3차 문제: 정확성 검정 불합격 — 원인은 기저율이 아니라 척도였다

초기 검증 결과가 **불합격**이었다:

```
HL χ² = 25.5, df=6, p = 0.00027      기각
Spiegelhalter Z = +3.41              과소예측 (예측 7.47% vs 실제 9.87%)
등급별 이항검정  FS2 ratio 1.45 (p=0.0006) · FS3 1.33 (p=0.047)   둘 다 불일치
```

**과소예측은 은행에 불리한 방향**이므로 숨기지 않고 그대로 실었다. 그 다음 원인을 팠다.

처음 가설은 "기저율 상승을 선행 연도 보정이 못 따라간다"였다. **틀렸다.**
2023 fold 의 선행 기저율은 9.90% 인데 실제는 8.11% — 기저율만으로는 **과대**예측이
나야 한다.

진짜 원인: **워크포워드 fold 원점수 평균이 0.2826 / 0.3306 / 0.1698 로 44.8%
벌어진다.** fold 마다 재학습하기 때문이다. 높은 척도에서 적합한 보정기를 낮은 척도에
적용하면 등온회귀 곡선 아래쪽으로 매핑돼 과소예측하고, 그 효과가 기저율 방향을
뒤집었다.

**수정 세 가지:**

1. **운영 모형 점수로 보정한다** — 모형이 하나라 척도가 안정적이다
   (0.1752 / 0.1853 / 0.1698). 학습 연도는 제외한다 — 포함하면 Z=+28.9,
   비율 2.96 으로 파탄한다(실측).
2. **계단별 경험적 베이즈 축소** (k+αB)/(n+α). 등온회귀 극단 계단이 소표본에서
   불안정하다(최하위 구간 예측 0.46% vs 실제 2.10%).
3. **α 는 보정표본 안에서만 K-겹 CV 로 고른다.** 검증 연도를 보고 고르면
   검정에 하이퍼파라미터를 맞추는 것이다. 선택 α = 10.

**순위 기반 보정은 배제했다.** fold 척도 문제를 깨끗이 없애고 Spiegelhalter 도
통과하지만, p = g(코호트 내 순위) 가 되어 **등급 컷이 다시 순위 컷이 된다** —
절대 등급으로 바꾼 이유가 통째로 무효화된다.

**HL 구현 결함도 함께 수정.** 구간을 값 분위로 자르고 있었는데 등온회귀 산출은
계단이라 경계가 계단값과 겹쳐 그룹이 극단적으로 불균등해진다. 같은 자료에서
값 분위 p=0.00000 vs 순위 등분할 p=0.018 — **세 자릿수 차이가 오직 구간 방식에서
나왔다.**

### 18-4. 결과

| 검정 | 이전 | 이후 |
|---|---|---|
| Hosmer–Lemeshow | χ²=25.5, p=0.00027 **기각** | χ²=11.85, df=8, **p=0.158 통과** |
| Spiegelhalter | Z=+3.41 과소예측 | **Z=-1.077 통과** (p=0.282) |
| 등급별 이항검정 | FS2·FS3 불일치 | **3/3 정합** |
| ECE | 0.0314 | 0.0354 |
| 예측 vs 실제 | 7.47% vs 9.87% | 9.97% vs 8.11% |

남은 편차는 **과대예측** 방향이다 — 보수적인 쪽이다.

### 18-5. 보정기 결함 2건 — 설명 층을 설계하다 발견

**결함 1: 계단 조회 실패 → 조용히 기저율로 대체**

`ShrunkIsotonic.predict()` 가 `mapping.get(계단값, self.base)` 로 조회했다. 그런데
`IsotonicRegression.predict()` 는 학습 knot **사이** 입력에 선형 보간값을 돌려준다 —
그 값은 mapping 의 키가 아니다. 조회 실패 시 **아무 경고 없이** 기저율 0.0987 로
대체됐다.

실측(2024 코호트): 14행이 이 경로를 탔고 그중 **12건의 등급이 틀렸다.
11건이 FS2 로 표시되었으나 실제로는 FS3** — **위험을 낮게 보여주고 있었다.**

→ `searchsorted` 로 계단 함수를 제대로 평가. floor 를 쓰는 이유는 각 계단이
실현율을 **실제로 관측한 표본 집단**이기 때문이다.

**결함 2: 축소 보정이 단조성을 깼다**

계단별로 따로 축소하므로 순서가 보장되지 않는다. 표본이 적은 낮은 계단은 기저율
쪽으로 세게 끌려 올라가 바로 위 계단을 추월한다.

```
계단 0.000000 → 축소 0.013156
계단 0.005814 → 축소 0.010916     ← 원점수가 더 나쁜 브랜드가 더 낮은 확률
```

→ 적합 시점에 **가중 PAV 로 단조 사영**. 누적최대로 밀어 올리지 않는 이유는 그러면
위반한 쪽만 일방적으로 올라가 기대 사건수가 부풀기 때문이다. PAV 는 합친 구간의
가중평균이라 **표본 전체의 기대 사건수를 보존**한다.

**회귀 방지:** `tests/test_calibration.py` 신설 (6종) — 계단 밖 출력·기저율 대체·
보정확률 단조·등급 단조·화면 등급 == 검증 척도·표시값 구간 이탈.
**예외도 로그도 없이 조용히 틀리는 종류의 결함이라 사람이 아니라 테스트가 잡아야 한다.**

---

## 19. 등급 체계

### 19-1. 순위 백분위를 폐기한 이유

초기 등급은 "상위 10% = High" 식 순위 컷이었다. 폐기했다. 같은 데이터·같은 운영
모형에 **등급 규칙만** 바꿔 적용해 측정했다(3,576행 / 라벨 2,063행, 2021~2023).

| 비교 항목 | 절대 컷 (신) | 순위 컷 (구) |
|---|---|---|
| **경기 대응성** — 최상위 등급 비중 연도별 | 24.5 / 28.1 / 25.3% (sd **0.0188**) | 10.04 / 10.06 / 10.02% (sd **0.0002**) |
| **무변화 이동** — \|Δp\|≤1e-6 인 518쌍 중 등급 이동 | **0건 (0.0%)** | 28건 (5.4%) |
| **등급 구분** — 인접 CI 분리 | **2/2** | 1/2 |
| 단조성 | 3/3 | 3/3 |
| **큐 효율** — 코호트 비중 → 사건 포착 | 13.1% → **54.5%** (정확도 38.5%) | 5.1% → 23.6% (40.9%) |

두 가지가 결정적이다:

1. **순위 체계는 구조적으로 10%에 못 박혀 있다.** 업계가 전반적으로 나빠져도 최상위
   등급 비중이 늘지 않는다 — 경기 신호를 원리적으로 전달할 수 없다.
2. **자기 위험이 그대로여도 남이 움직이면 등급이 바뀐다** (28건). 심사역에게
   설명 불가능한 등급 변동이다.

### 19-2. 절대 컷과 실현율 실적표

컷: **[0.045, 0.16]**

| 등급 | 명칭 | n | 사건 | 실현율 | Clopper–Pearson 95% CI |
|---|---|---|---|---|---|
| **FS1** | 안정 | 631 | 14 | **2.22%** | [1.22, 3.69] |
| **FS2** | 관찰 | 616 | 72 | **11.69%** | [9.26, 14.49] |
| **FS3** | 주의 | 182 | 55 | **30.22%** | [23.65, 37.45] |

최소 CI 분리 **5.56%p**. 검증 표본 n=1,429 · 사건 141.

**컷 선택 규칙 (사전 선언):**
- 등급별 최소 표본 150
- 실현율 단조 증가
- 인접 Clopper–Pearson 95% 구간 **비중첩**
- 검증 전 연도에서도 단조성 유지

### 19-3. 등급을 어느 척도로 매기는가 — 48개 브랜드가 틀려 있었다

컷 [0.045, 0.16] 은 보정기의 **계단값** 위에서 도출됐다. 공표 실현율도 그 척도의
실적이다. 그런데 `src/score.py` 는 그 컷을 **보간값**에 적용하고 있었다.

실측: 계단 0.159823 에 **98개 브랜드**가 앉아 있고 컷이 0.16 이라, 보간이 그중
**48개를 컷 위로 밀어 올려 주의(FS3)로 만들었다.** 그 브랜드들의 실제 추정 확률은
컷 아래다. 더구나 계단 내부 순서는 이 저장소가 직접 측정해 **판별력이 없다고 결론
낸 값**이다 — 신호가 없는 양이 등급 경계를 넘기고 있었다.

→ 등급은 `deterioration_step`(계단값)으로 매긴다. 표시값은 자기 등급 구간 안으로
되돌린다(48행 클립, 순서는 보존).

**검증:** 화면 등급 == 검증 등급 불일치 **0건** / 구간 밖 표시 **0건** / 표시값 897종.

### 19-4. 최종 등급 분포 (2024 코호트 1,442개)

| 등급 | 명칭 | 브랜드 수 |
|---|---|---|
| FS1 | 안정 | **443** |
| FS2 | 관찰 | **632** |
| FS3 | 주의 | **367** |

---

## 20. 등급 컷의 순환논증을 제거했다

### 20-1. 지적

배포 컷은 2022+2023 풀에서 '분리폭 최대'로 골랐고, 연도별 단조 필터가 **2023 라벨을
이미 본다.** 그 뒤 2023 실현율을 '시점 밖 확인'으로 제시하면 **컷을 고를 때 본
데이터로 컷을 검증하는 셈**이다.

CI 비중첩·단조성은 **격자 탐색의 선택 제약**이므로, 그 제약을 만족하는 컷을 고른 뒤
"제약이 만족된다"고 말하는 것은 검증이 아니다.

### 20-2. 검증 3종으로 대체

**① 절차의 시점 밖 검증** — 검증 대상은 특정 컷 값이 아니라 **절차**다.

```
2022만 보고 같은 절차로 컷 선택  →  [0.035, 0.095]
그 컷을 무접촉 2023에 적용
```

| 등급 | n | 사건 | 실현율 |
|---|---|---|---|
| FS1 | 262 | 6 | 2.29% |
| FS2 | 275 | 24 | 8.73% |
| FS3 | 203 | 30 | 14.7% |

**단조 유지.** 배포 컷은 검증된 절차를 전체 자료에 적용한 결과다(표준 관행).

**② 라벨 치환 귀무검정** — 다중비교의 크기를 잰다.

연도 내 라벨을 무작위로 치환한 자료에 **같은 격자 탐색**을 200회 돌렸다.

```
조건(표본·단조·비중첩·연도별 단조)을 모두 만족하는 컷이 나온 횟수:  0 / 200
p < 0.005
```

관측 분리폭 **5.56%p** 는 격자 1,770쌍의 선택 효과만으로는 설명되지 않는다.

**③ 확률의 시점 밖 보정 확인** — 2022 적합 → 2023 적용에서 단조 유지 (§20-2 ①).

### 20-3. 명칭도 바로잡았다

pooled 실현율표를 **'검증'에서 '실적표(앵커)'로** 이름을 고쳤다. 그리고
보정 자료 중 **2022 가 운영 모형의 valid 연도**라는 사실을 `grade_bands.json` 과
`METHODOLOGY.md` 에 공시했다 — 그 해 점수의 분리력은 순수 OOS 보다 유리할 수 있다.
(절차 검증의 채점 연도는 2023 이므로 이 우려의 영향권 밖이다.)

**배포 컷은 [0.045, 0.16] 그대로 유지.** 검증 방식을 고쳤을 뿐 결론을 바꾸지 않았다.

---

## 21. 모형 검증 산출물 — 규정만 있고 측정이 없으면 그 규정은 장식이다

`docs/OPERATIONS.md` 가 PSI 임계 0.10/0.25 를 규정하는데 구현이 **0건**이었다.
`src/validation.py` 를 만들어 은행 실무자가 "검증표를 보여달라"고 했을 때 내놓을
것을 만들었다.

### 21-1. 판별력 — 통과

| scope | n | 사건 | AUC | Gini | KS | Lift@10% |
|---|---|---|---|---|---|---|
| **풀링** | 2,063 | 191 | **0.707** [0.671, 0.742] | 0.414 | 0.316 | **2.517** [1.98, 3.11] |
| 2021 | 634 | 50 | 0.677 | 0.354 | 0.296 | 2.818 |
| 2022 | 689 | 81 | 0.731 | 0.463 | 0.408 | 2.712 |
| 2023 | 740 | 60 | 0.723 | 0.446 | 0.391 | 2.667 |

신뢰구간이 무작위(0.5)를 배제한다.

### 21-2. 정확성 — 통과 (§18-4 참조)

### 21-3. 안정성 — 통과

**이행행렬** (이탈 열 포함)

| from→to | n | FS1 | FS2 | FS3 | **이탈** |
|---|---|---|---|---|---|
| 2021→22 FS1 | 364 | **65.1%** | 22.3% | 4.1% | 8.5% |
| 2021→22 FS2 | 445 | 17.3% | **48.1%** | 23.6% | 11.0% |
| 2021→22 FS3 | 267 | 4.5% | 33.7% | **51.7%** | 10.1% |
| 2022→23 FS1 | 350 | **65.7%** | 24.6% | 2.0% | 7.7% |
| 2022→23 FS2 | 514 | 21.4% | **46.3%** | 19.7% | 12.7% |
| 2022→23 FS3 | 339 | 3.5% | 36.9% | **44.5%** | 15.0% |

**대각 우세 6/6 충족.** 평균 이탈률 11.0%.

**PSI (점수)** — 운영 모형 원점수, 과거 코호트 → 배포 코호트

| 기준 연도 | PSI | 판정 |
|---|---|---|
| 2021 → 2024 | 0.0539 | 정상 |
| 2022 → 2024 | 0.0118 | 정상 |
| 2023 → 2024 | 0.0078 | 정상 |

**PSI (피처)** — 두 질문으로 분리했다. 합치면 원인을 못 가린다.

| 질문 | 정상 | 적색 | 경보 |
|---|---|---|---|
| 대표성 (개발표본 → 적용모집단) | 23 | **14** | 12 |
| 안정성 (동일 자격조건 시점 간) | — | **7** | — |

대표성 적색 14 는 `healthy` 게이트로 생긴 모집단 차이이고(§17), 안정성 적색 7 은
시간에 따른 실제 이동이다. **분리해서 봐야 처방이 다르다.**

### 21-4. 측정 과정에서 잡은 오류 3건 — 전부 "다른 것을 비교하고 있었다"

**① 자기 자신을 채점하고 있었다.** 보정 검정을 배포 보정기로 하니 HL p=1.0,
ECE=0.0000 이 나왔다. 그 보정기가 **같은 데이터로 적합됐기 때문**이다.
→ 시점 정합 확률로 교체.

**② PSI 1.41 의 정체.** 워크포워드는 fold 마다 재학습한 **서로 다른 모형 3개**의
점수를 섞은 것이고 2024 는 단일 운영 모형이다. PSI 는 **한 모형의 시점 간 이동**을
재는 지표다. → 운영 모형을 과거 코호트에 적용해 비교하도록 수정.
(보정 계단값(고유값 14~22개)으로 재면 분위 구간이 퇴화해 4.37 로 폭주하는 것도 확인.)

**③ FS3 유지율 42.2% → 6.0% 붕괴.** 브랜드가 옮겨간 것이 아니라 fold 모형의 원점수
척도가 달랐다(평균 0.28/0.33/0.17). → 운영 모형으로 통일하니 대각 우세 6/6 회복.

> **검증 지표는 잘못 계산하면 좋은 쪽으로 틀린다.** 실제로 세 번 그랬고, 세 번 다
> 원인이 같았다. 이 사실을 `docs/METHODOLOGY.md` 에 공개했다.

---

## 22. 부문별 진단 — 점수를 매기지 않기로 한 결정

브랜드를 6개 부문으로 나눠 부문 점수를 합성하는 설계를 검토했다. **3개만 남기고
부문 점수를 매기지 않기로 했다.**

| 부문 | 채택 | 사유 |
|---|---|---|
| 존속성 | ✅ | 배포 코호트 커버리지 91.5~100% |
| 계약 안정성 | ✅ | 〃 |
| 집중도 | ✅ | 〃 |
| 본부재무 | ❌ | 브랜드 커버리지 **11.4%** — 자료 없는 브랜드가 좋아 보이는 왜곡 |
| 시장수요 | ❌ | 지표가 전부 평탄한 순위값, 뉴스 23건 |
| 창업비용 | ❌ | 2024 공시에 **한 건도 없다** |

**그리고 부문에 점수를 매기지 않는다.** 여러 지표를 하나로 합치려면 가중치가
필요한데 **그 가중치를 정당화할 근거가 없다.** 관측값과 업종 내 위치만 보여주고
그 사실을 화면에 쓴다.

이 과정에서 표기 결함 2건도 잡았다:

1. 위치를 항상 "상위 {100−pct}%" 로 찍어서, 점포 성장률 **−46.1%**(실제로는 하위
   11%)가 "업종 내 상위 89%" 로 나와 **좋아 보였다.** → 절반을 기준으로 상위/하위를 나눈다.
2. 동점 처리가 없어 직영점 0개처럼 값이 몰린 지표의 백분위가 0으로 찍혔다.
   → 중간순위 ((s<v)+(s≤v))/2 로 변경.

---

## 23. 진단 엔진 — 34종 규칙이 만드는 한국어 소견

### 23-1. 왜 필요했나

심사 화면의 위험 근거가 SHAP 피처명 3개뿐이라 **상위 브랜드의 설명이 전부 같았다.**
"각 프랜차이즈를 낱낱이 분석한다"는 도구의 목적과 화면이 어긋나 있었다.

### 23-2. 설계

규칙 34종이 공시 이력·본부 재무·검색수요·보도를 훑어 **그 브랜드의 실제 수치를 담은
한국어 문장**으로 소견을 만든다. 업종 벤치마크(같은 연도·업종그룹 분위수)를 함께
넣어 비교 가능하게 쓴다.

```
"2023년 공시 기준 창업비용 합계 2.2억원은 한식 업종 상위 3% 수준입니다"
"한식 업종 상위 8%, 업종 중간값 4.6%"
```

### 23-3. 실측 (2024 코호트)

| 항목 | 값 |
|---|---|
| 브랜드 | 1,442 |
| 소견 | **9,371건** |
| 브랜드당 평균 | 6.51건 |
| 서로 다른 소견 조합 | **1,081종** |
| 대표 소견 종류 | 41종 |

상위 발동 규칙:

| 코드 | 건수 | 코드 | 건수 |
|---|---|---|---|
| HQ_NO_DATA | 1,269 | CONTRACT_END_HIGH | 630 |
| STORE_DECLINE | 827 | STORE_DECLINE_STREAK | 565 |
| SMALL_SCALE | 803 | YOUNG_BRAND | 491 |
| CHURN_EXCEEDS_OPEN | 710 | SALES_PER_AREA_DECLINE | 435 |

### 23-4. 설계 판단 3가지

**① 검증된 부분과 규칙 부분을 분리 기록한다.** 감시점수는
`det_component`(백테스트 검증 모델 순위)와 `rule_component`(규칙 정책)를 따로 남긴다.
**어디까지가 검증된 부분인지 산출물에서 드러나야 한다.**

**② 단순 합산을 쓰지 않는다.** 규칙 성분은 카테고리별 체감 합산 후 tanh 포화.
단순 합은 상관된 소견 3~5개가 겹쳐 **+126점까지 치솟아** 검증된 모델 성분(최대 100)을
압도했다.

**③ 조사(助詞)를 처리한다.** "3,879만원**는**"·"1.6억원**로**" 가 그대로 나갔다.
읽는 사람이 도구를 신뢰하지 않게 되는 종류의 결함이다.

### 23-5. 잠자는 규칙을 금지한다

`tests/test_diagnosis.py::no_dormant_rules` — **선언된 규칙이 산출물에서 실제로
발동하는지** 검사한다. 있는 척하는 기능을 막는다.

실제로 이 검사가 잡은 것: `STARTUP_COST_HIGH` 가 **원리적으로 발동 불가**였다.
대상연도(2024)의 `startup_total` 이 전 브랜드 결측(창업비용 공시가 그 해에 없음)이라
규칙이 코드에만 존재했다.

→ 창업비용은 해마다 바뀌지 않는 구조적 속성이므로 **대상연도 이하에서 가장 최근에
공시된 값**을 쓰고, 그 값의 연도를 문장에 명시하도록 고쳤다. 비교 분포도 같은 연도의
것을 쓴다(다른 해 분포와 비교하면 사과를 배와 비교하는 셈). → **185건 발동.**

---

## 24. 여신 포트폴리오 — 난수를 실측으로 바꾼 일

### 24-1. 처음에는 합성 난수였다

```
exposure = 점포수 × 5천만원 × LogNormal(0, 0.5)
```

브랜드 간 상대 규모가 **난수의 산물**이었다. 집중도(HHI)·손실 수치가 전부 가정 위에
서 있었다는 뜻이다.

### 24-2. 공시 실측을 발굴했다

**15110265 브랜드별 창업 금액 현황** — 정보공개서 필수 공시 항목이라 브랜드별
실측값이 존재한다(가맹금·교육비·보증금·기타 및 합계, 천원). 2017~2025년 각 연도
약 1.1만 브랜드.

```
exposure = 가맹점수 × 브랜드별 창업비용(공시 실측) × 대출조달비율 × 은행 점유율
             ↑ 실측        ↑ 실측                    ↑ 가정 0.6      ↑ 가정 0.15
```

**창업비용·가맹점수는 실측이고, 뒤 두 비율만 가정이다.** 난수를 쓰지 않으므로
브랜드 간 상대 규모와 집중도가 **실제 구조를 반영**한다.

- 예시 포트폴리오 60/60 브랜드 전부 실측 확보
- 점포당 창업비용 중앙값 **79.5백만원**
- 패널 병합률 67.3%(1차 정확키 61.5%), 결측은 업종 중앙값 대체(**난수 미사용**)

### 24-3. 3층 구조 — 은행 내부 실행 경로 포함

| 층 | 조건 | 근거 표기 |
|---|---|---|
| ① | `config.portfolio.exposure_source` 에 CSV 경로 | **실제 여신 데이터** |
| ② | 기본값 | 공시 창업비용 기반 추정 |
| ③ | 원천이 전혀 없을 때만 | 합성 폴백 |

산출 근거는 `portfolio.csv` 의 `exposure_basis` 컬럼과 요약 JSON 의
`assumptions.exposure`, 그리고 화면 상단 고지에 **행 단위로** 남는다.
**어느 층에서 나온 숫자인지 숨기지 않는다.**

은행 내부에서는 아래 3컬럼 CSV 한 장만 채우면 ① 층으로 전환된다:

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `brand_id` 또는 `brand_mnno` | str | 브랜드 식별자 (관리번호 권장 — 리브랜딩에 불변) |
| `exposure_mkrw` | float | 해당 브랜드 가맹점 여신 합계 (백만원) |
| `n_borrowers` | int | 차주(가맹점) 수 — 2단계 상관 모형의 분모 |

### 24-4. 수치 변화 — 금액이 크게 줄었지만 이쪽이 옳다

| 지표 | 합성 난수 | **공시 실측 기반** |
|---|---|---|
| 총 여신 | 4,954억 | **666억** |
| EL (LGD 45%) | 94.9억 | **15.3억** |
| 상위 10 집중도 | 68.6% | **62.2%** |
| HHI | 0.091 | **0.068** |

### 24-5. 현재 포트폴리오 (2023 test 브랜드 60개)

| 지표 | 값 |
|---|---|
| 총 여신 | 666억원 |
| HHI | 0.0683 |
| 상위 10 집중도 | 62.2% |
| 최대 단일 브랜드 | 15.1% |
| EL (LGD 30/45/60%) | 10.2 / **15.3** / 20.4억 |
| 스트레스 EL (상위 10% × 1.5) | 11.2 / **16.8** / 22.3억 |

---

## 25. 상관을 의사결정면에 배선했다 — Euler 성분 ES

### 25-1. 문제

ρ_W = 0.416 을 측정해 놓고 화면에는 **총량(UL 배수)만** 있었다. "그래서 어느 브랜드
한도를 먼저 보라는 것인가"에 답하지 못했다.

### 25-2. 방법

**성분 ES (Euler 배분)** — E[브랜드 손실 | 총손실 ≥ VaR₉₉].
합이 전체 ES 와 **정확히 일치하는 유일한 가법 배분**이다.

구현상 문제는 200,000회 시뮬레이션의 전체 손실 행렬(브랜드 60 × 시나리오 20만)을
메모리에 들고 있을 수 없다는 것이었다. **2-pass 공통난수(CRN)** 로 해결했다:

1. 1-pass — 총손실만 계산해 VaR₉₉ 를 구한다
2. 2-pass — **같은 seed 와 같은 청크 순서**(z → y → binomial)로 재생하면 동일
   시나리오가 재현된다. 꼬리 시나리오에서만 브랜드별 손실을 누적한다

**검산: 성분 합 − 전체 ES 상대오차 = 0.00e+00** (정확히 일치).

### 25-3. 결과 (`outputs/brand_ul_contribution.csv`)

| 브랜드 | 점포 | 여신 비중 | **UL 기여** | **쏠림 배수** |
|---|---|---|---|---|
| 빽다방 | 1,449 | 15.1% | **35.6%** | **2.37배** |
| 본죽&비빔밥 | 1,003 | 13.8% | **30.0%** | **2.16배** |
| 하이오커피 | 249 | 2.4% | 4.3% | 1.79배 |
| 인쌩맥주 | 213 | 3.6% | 4.2% | 1.18배 |
| 육쌈냉면 | 83 | 2.6% | 3.3% | 1.27배 |

**상위 5개 브랜드가 UL 의 77.4%.**

`concentration_ratio > 1` 이면 익스포저 비중보다 꼬리 기여가 큰 브랜드 —
**상관·집중이 만드는 위험 쏠림**이다.

### 25-4. 이것이 바꾸는 것

> **한도 재검토 순서는 여신 잔액이 아니라 이 표가 정한다.**

여신 잔액 순서로 보면 빽다방(15.1%)과 본죽(13.8%)은 비슷하다. 꼬리 기여로 보면
35.6% vs 30.0% 이고, 둘 다 잔액 비중의 2배가 넘는다. 반면 잔액 3.6% 인 인쌩맥주는
꼬리 기여도 4.2% 로 비례한다 — **같은 크기의 여신이 같은 크기의 위험이 아니다.**

포트폴리오 화면 '예상손실' 탭에 본문 표로 노출 (렌더 확인 완료).

### 25-5. 발견한 낡은 산출물

이 작업 중 **보정기 수정으로 점수가 바뀐 뒤 포트폴리오는 재산출했지만 상관 단계는
재산출하지 않았다**는 것을 발견했다.

| 지표 | 낡은 값 | **재산출 후** |
|---|---|---|
| UL 배수 | 5.31배 | **7.30배** |
| p99 과소추정 | +6.8억 | **+11.0억** |

문서 수치 전량 갱신 (대조 통과).

### 25-6. 추정기 자체를 합성 데이터로 검증한다

`tests/test_sanity.py::correlation_math` — 정답을 아는 합성 데이터로 추정기를 검증한다.

| 검사 | 기준 |
|---|---|
| 역산 왕복 오차 | < 1e-3 |
| 독립 데이터에서 ρ | ≈ 0 |
| 심어둔 ρ=0.30 복원 | 통과 |
| 거시충격만 있는 데이터에서 층화 효과 | 0.142 → 0.003 제거 |
| 손실 시뮬레이션 재현성 | 동일 seed 동일 결과 |
| LGD 단조성 | LGD↑ → EL↑ |

---
---

# 제4부 · AI 활용

## 26. 원칙 — 어디에 쓰고 어디에 안 쓰는가

이 프로젝트의 AI 활용에는 하나의 선이 있다.

> **점수와 등급에는 LLM 이 들어가지 않는다.**
> LLM 은 **읽고·설명하고·판독하는 층**에만 쓴다.

`config.yaml` 에 `llm.score_injection: false` 로 못박혀 있다. 이유는 세 가지다:

1. **재현성** — LLM 출력이 점수에 들어가면 같은 입력에 같은 점수가 나온다는 보장이
   사라진다. 백테스트가 무의미해진다.
2. **누출** — 뉴스는 시점 통제가 어렵다. 2023년 기사를 2021년 점수에 넣으면 미래를
   보는 것이다.
3. **거버넌스** — 모형 검증(§21)이 불가능해진다. 은행이 검증할 수 없는 것은 쓸 수 없다.

| 층 | AI 사용 | 대체 경로 |
|---|---|---|
| 점수·등급·보정·포트폴리오 | **미사용** | — |
| 뉴스 사건 추출 | **Gemini** (구조화 출력) | 규칙 기반 폴백 |
| 심사 메모 생성 | **Gemini** | 규칙 기반 템플릿 |
| AI 상담 | **Gemini** | 키 없으면 안내 |
| RAG 검색 | **미사용** (TF-IDF) | — (외부 임베딩 API 의존 회피) |
| 로고 판독 검증 | **Gemini Vision** | 글자 마크 |

**LLM 계층은 언제나 선택적이다.** 키가 없어도, 안전 차단돼도, 쿼터가 나가도
파이프라인 전체는 규칙 기반으로 완주하며 **점수·등급·포트폴리오 지표는 영향을 받지 않는다.**

---

## 27. LLM 클라이언트 설계 (`src/llm.py`, 425줄)

### 27-1. SDK 를 쓰지 않은 이유

Gemini Generative Language API v1beta 를 **REST 로 직접** 호출한다. 공식 SDK 를
도입하지 않은 이유는 두 가지다:

1. 이미 있는 `requests` 재시도 계층과 실패 정책을 통일하기 위해
2. **제출본 고정 시 SDK 마이너 버전이 응답 형태를 바꿔 재현이 깨지는 위험**을 없애기 위해

### 27-2. 실패를 유형별로 다르게 다룬다

| 실패 유형 | 처리 | 왜 |
|---|---|---|
| 네트워크·429·5xx | **지수 백오프 재시도** | 일시 장애 |
| 안전정책 차단 | **재시도 없이 즉시 폴백** | 같은 입력이면 결과가 같다 |
| HTTP 400 | 〃 | 요청 자체가 잘못됨 |
| 토큰 절단 | 〃 | 재시도해도 같은 곳에서 끊긴다 |

**같은 입력에 같은 결과가 나오는 실패는 재시도하지 않는다.** 재시도는 시간을
낭비할 뿐 아니라, 로그를 보는 사람에게 "불안정한 시스템"이라는 잘못된 인상을 준다.

### 27-3. 네 가지 세부 처리

**① 사고(thought) 파트 제거** — 모델 내부 추론이 심사메모로 새지 않게 응답에서
제거한다. 테스트로 검증: `assert "내부추론" not in text`.

**② responseSchema 정제** — Gemini 가 지원하지 않는 JSON Schema 키워드
(`additionalProperties` 등)를 **재귀적으로 제거**하고 `propertyOrdering` 을 부여한다.
제거하지 않으면 HTTP 400 이 난다.

**③ 안전 필터 BLOCK_ONLY_HIGH** — 입력이 소송·부도·집단폐점 등 **부정적 기업뉴스**라
기본 임계값에서 정상 기사가 오탐 차단된다. 우리가 다루는 도메인이 그런 도메인이다.
(차단 시엔 규칙 폴백.)

**④ 다중 키 폴백** — `GEMINI_API_KEY` + 예비 11개 슬롯.
429/401/403 은 **그 키의 문제**이므로 백오프 없이 즉시 다음 키로 넘어간다.
400·안전차단·토큰절단은 키를 바꿔도 같으므로 전환하지 않는다.

> ⚠️ **무료 한도는 키가 아니라 GCP 프로젝트 단위로 걸린다.** 한 프로젝트에서 키를
> 여러 개 만들어도 같은 통을 나눠 쓴다(실측 확인). 처리량을 늘리려면 **프로젝트를**
> 늘려야 한다. 이 사실을 모르고 키를 7개 등록했다가 전부 같은 한도를 공유하는 것을
> 확인했다.

### 27-4. 내가 틀렸던 판정 하나 — 철회 기록

한때 `AQ.` 로 시작하는 키를 **OAuth 액세스 토큰으로 보고 거부**했다. 근거는 그 접두를
가진 키가 401 `ACCESS_TOKEN_TYPE_UNSUPPORTED` 를 냈다는 실측이었다.

**원인은 형식이 아니라 그 키가 무효였던 것이다.** 새로 발급한 `AQ.` 키는
`gemini-2.5-flash` 와 `gemini-flash-latest` 모두 HTTP 200 을 받는다(실측). 같은 시점에
옛 키는 여전히 401 이다.

Google AI Studio 가 현재 발급하는 형식이 `AQ.` 이므로, **형식만 보고 막으면 멀쩡한
키를 우리가 차단하게 된다.** 판정은 API 에 맡기고 형식은 **시도 순서 정렬**에만 쓴다.
"AQ. 는 OAuth 토큰이라 쓸 수 없다"는 안내문을 전부 철회했다.

---

## 28. LLM 층 실동작 검증 — "붙였다"를 증명하는 방법

### 28-1. 문제

개발 환경에 LLM 키가 없으면 `news_llm`/`memo_llm` 은 **규칙 기반 폴백으로만** 동작한다.
폴백만 테스트하면 **"LLM 을 붙였다"는 주장이 검증되지 않는다.** 허수아비다.

### 28-2. 해법 — HTTP 경계 하나만 모의로 치환

`tests/test_llm_paths.py` 는 `src.llm._http_post`(HTTP 경계) **하나만** 모의로
바꿔서, **키 없이도 LLM 분기 전체**를 실행·검증한다.

```
요청 본문 구성 → responseSchema 변환 → 사고 파트 제거 → 응답 검증
→ 안전차단/절단/네트워크오류 폴백 → 재시도
```

**HTTP 경계에서만 치환하므로 우리가 작성한 코드는 한 줄도 우회되지 않는다.**

검증 항목 예:

```python
assert cls.calls, "LLM 호출이 일어나지 않음 (LLM 경로 미실행)"
assert "additionalProperties" not in gschema, "미지원 키워드가 제거되지 않음 (400 유발)"
assert text == "최종답변입니다", f"사고 파트가 섞여 들어옴: {text!r}"
assert body["safetySettings"], "safetySettings 미전달 (부정 기업뉴스 오탐 차단 방지)"
assert len(MockTransport.calls) == 1, "차단은 재시도 무의미한데 재시도가 일어남"
assert not MockTransport.calls, "키가 없는데 HTTP 호출을 시도함"
```

### 28-3. 이 테스트가 나 자신의 실수를 잡은 사례

다중 키를 도입할 때 만든 문제다. 테스트가 **주 키 하나만** 지우고 시험했다.
예비 키가 환경에 남아 있으면 "키 없음" 검사가 통과해 버리고, 재시도 횟수도 키
개수만큼 곱해진다(4회 기대 → **12회 실측**). 실제로 예비 키를 등록하자마자
4/4 → **2/4** 로 떨어졌다.

> **키가 없는 개발 환경에서는 통과하고 키가 있는 환경에서는 실패하는 테스트는
> 운영 조건을 검증하지 못한다** — 정작 확인해야 할 상황에서 무력하다.

→ `_IsolatedKeys` 컨텍스트로 **모든 키 슬롯**을 치우고 끝나면 복원하도록 고쳤다.

---

## 29. 뉴스 사건 추출 — 정량 평가

### 29-1. 문제 제기

"LLM 을 붙였다"만 검증돼 있고 **"규칙보다 낫다"는 근거가 0건**이었다. 측정했다
(`src/eval_llm.py`).

### 29-2. 정답셋 설계 — 순환논증을 피한다

`data/eval/news_gold.jsonl` (59건)

- **실수집 기사 41건에 사람이 라벨을 붙였다.**
  규칙이 붙인 라벨을 정답으로 쓰면 **순환논증**이므로 그렇게 하지 않았다.
  판정 기준은 `tools/build_news_gold.py` 에 명시.
- 실수집분에 본부분쟁·재무이슈·집단폐점이 **0건**이라 그 클래스 재현율을 잴 수 없다
  → **합성 프로브 18건**을 별도로 두고 **real/synthetic 분리 보고**
  (합쳐서 뭉개지 않는다).
- 프로브는 규칙 실패의 **두 방향을 모두** 담는다:
  키워드 있으나 위험 아님 / 위험이나 키워드 없음.

### 29-3. 결과

**실수집 41건**

| | 정확도 | macro F1 | 위험 F1 | 위험 재현율 |
|---|---|---|---|---|
| 규칙 | 0.488 | 0.381 | 0.500 | 0.333 |
| **Gemini** | **1.000** | **1.000** | **1.000** | **1.000** |

**합성 프로브 18건**

| | 정확도 | macro F1 | 위험 F1 | 위험 재현율 |
|---|---|---|---|---|
| 규칙 | 0.444 | 0.432 | 0.636 | 0.636 |
| **Gemini** | **0.889** | **0.911** | **0.917** | **1.000** |

**전체 59건**

| | 정확도 | macro F1 | 위험 F1 | 위험 재현율 |
|---|---|---|---|---|
| 규칙 | 0.475 | 0.498 | 0.615 | 0.571 |
| **Gemini** | **0.966** | **0.945** | **0.933** | **1.000** |

클래스별로 보면 규칙의 실패가 어디에 있는지 드러난다:

| 클래스 | 규칙 F1 | Gemini F1 |
|---|---|---|
| 본부분쟁 | 0.400 | 0.857 |
| 재무이슈 | 0.857 | 0.857 |
| 집단폐점 | 0.667 | **1.000** |
| 수요급감 | 0.500 | **1.000** |
| 기타 | 0.563 | 0.957 |
| **무관** | **0.000** | **1.000** |

규칙은 **'무관' 클래스 F1 이 0** 이다. 키워드가 걸리면 무조건 뭔가로 분류하기
때문에 무관한 기사를 걸러내지 못한다. 이것이 실무에서 가장 비싼 실패다 —
심사역이 무관한 기사를 읽는 데 시간을 쓰게 된다.

### 29-4. 무효화 가드 — 폴백이 섞이면 지표를 버린다

**폴백이 하나라도 섞이면 LLM 지표를 무효 처리한다.** 쿼터 초과로 절반이 폴백된
점수를 LLM 성능으로 보고하면 **그 자체가 허위**다.

```json
"llm_evaluated": true, "llm_fallback_brands": 0, "llm_invalidated_reason": null
```

**실제로 첫 실행이 이 가드에 걸려 무효 처리됐다.**

### 29-5. 측정한 모델을 그대로 운영에 쓴다

`config.llm.model = gemini-2.5-flash` 로 고정. 3.6-flash 는 무료 쿼터 소진으로
평가를 완주하지 못했다. **측정하지 않은 모델은 쓰지 않는다.**

### 29-6. 평가가 찾아낸 분류 체계 결함

실측 기사 **"탕후루 유행 꺾이더니 1년 새 매출 92% 급감"** 을 규칙·LLM **양쪽 모두**
'기타'로 흘려보내고 있었다. **프랜차이즈 여신의 핵심 위험인데 5분류에 자리가 없었다.**

→ **'수요급감' 유형 신설** (INTERFACES v5).

> **평가셋이 없었으면 못 찾았을 결함이다.** 정량 평가의 가치는 점수를 얻는 것이
> 아니라 이런 것을 찾는 데 있다.

### 29-7. 뉴스 수집 대상 선정도 고쳤다

초기에는 **위험 상위만** 뽑아 '계계속속'·'맘마찬' 같은 무명 브랜드가 선정됐고,
262건 중 **위험 이벤트 0건**, 동명이인 기사('오마이걸 미미')만 걸렸다.

→ 점포수 하한(100)을 함께 걸어 **위험도 × 보도가능성**으로 선정.
그리고 시의성 필터(`max_age_months: 24`) 추가 — 2012·2015년 기사가 '뉴스 경보'로
노출되고 있었다.

---

## 30. RAG — 외부 임베딩 API 없이

### 30-1. 구성

| 항목 | 값 |
|---|---|
| 문서 | **23,707건** |
| — 공시 | 20,000 |
| — 본부 감사보고서 원문 | 3,684 |
| — 뉴스 | 23 |
| 커버 브랜드 | **5,085** |
| 검색 | TF-IDF (word 1-2gram + char_wb 2-4gram) + 코사인 유사도 |

**외부 임베딩 API 를 쓰지 않는다.** 키 없이 완전 재현 가능해야 하고, 폐쇄망 이식
시 외부 호출 지점을 하나라도 줄여야 하기 때문이다. 한국어 브랜드명·상호는
char n-gram 이 잘 잡는다.

### 30-2. 결함: 색인이 만든 진입점에서만 로드 가능했다

인스턴스를 피클해 `__main__.RagIndex` 경로가 박히는 바람에, 대시보드·다른 모듈에서
**로드가 조용히 실패**했다. 즉 **화면의 근거 인용이 죽어 있었다.**

→ 객체가 아니라 **상태(state)** 를 저장하도록 변경 + 회귀 테스트
(색인 파일에 `__main__`/클래스 참조가 없어야 통과).

### 30-3. 배포용 경량화 — 품질을 재고 줄였다

float32 + char 피처 상한 40,000. **91MB → 67MB**, 상주 263MB → 236MB.

줄이기만 하고 끝내지 않았다. 검색 품질을 실측 확인했다 (`tools/check_rag_quality.py`):

```
상위 5개 겹침  95%
1위 일치       11/12
```

---

## 31. AI 상담 — "검색 기능만 있는 것 같다"를 고친 과정

### 31-1. 원인은 프롬프트가 아니라 재료였다

지적을 받고 처음에는 프롬프트를 고쳤다. 나아지지 않았다. 진짜 원인은
**질문이 무엇이든 그 브랜드의 모든 사실을 통째로 넘기고 있었다**는 것이다.
재료가 같으면 답도 같은 모양이 된다.

### 31-2. 의도별 재료 라우팅

| 의도 | 넘기는 재료 | 답의 모양 |
|---|---|---|
| `greeting` | 없음 | 인사 + 예시 안내 |
| `off_domain` | 없음 | 범위 밖임을 3문장 이내로 |
| `brand_metric` | 해당 지표만 | 표 + 해석 |
| `compare` | 두 브랜드 대비 항목 | 비교표 |
| `industry` | 업종 집계 | 업종 관점 |
| `startup` | 창업비용·회수·만기이탈 | 창업자 관점 |
| `followup` | 앞 대화 | 물어본 부분만 |

시스템 지시문도 **의도별로 조립**한다(`build_system`). 고정 프롬프트 하나로는
답의 모양이 고정될 수밖에 없다.

**실측 효과:** "네네치킨의 매출을 조사해줘" → 넘긴 재료 **2,209자 → 948자**,
답은 매출 표 + 2문장 해석만.

### 31-3. 숫자를 해석하도록 요구한다

시스템 지시문에 명시:

```
"58.6% 는 10곳 중 6곳이 한 해에 문을 닫았다는 뜻" 처럼 숫자를 해석하라
서로 다른 신호를 하나의 이야기로 엮어라
```

### 31-4. 브랜드 인식 오탐 — 실측으로 두 번 고쳤다

| 질문 | 오탐 | 원인 |
|---|---|---|
| "가장 위험한 브랜드는?" | `가장맛있는족발` | **'가장'** 이 부분일치 |
| "sk하이닉스 주가분석해줘" | `하이오커피` | '하이' 부분일치 |

수정:
- 불용어 44개로 확대
- 퍼지 매칭 최소 길이 2 → **3자**
- 업종명은 브랜드가 아니라 **업종 질의**로 라우팅
- **범위 밖 신호**(주가·환율·날씨…)가 있으면 브랜드가 잡혔더라도 범위 밖으로 판정

검증: '치킨 업종에서 가장 위험한 브랜드는?' → 브랜드 0개(업종 질의로 처리),
'메가커피와 컴포즈커피 중 어디가 나아?' → 공시 등록명 2개 정확 변환.

### 31-5. 오류 문구를 원인별로 나눴다

키는 정상인데 Gemini 무료 한도(429)에 걸린 경우까지 **'연결 안 됨'** 으로 표시했다.
사용자가 키 설정을 의심하며 엉뚱한 곳을 고치게 된다.

→ 키 미설정 / 무료 한도 초과(1~2분 뒤 재시도) / 기타 오류로 분리.

---

## 32. 심사메모 — 환각을 자동 검증한다

프롬프트에 "지어내지 마라"는 지시만 있고 **출력 검증이 없었다.**

→ 메모의 **모든 수치를 입력 context 와 대조**한다(반올림·백분율 변환 허용).
근거 없는 값이 있으면 **메모에 경고를 부착**한다.

회귀 테스트로 지어낸 수치(412.7%, −18.3%) 검출을 확인했다.

메모는 파일(.md)로 내려받을 수 있게 했다 — 세션에만 있으면 창을 닫는 순간 사라져
여신 파일에 첨부할 수 없다. **파일명에 타임스탬프를 넣지 않아** `memo_llm` 의
'동일 입력 → 동일 출력' 결정성을 유지한다.

---

## 33. 비전 LLM — 로고 판독 검증

### 33-1. 왜 검증이 필요한가

'{브랜드} 로고' 로 이미지 검색을 하면 상위 결과가 이렇다:

```
4000x3000  음식 사진 (네이버 플레이스)
 736x470   핀터레스트 이미지
 600x600   포트폴리오 사이트 썸네일
```

**로고가 아니다.** 색 수로 사진과 로고를 가르려 했으나 분리되지 않았다
(로고 435색 vs 사진 3,558색이 섞인다). 크기·형식·종횡비 어느 것도 신호가 약하다.

> **틀린 로고는 로고가 없는 것보다 나쁘다.**

### 33-2. 판독 검증 설계

로고에는 거의 항상 **브랜드명이 글자로 박혀 있다.** 그래서 이미지를 LLM 에 보여 주고:

1. **보이는 글자를 그대로 옮겨 적게** 한다
2. 그 글자가 브랜드명과 맞는지 **판정**하게 한다

음식 사진·간판 사진·다른 회사 로고는 false 로 떨어진다.

**실측:** 지코바양념치킨 — 후보 4개 중 3개를 거부하고('GCOVA CHICKEN' 만 있는
뉴스 이미지 등) 4번째 'GCOVA 지코바치킨' 을 채택.

판독 결과(보이는 글자·판정 사유)는 **캐시에 그대로 남긴다.** "왜 이 그림이 이
브랜드의 로고인가"를 나중에 사람이 확인할 수 있어야 하기 때문이다.

### 33-3. ⚠️ 모델 선택이 검증을 무력화할 뻔했다

`gemini-2.5-flash` 는 같은 이미지에 대해 **보이는 글자를 빈 문자열로 돌려주고
전부 false 로 판정**했다(실측). `gemini-flash-latest` 를 쓴다.

> **이미지 판독이 되는 모델인지 확인하지 않고 붙이면 검증이 하는 일 없이 전부
> 기각된다.** 통과율 0% 를 "엄격한 검증"으로 오해할 뻔했다.

### 33-4. 내가 한 보고를 정정한 사례

한때 "BHC·교촌·한솥은 32px 파비콘만 게시하므로 **구조적 한계**"라고 보고했다.
**구조적 한계가 아니라 맞는 수집원을 안 쓴 것이었다.** 이미지 검색 + LLM 판독으로
정확히 그 브랜드들을 확보했다 — bhc('bhc CHICKEN'), 교촌치킨('KyoChon 1991'),
처갓집양념치킨, 더벤티, 본죽&비빔밥, 페리카나, 네네치킨, 텐퍼센트.

### 33-5. 3층 수집 구조

실패 원인부터 셌다. 하나가 아니었다.

| 실패 원인 | 건수 | 비중 |
|---|---|---|
| 도메인 공유(포털)로 제거 | 413 | 28.7% |
| 자기 도메인은 있는데 아이콘 없음 | 343 | 23.7% |
| 도메인 못 찾음 | 113 | 7.9% |
| 아이콘이 64px 미만 | 24 | 1.7% |

| 층 | 방법 | 방어 장치 |
|---|---|---|
| ① | 공식 도메인의 아이콘 (apple-touch-icon → icon → og:image → 관행 경로) | 실제로 받아 **크기를 재고** 가장 큰 것 (선언된 sizes 속성은 거짓이 흔하다) |
| ② | 자기 사이트 페이지 안의 로고 | `site_mentions_brand` 게이트 — 없으면 엉뚱한 사이트가 들어온다 |
| ③ | 이미지 검색 + **LLM 판독 검증** | 위 §33-2 |

**층 ② 의 게이트가 실제로 막은 것:** 일미리금계찜닭에 `gimhae.go.kr`(김해시청),
생어거스틴에 `booking.kakao.com`. **224건**이 이 관문에서 걸러졌다.

**og:image 를 조인 이유:** 그대로 받았더니 배너·SNS버튼·카톡캡처·자리표시자가
로고 자리에 들어갔다(고망고 1920px 메인배너, 할머니가래떡볶이 `sns_link.png` 등).
**32건을 철회**하고, og:image 는 경로에 `logo`/`ci`/`bi` 가 있을 때만 받도록 조였다.

> 커버리지 43.2% → 40.9% 로 **내려갔지만 그게 맞는 숫자다.**

### 33-6. 현황

| 기준 | 값 |
|---|---|
| 브랜드 수 | **42.4%** (611 / 1,442) |
| **점포 가중** | **67.7%** |

색인과 디스크의 불변식을 테스트로 못박았다 (→ 제7부 §38-23).

---

## 34. 개발 도구로서의 AI — 정직하게

이 프로젝트는 AI 코딩 에이전트(Claude)와 함께 개발했다. 감추지 않는다.
`docs/AI_USAGE.md` 에 단계별로 기록돼 있다.

**그러나 이 문서 제7부가 보여주는 것이 그 협업의 실제 모습이다.** 스스로 제안한
설계가 틀렸다는 것을 실측으로 확인하고 되돌린 기록이 제7부에 전수 정리돼 있다.
게이트 완화안을
"채택 가능"이라 판정했다가 자신의 기준을 적용해 **기각으로 뒤집은 기록**,
"구조적 한계"라 보고했다가 **정정한 기록**, 커버리지 43.2% 를 42.4% 로 **낮춘 기록**이
전부 커밋 메시지에 남아 있다.

> **AI 로 코드를 생성하는 것과, 그 코드가 틀렸다는 것을 측정으로 찾아내 되돌리는
> 것은 다른 일이다.** 이 프로젝트의 실질은 후자에 있다.

구체적으로 사람이 방향을 잡은 지점들:

- "브랜드 로고가 안 나온다" → 3층 수집 재설계로 이어짐
- "검색 기능만 있는 것 같다" → 의도별 재료 라우팅으로 이어짐
- "메가커피가 없다" → 브랜드 검색 정규화·별칭 체계로 이어짐 (실제로는 데이터에
  있었다 — 공시 등록명이 '메가엠지씨커피(MEGA MGC COFFEE)')
- "흰 배경에 이미지를 붙여넣은 수준" → 디자인 레이어 전면 재작업
- "허수아비 보여주기 식 개발 금지" → 잠자는 규칙 검사·LLM 실경로 테스트·
  문서 수치 자동 대조의 근거가 된 원칙

---
---

# 제5부 · 제품

## 35. 화면 5종

<https://franscore.streamlit.app>

| 화면 | 모듈 | 무엇을 하는가 |
|---|---|---|
| **FRANSCORE** | `views/franscore.py` | 한눈에 보기 + 브랜드 조회 + 상세 진단 |
| **점검 큐** | `views/queue.py` | 담당자 배정·처리상태·확인 메모 |
| **여신 포트폴리오** | `views/portfolio.py` | 집중도·EL·스트레스·꼬리 기여 |
| **AI 상담** | `views/assistant.py` + `chat.py` | 자연어 질의 |
| **서비스 소개** | `views/about.py` | 두 층 구조 (일반 / 심사역) |

### 35-1. FRANSCORE — 통합 화면

'한눈에 보기'와 '브랜드 조회'를 **하나로 합쳤다**. 목록·검색 결과 어디서든 브랜드를
누르면 그 자리에서 상세로 들어간다.

**브랜드 검색이 안 되던 문제** — '메가커피'가 없다는 지적을 확인해 보니 데이터에는
있었다. 공시 등록명이 **'메가엠지씨커피(MEGA MGC COFFEE)'** 라서 문자열 일치로 안
걸렸을 뿐이다.

→ 정규화(공백·괄호·영문 제거) 부분일치 + 통칭 별칭 + 앞2뒤2 토큰 + 근접 제안.
검증: 메가커피→메가엠지씨커피, 베라→배스킨라빈스, 맥날→맥도날드.

**타입어헤드** — 한 글자만 쳐도 후보가 뜬다(selectbox 네이티브).
`text_input` 은 Enter 를 눌러야 값이 넘어와 **자동완성이 원리상 불가능**했다.

### 35-2. 위험 표기 — 숫자에서 신호등으로

| 이전 | 현재 |
|---|---|
| 빨간 확률 숫자 | **신호등** (숫자는 보조) |
| 게이지 축 0~50 | **0~100%** |
| 등급 구간표 없음 | 화면에 노출 |

게이지 축을 확률의 정의역대로 두지 않으면 **'절반이 찼다'는 인상이 실제 위험보다
과장**된다.

'악화 가능성' → **'브랜드 리스크'** 로 명칭을 통일했다. 한때 FRANSCORE 화면만 바꾸고
점검 큐·포트폴리오·AI 상담은 '악화 가능성' 그대로여서, 심사역이 **다른 지표로
오해**할 수 있었다. LLM 에 넘기는 **사실 키 이름**도 함께 바꿨다 — 그 키가 답변
문장에 그대로 나온다.

등급 명칭도 공표 방법론과 통일했다. 범례는 'FS1 안정'인데 카드는 '양호'로 나오고
있었다. **'양호'는 문제 없음으로 읽히지만 FS1 도 실제로 2.22% 는 악화한다** —
'안정'이 정확하다.

### 35-3. 진단 소견 — 조합 요약 + 보고서

**소견을 다 읽어야 결론이 나오면 아무도 안 읽는다.** 부문별로 가장 무거운 것 하나씩
뽑아 조합을 먼저 보인다. 보고서(.md)에는 등급 의미표·전 소견·사용범위 고지를 담는다.

### 35-4. 점검 큐 — 명세를 어기던 것을 고쳤다

`MODEL_USE_SPEC` 은 요주의 구간에 대해 **"확률값에 성능 근거가 없으니 순위를 매기지
말라"** 고 못박는다. 그런데 큐는 `deterioration_1y × n_stores` 하나로 전부 줄세웠고,
**상위 20건 중 18건이 요주의**였다. `queue.py` 의 `brand_state` 참조는 **0건** —
화면 어디에도 그 사실이 없었다.

**요주의를 큐에서 빼는 것은 답이 아니다.** 실제 재발동률이 24.1% / 45.9% / 64.8% 로
건전(9.4%)보다 훨씬 높아 **봐야 할 브랜드가 맞다.** 잘못된 것은 목록에 넣은 것이
아니라 **근거 없는 숫자로 줄세운 것**이다.

```python
def _prioritize(view):
    """상태별로 다른 위험도를 쓴다 — 한 척도로 줄세우면 명세를 어긴다."""
    rates = C.watch_rates(0.0)
    def risk(r):
        if str(r.get("brand_state")) == "요주의":
            hit = rates.get(int(r.get("n_events_at_t")))
            if hit: return float(hit["rate"])       # 실적표
        return float(...deterioration_1y...)        # 모델 확률
```

둘 다 '1년 내 악화' 확률이라 **같은 축에서 비교 가능**하고, 요주의 쪽은 모델이 아니라
**실적**이라 명세를 어기지 않는다.

카드 문구도 바꿨다:
`'브랜드 리스크 32.7%'` → `'악화 사건 2건 · 같은 조건 브랜드의 다음 해 재발동률 45.9%'`

> 바뀐 뒤에도 상위 20건 중 18건이 요주의다. 다만 이제는 **정당하게** 그렇다 —
> 실현율 45.9%·64.8% 가 건전 브랜드의 모델 확률 26% 보다 실제로 높기 때문이다.

**기록이 사라지던 문제** — 상태가 세션에만 있어 새로고침 한 번에 배정·메모가 전부
날아갔다. **기록이 사라지는 큐는 큐가 아니다.** `queue_state.json` 으로 영속화하고,
클라우드 재배포 시 초기화된다는 사실을 밝히고 엑셀 반출을 1차 보존 수단으로 안내한다.

### 35-5. 여신 포트폴리오 — 실시간 재계산

실행·회수를 입력하면 총여신·HHI·집중도·EL·스트레스가 **그 자리에서 다시 계산**된다.
브랜드·업종 한도 초과 경고 포함.

실증: 29억 실행 → 666→695억, HHI 0.0683→0.0645, EL 15.3→21.6억.

**추정 익스포저 고지를 표 위로 올렸다.** 이 고지가 화면 맨 아래 캡션에 있으면
아무도 안 읽고, 모르고 인용하면 **근거 없는 금액이 결재로 넘어간다.**
경고 박스로 올리고 **'금액이 아니라 구조를 보라'**, 실제 여신 CSV 연결 방법을 함께 안내.

### 35-6. 서비스 화면 정화

산출물 존재 체크리스트·하이퍼파라미터 JSON·백테스트 지표표·"--step 실행하세요"
안내를 서비스 화면에서 걷어냈다. **그 근거는 문서와 산출물에 있지 화면의 것이 아니다.**

서비스 화면 금지어 검사를 자동화했다 (run_pipeline·--step·SHAP·LightGBM·Lift@·
parquet 등) — 현재 **0건**.

용어도 평이화했다:

| 내부 용어 | 화면 표기 |
|---|---|
| 확률 산출값 | 1년 내 악화 가능성 |
| SHAP | 이렇게 본 이유 |
| 등급 | 주의 / 관찰 / 안정 |
| `f_ind_contract_end_pct` | [업종상대] 계약종료 백분위 |

원시 피처명이 화면에 그대로 나오던 문제를 `src/feature_names.py` 로 분리해
**화면·CSV·메모가 같은 이름을 쓰게** 했다.

---

## 36. 디자인 시스템 (`src/theme.py`, 800줄)

### 36-1. KB 팔레트 실측

KB국민은행 운영 사이트에서 실측한 팔레트로 디자인 시스템을 만들었다
(시그니처 #FFCC00, 웜 그레이 텍스트/보더, 딥톤 사이드바). 차트는 전부 Plotly
KB 템플릿으로 교체.

### 36-2. "붙여넣은 그림" 을 벗어나기 위한 세 원칙

1. **그림자는 두 겹** — 가까운 경계선 + 멀리 퍼지는 그늘이라야 종이처럼 보인다
2. **움직임은 감속** — `cubic-bezier(.22, 1, .36, 1)`
3. **등장은 아래에서 위로** — 뚝 나타나면 붙여넣은 그림이 된다

`prefers-reduced-motion` 을 존중해 움직임을 끌 수 있게 했다.

### 36-3. 접근성 — 눈으로 본 것이 아니라 잰 것이다

브라우저 DOM 을 직접 계측해 결함을 찾았다.

**색 (명도대비, 배경 #F5F4F1 기준)**

| 요소 | 이전 | 이후 |
|---|---|---|
| 사이드바 비활성 메뉴 | **1.14** (사실상 비가시) | **8.01** |
| 의미색 '관찰' | 2.59 | 기준 충족 |
| 의미색 '안정' | 3.94 | 〃 |
| 의미색 '주의' | 4.42 | 〃 |
| 보조 텍스트 | 2.79 | 〃 |
| 입력 안내문 | 3.70 (Streamlit 기본) | 〃 |

사이드바 1.14 의 원인은 **특이도 역전**이었다. `[data-testid=stSidebar] *` 는
(0,1,0) 인데 본문 규칙 `[data-testid=stMarkdownContainer] p` 가 (0,1,1) 이라
본문색이 이겼다. Streamlit 이 버튼 라벨을 `stMarkdownContainer > p` 로 렌더하기 때문.

**글자용/도형용 토큰을 분리했다** (4.5:1 vs 3:1 — 기준이 다르다). 색조·채도는 원래
값 근처에 묶고 명도만 최소한 낮췄다. **채도를 올리면 대비는 쉽게 오르지만 원색이
되어 '경고를 과장하지 않는다'는 전제를 깬다.**

**타이포** — 소스에 크기가 29종, 렌더 28종이었다(.76~2rem 을 사실상 전부 사용).
**여덟 단계로 수렴.** 13px 이 바닥 — 10px 게이지 눈금, 11px 등급 라벨,
11.2px 사용범위 고지를 없앴다. **안 읽히는 고지는 고지가 아니다.**

**차트** — `st.plotly_chart` 가 기본값 `theme="streamlit"` 으로 **KB 템플릿을 통째로
덮어쓰고 있었다.** trace marker 색만 살아남아 색이 맞으니 먹은 줄 알았다.
`theme=None` 으로 실제 적용.

### 36-4. 계측할 때 알아야 하는 것

> **트랜지션을 먼저 정지시켜야 한다.** 안 그러면 `getComputedStyle` 이 시작값을
> 돌려줘 결함이 가려진다. 대비 1.14 를 한동안 놓친 이유가 이것이었다.

### 36-5. 회귀 방지

`tests/test_theme_contrast.py` — 대비 35건 + 스케일 + 화면 코드 이탈 검사를
매번 다시 계산한다. **표준값(검정/흰 21.00, #767676/흰 4.54)으로 계산식 자체를
먼저 자기 검증한다.**

**실측 결과:** 6개 화면 × 3개 폭(1512/1280/1024) — 대비 미달 0 · 13px 미만 0 ·
가로 넘침 0 · 글자 잘림 0 · 저해상도 이미지 0.

---
---

# 제6부 · 운영

## 37. 파이프라인

```bash
python run_pipeline.py --step collect      # 공정위 오픈API 7종 → data/raw 스냅샷
python run_pipeline.py --step panel        # 엔티티 정합 + 패널 + 생존성 진단
python run_pipeline.py --step features     # 피처 49개
python run_pipeline.py --step labels       # 라벨 3,635행
python run_pipeline.py --step model        # LightGBM + 기준모형 3종
python run_pipeline.py --step evaluate     # 지표·부트스트랩 CI·SHAP
python run_pipeline.py --step backtest     # 워크포워드 확장창
python run_pipeline.py --step bands        # 등급 컷 도출
python run_pipeline.py --step score        # 최신 코호트 점수 (재학습 없음)
python run_pipeline.py --step validate     # PSI·KS·HL·이행행렬
python run_pipeline.py --step portfolio    # 여신 집중도·EL
python run_pipeline.py --step correlation  # ρ 실증 + 손실 분포 + Euler 배분
python run_pipeline.py --step diagnose     # 진단 소견 9,371건
python run_pipeline.py --step news         # 뉴스 사건 추출 (LLM)
python run_pipeline.py --step demand       # 검색수요 + 로고
```

### 37-1. 운영 점수 경로가 따로 있는 이유

`model.py` 는 features × labels 를 inner join 하므로 **라벨이 확정된 과거 연도만**
점수를 가졌다. 라벨은 t+2년 공시로 확정되므로 **정의상 최신 코호트에는 라벨이 없고**,
결국 은행이 정작 알고 싶은 대상에 점수가 없었다 — **예측기가 아니라 백테스트
하네스**였다.

`src/score.py` 의 설계:

- 학습 산출물(booster·calibrator·피처명 매핑)만 **읽어서** 점수화. **재학습 없음.**
- 보정기는 과거 적합본을 **적용만** 한다 (재적합 금지 — 실전에 없는 정보 사용 방지)
- 학습 피처가 하나라도 없으면 0으로 채우지 않고 **명시적으로 실패**시킨다.
  **조용히 틀린 점수가 나가는 것이 가장 위험하다.**
- SHAP 상위 3개 요인을 CSV에 함께 실어 심사역이 '왜'를 바로 본다

실측: 2024년 코호트 9,333행 → 자격 **1,442개 브랜드** 점수화.

### 37-2. 타임라인

```
브랜드-연도 t 의 피처   →  t+1년 공시로 계산 가능
라벨(t+1년 실적 악화)   →  t+2년 공시로 확정
```

즉 **점수 산출 시점 기준 약 1년 앞**을 본다.

---

## 38. 일간 배치 — 무엇이 바뀌고 무엇이 안 바뀌는가

**공시는 연 1회 나오고 모델 피처가 전부 거기서 나온다.** 그러므로 확률은 매일 움직일
수 없다. **매일 재산출하는 척하며 같은 값을 다시 쓰는 것은 실시간 갱신이 아니다.**

| 항목 | 주기 | 출처 |
|---|---|---|
| 진단 소견 · 점검 우선순위 | **매일** | 뉴스, 네이버 검색어트렌드 |
| 브랜드 리스크 확률 | **연 1회** | 공정위 가맹사업 공시 |
| 가맹본부 재무 | 연 1회 | DART 감사보고서 |

이 두 주기를 **화면 하단에 표로 구분해** 보여준다.

**호출량을 보수적으로 잡았다:** 뉴스 12브랜드 × 4질의 = 48회/일(한도 25,000),
수요 신규 40개/일(한도 1,000), 로고 60개/일. 이미 수집된 브랜드는 건너뛰어
**날마다 넓어진다.**

**한 단계가 실패해도 나머지는 진행한다** — 뉴스가 안 와도 진단은 돌아야 한다.

GitHub Actions 가 매일 06:00 KST 에 돌고 **변경분만 커밋**한다. 저장소가 갱신되면
Streamlit Cloud 가 자동으로 앱을 다시 띄운다.

### 38-1. 축소 가동을 드러낸다 — 초록불의 함정

일간 배치가 GitHub Actions 에서 **매일 success 로 끝나고 있었다.** 실행 로그를 열어
보니 `NCP_API_KEY_ID` 가 빈 값이었고(`gh secret list` 가 공백 — **시크릿이 하나도
등록돼 있지 않았다**), 뉴스는 키가 필요 없는 Google News RSS 제목만으로, 사건 추출은
규칙 기반 폴백으로 돌고 있었다.

**폴백이 제대로 동작한 덕분에 어떤 단계도 실패하지 않았고, 그래서 성능이 줄어든 채
매일 초록불이 켜졌다.**

> **폴백 자체는 옳은 설계다. 잘못된 것은 줄어든 사실이 어디에도 안 남는다는 점이다.**

대응:

- `src/common.capability_report()` — 기능 5종이 어떤 키를 필요로 하고, 없으면
  무엇으로 대체되며, 그 결과 무엇이 나빠지는지. **키 값은 담지 않는다**(검증 완료).
- `tools/check_credentials.py` — 배치보다 **먼저** 실행. 정상/축소를 기능별로 출력.
  폴백은 정상 경로이므로 **축소 자체로는 종료코드 0** (`--strict` 로 변경 가능).
- `refresh_state.json` 에 capability 블록 기록 + **화면에 축소 가동 경고**
- 커밋 메시지에 `축소가동 N건` 표기

---

## 39. 배포

| 항목 | 값 |
|---|---|
| 플랫폼 | Streamlit Community Cloud |
| URL | <https://franscore.streamlit.app> |
| Python | **3.13 고정** |
| clone 크기 | 251MB (최대 파일 `rag_index` 64MB) |
| 상주 메모리 | 236MB |

**Python 3.13 을 고정한 이유** — Community Cloud 가 버전 미지정 시 **3.14** 를 잡는다.
numpy 2.2.4·pandas 2.2.3·scikit-learn 1.6.1·matplotlib 3.10.1 은 cp314 wheel 이
없어 소스 빌드로 넘어가고, 컨테이너에 컴파일러가 없어 완료되지 않는다.
**uv 의 "Resolved 67 packages" 까지는 정상 통과하므로 성공한 것처럼 보이는 것이
함정이다.** 모델이 scikit-learn 1.6.1 로 직렬화돼 있어 핀을 푸는 대신 Python 을 맞춘다.

**키가 하나도 없어도 앱은 정상 동작한다.** 새로 clone 한 상태에서 키 없이 5개 화면이
전부 뜨는 것을 실측 확인했다. 이미 수집해 둔 산출물(점수·진단 소견·로고 611건·
검색수요 292건)이 저장소에 들어 있기 때문이다. 키는 **다시 수집하거나 AI 상담 답변을
생성할 때만** 필요하다.

---

## 40. 폐쇄망 이식 (`DEPLOY.md` §3-1)

은행 내부망 반입을 전제로 문서화했다.

### 40-1. 아웃바운드 목적지 전량 표

| 호스트 | 용도 | 주기 | 차단 시 영향 |
|---|---|---|---|
| `apis.data.go.kr` | 공정위 공시 수집 | 연 1회 | 기존 스냅샷으로 계속 운영 |
| `opendart.fss.or.kr` | DART 감사보고서 | 연 1회 | 〃 |
| `generativelanguage.googleapis.com` | Gemini (뉴스·상담·로고) | 일간 | **규칙 폴백 자동 전환, 점수 무영향** |
| `openapi.naver.com` / API HUB | 검색·수요 | 일간 | 해당 소견만 미생성 |
| 브랜드 공식 도메인 다수 | 로고 1회 수집 | 1회성 | 글자 마크로 대체 |

### 40-2. 오프라인 설치

```bash
pip download -r requirements.txt -d wheelhouse   # 외부망에서
pip install --no-index --find-links wheelhouse -r requirements.txt   # 내부망에서
```

### 40-3. 사내 LLM 교체 지점

**`src/llm.py` 의 `generate()` / `generate_json()` 두 함수만** 사내 모델로 바꾸면
된다. 나머지 모듈은 이 두 함수만 호출한다.

### 40-4. 데이터 반출 경계

> **여신 금액은 어떤 외부 API 로도 나가지 않는다.**
> 코드로 검증: `chat.py` 의 exposure 참조 **0건**.

---

## 41. 모형 사용 명세 (`docs/MODEL_USE_SPEC.md`)

SR 26-2 조사에서 얻은 결론 — **1순위 산출물은 성능표가 아니라 사용 범위 명세**(§5-1).

| 구분 | 개수 | 예 |
|---|---|---|
| 허용 용도 | 4 | 점검 우선순위 선정, 한도 재검토 순서, 포트폴리오 집중 모니터링, 심사 참고 |
| **금지 용도** | **8** | 자동 승인·거절, PD 로 표기·인용, 요주의 구간 확률로 순위 매기기, … |
| 중요도 전환 조건 | 4 | — |
| 알려진 한계 | 10 | — |

등급 밴드가 **부도확률이 아니라 '공시 지표의 업종 하위구간 진입 확률'** 임을 명시한다.

### 41-1. PD 명칭 모순 — 두 문서가 정면 충돌하고 있었다

```
MODEL_USE_SPEC  "'부도확률'·'PD'로 표기하거나 그렇게 인용" → 금지 용도
MODEL_USE_SPEC  "어떤 산출물도 PD 가 아니다."
OPERATIONS      "PD로 쓰되 신용 부도율과는 다르다."        ← 충돌
```

더 나쁜 것은 **공개 산출물 `scores_latest.csv` 의 컬럼명이 `pd_1y`·`pd_raw`·
`pd_calibrated_step`·`pd_rank_pct`** 였다는 점이다. 은행 분석가가 이 CSV 를 열면
**명세가 금지한 바로 그 오독**을 한다 — **우리가 이름으로 유도한 오독**이다.

→ 식별자 **156건 개명** (20개 파일):

| 이전 | 이후 |
|---|---|
| `pd_1y` | `deterioration_1y` |
| `pd_raw` | `score_raw` |
| `pd_calibrated_step` | `deterioration_step` |
| `pd_rank_pct` | `deterioration_rank_pct` |
| `pd_floor` / `pd_cap` | `prob_floor` / `prob_cap` |

**바젤 IRB 의 PD 개념 참조는 그대로 둔다** — 확률 하한 관행을 준용한 근거이지
우리 숫자가 PD 라는 주장이 아니다. 그 구분을 주석에 적었다.

**순수 개명임을 확인:** 재산출 후 등급 분포 FS1 443 / FS2 632 / FS3 367 **불변**,
컷 [0.045, 0.16] **불변**.

`tests/test_naming.py` 신설 — 옛 식별자 부활 · PD 주장 문장 · 공개 컬럼명 ·
명세와 운영 문서의 기준 일치 · **반출 산출물 설명문** · 로고 색인 정합 (현재 7종).

---
---

# 제7부 · 품질 체계

> 이 부가 이 문서의 핵심이다. 앞의 여섯 부는 **무엇을 만들었는지**를 말하고,
> 이 부는 **그것이 맞다는 것을 어떻게 아는지**를 말한다.

## 42. 자동 검사 체계

### 42-1. 테스트 6종 (pytest 30건)

| 파일 | 검사 | 무엇을 막는가 |
|---|---|---|
| `test_sanity.py` (6) | 라벨 규칙 · **시점 누출** · 시간 분할 · 예측 스키마 · 포트폴리오 · **상관 수학** | 백테스트를 거짓으로 만드는 결함 |
| `test_llm_paths.py` (4) | LLM 클라이언트 · 뉴스 경로 · 메모 경로 · RAG | "LLM 을 붙였다"가 허수아비인 것 |
| `test_diagnosis.py` (9) | 소견 생성 · **잠자는 규칙 없음** | 있는 척하는 기능 |
| `test_calibration.py` (6) | 계단 밖 출력 · 기저율 대체 · 보정 단조 · 등급 단조 · 척도 일치 · 구간 이탈 | **조용히 틀리는** 확률 |
| `test_theme_contrast.py` (35+) | 명도대비 · 타이포 스케일 · 진입 스크립트 상수 참조 | 접근성 퇴행 · 배포 크래시 |
| `test_naming.py` (7) | 옛 식별자 · PD 주장 · 공개 컬럼 · 문서 정합 · **반출 산출물 문장** · 로고 색인 | 이름이 유도하는 오독 |

```bash
python -m pytest tests -q     # 30 passed
```

### 42-2. 두 진입 방식이 모두 동작해야 한다

`test_sanity.py`·`test_llm_paths.py` 는 "pytest 불요" 독립 스크립트로 설계돼
검증 함수가 공유 컨텍스트를 **인자**로 받는다. 그런데 함수명이 `test_*` 라서
pytest 가 이를 수집하고 인자를 fixture 로 해석해 찾지 못했다.

```
python -m pytest tests -q  →  18 passed, 10 errors
```

**아무것도 깨지지 않았는데 "테스트 10건 오류"로 보인다.** 코드가 멀쩡한지와 무관하게,
남이 관례대로 돌렸을 때 **거짓 실패를 보는 것 자체가 결함**이다.

→ `tests/conftest.py` 신설. 격리 원칙도 그대로 옮겼다 — 임시 디렉터리에서만 쓰고,
세션 종료 시 `data/processed` 지문(파일명·크기·mtime) 불변을 확인하며,
모의로 치환한 `src.llm._http_post` 를 실제 함수로 되돌린다.

### 42-3. 문서 수치 자동 대조 (142건)

```bash
python tools/check_doc_numbers.py
```

문서에 적힌 숫자를 **산출물에서 다시 계산해** 대조한다. 그리고 **폐기된 표기**
(옛 모델명·옛 컬럼명 등)가 남아 있어도 실패시킨다.

**이 도구 자신이 두 번 뚫렸다:**

**뚫림 ①** `need()` 는 "정답이 문서에 있는가"만 봐서, **옛 값이 함께 남아 있어도
통과**하는 사각지대가 있었다(실제로 헤드라인에 옛 수치가 남았다).
→ 같은 패턴에 매칭되는 다른 값이 남아 있으면 실패시키는 검사 추가.

**뚫림 ②** `DOCS` 목록에 MODEL_USE_SPEC·RATING_CONCEPT·METHODOLOGY·OPERATIONS 가
**아예 없었다.** 그래서 88/88 통과인데 그 안의 수치는 낡아 있었다 —
FS3 등급이 404개로 적혀 있었지만 실제 367개, 학습 모집단 밖이 708개(49.1%)로 적혀
있었지만 550개(38.1%).

> **검사 대상에 없으면 통과 숫자는 안전의 증거가 아니라 착시다.**

더 나쁜 사례가 이어졌다. OPERATIONS 를 **등록해 놓고 정작 어떤 `need()` 호출도 그
문서를 겨냥하지 않아 실질 검사 0건**이었다. 그 사이 배포 가부를 가르는 게이트 문서의
근거 숫자가 전부 낡았다:

| 항목 | 낡은 값 | 실제 |
|---|---|---|
| 큐 규모 | High 145 / Medium 288 / Low 1,009 | FS3 367 / FS2 632 / FS1 443 |
| 전량 검토 소요 | 약 15주 | 약 **37주** |
| 게이트 근거 | +0.775, CI 하한 0.602 | **+1.059**, CI [1.003, 1.115] |
| fold 변동폭 | 2.617 / 2.252 / 2.500 (±15%) | 2.818 / 2.752 / 2.667 (**±3%**) |

**게이트를 규정한 문서가 게이트를 안 받고 있었다.**

검사 건수 이력: 37 → 63 → 83 → 88 → 108 → 114 → 115 → 117 → 118 → 141 → **142**

### 42-4. 린트

`ruff.toml` + `requirements-dev.txt` 로 규칙과 버전을 함께 고정.
설정이 없으면 ruff 버전마다 결과가 달라 **"린트 통과" 주장이 검증 불가능**해진다.

### 42-5. 재현성

- 새 클론 + `data/raw` 스냅샷만으로 API 호출 없이 전 지표 재현 (**bit-identical 확인**)
- 라벨 규칙·임계값이 학습 **전** git 커밋(`cfcf164`)으로 동결 — 사후 조정 여부를
  이력으로 검증 가능
- seed 42 고정, 상관 시뮬레이션 재현성 테스트

---

## 43. 우리가 스스로 잡은 결함 — 전수 기록 (결함 103건)

아래는 **개발 중 실측으로 발견해 수정한 결함**의 전수 목록이다. 외부 지적이 아니라
스스로 찾은 것이 대부분이며, 전부 커밋 이력에 남아 있다.

### A. 데이터 · 라벨 · 누출 (13건)

| # | 결함 | 어떻게 발견 | 조치 |
|---|---|---|---|
| 1 | **표본 선별 룩어헤드** — 최종 점포수로 표본 선정 | 적대적 리뷰 | cummax·연속구간으로 실시간 자격 판정 |
| 2 | 미검증 '양호' 라벨 27% | 〃 | 판정 가능 요건 추가 |
| 3 | 0점포 유령 라벨 양성 11.6% | 〃 | 아티팩트 결측 처리 |
| 4 | 피처 스키마가 t+1 값에 의존 (분산 0 사후 제거) | **누출 테스트가 검출** | 업종 더미를 범주 2개 이상일 때만 생성 |
| 5 | 누출 테스트 사각지대 — 합성 패널이 신규 9컬럼 미생성 | 코드 리뷰 | `PANEL_VALUE_COLS` 단일 정의 + 커버리지 가드 2종 |
| 6 | **그 커버리지 가드가 항상 실패** → 52개 전 피처 누출 검증 미실행 | 리뷰 | 합성 픽스처가 커버리지 모사하도록 수정 |
| 7 | 윈저라이즈가 이진 플래그에 적용 — 계속기업 플래그(0.1%) 전멸 직전 | 리뷰 | 이진 컬럼 제외 |
| 8 | 손익 라벨을 문서 전체에서 탐색 → 다른 표 값 혼입(`revenue=2017.0`) | 실측 | 손익계산서 구간 앵커링 + 부호 이중반전 수정 |
| 9 | [기재정정] 감사보고서가 후보에서 제외 → 폐기된 원본 수치 사용 | 실측 | 후보 포함 |
| 10 | 실패 응답을 영구 캐시 → 일시 오류가 "재무 없음"으로 고착 (3곳) | 리뷰 | 실패는 캐시하지 않음 |
| 11 | 정보공개서 데모 판정이 **fail-open** | 리뷰 | 판정 불가는 안전한 쪽으로 닫는다 |
| 12 | UI 가 브랜드명 문자열로 본부 조회 → `dart.py` 가 막은 오배정 부활 | 실측 | 확정 매칭만 사용 |
| 13 | 라벨 구성표가 자격 이전 프레임 → 두 문서가 서로 다른 값 인용 | **문서 작성 중 대조** | scope 컬럼 + 파일 분리 |

### B. 모형 · 보정 · 등급 (15건)

| # | 결함 | 영향 | 조치 |
|---|---|---|---|
| 14 | logloss 조기종료 | 순위 성능과 어긋남 | valid AUC 기준(`first_metric_only`) |
| 15 | **워크포워드 집계 편향** — 풀링 Lift 가 모든 fold 보다 낮음 | 재학습 모형에 불리한 인위적 편향(배수 0.12) | fold 안에서 계산, `macro_avg_folds` 주 지표화 + 편향 진단표 공개 |
| 16 | 확률 포화 — 1.000(3행) / 1.4e-08(830행) | 화면에 "악화확률 100.0%" | 하한 0.03% · 상한 99% 클립 |
| 17 | 확률 하한 위반 27행 (정확히 0.0 이 1행) | "절대 악화하지 않는다"는 주장 | 클립 적용 |
| 18 | 보정확률 동률 붕괴 | 상위 브랜드 순서 없음 | 원점수 순위 tie-break |
| 19 | **정확성 검정 불합격** HL p=0.00027 | 과소예측 (은행에 불리) | 보정 재설계 3단 (§18-3) |
| 20 | HL 구간을 값 분위로 절단 | 같은 자료 p=0.00000 vs 0.018 — **세 자릿수 차이** | 순위 등분할 |
| 21 | **보정기 계단 조회 실패 → 조용히 기저율 대체** | 14행 중 **12건 등급 오류**, 11건이 FS3 인데 FS2 로 표시 | `searchsorted` 로 계단 함수 정확 평가 |
| 22 | 축소 보정이 단조성 붕괴 | 원점수가 나쁜 브랜드가 낮은 확률 | 가중 PAV 단조 사영 |
| 23 | **등급 배정 척도 오류** — 컷은 계단값에서 도출, 적용은 보간값에 | **48개 브랜드 등급이 틀림** | 등급은 계단값으로, 표시값은 구간 내 클립 |
| 24 | 등급 컷 왜곡 High 29% | 설계값 초과 | 순위 기반 8.3% (이후 절대 컷으로 전면 교체) |
| 25 | **등급 컷 순환논증** | 컷 선택에 쓴 데이터로 컷 검증 | 절차 OOT + 라벨 치환 귀무검정 |
| 26 | 대시보드 스트레스 방향 반전 | 스트레스가 더 낮게 표시 | 동일 LGD 짝 맞춤 |
| 27 | **층위 이중계상** — ρ_W(브랜드 내부)를 브랜드 간에 사용 | UL 290.6억 → **60.6억으로 감소** (헤드라인이 줄어드는 방향) | 차주 단위 2단계 요인 모형 |
| 28 | **개발표본 ≠ 적용모집단** | 2024 코호트 38.1% 가 모델이 본 적 없는 모집단 | 상태 컬럼 + 화면 배지 + 실현율표 |

### C. 검증 지표 계산 (5건)

| # | 결함 | 증상 | 원인 |
|---|---|---|---|
| 29 | **자기 자신을 채점** — 배포 보정기로 보정 검정 | HL p=1.0, ECE=0.0000 | 같은 데이터로 적합된 보정기 |
| 30 | 점수 PSI 1.41 | 정상 범위 밖 | 서로 다른 모형 3개 점수를 섞음 |
| 31 | 이행행렬 FS3 유지율 42.2% → 6.0% 붕괴 | 대각 우세 실패 | fold 모형 원점수 척도 차이 |
| 32 | 보정 불공정 비교 — LGBM 에만 isotonic | "Brier 최우수" 주장 | 기준모형에도 동일 보정 적용 → **주장 철회** |
| 33 | 게이트 완화 판정 오류 — 평균 우위만 보고 "채택 가능" | 두 완화안 모두 통과 판정 | **프로젝트 자신의 t-CI 기준을 게이트에 넣으니 둘 다 기각으로 뒤집힘** |

> **#29~31 은 세 번 다 원인이 같았다: "다른 것을 비교하고 있었다."**
> 그리고 세 번 다 **좋은 쪽으로** 틀려 있었다. 검증 지표는 잘못 계산하면 좋게 나온다.

### D. LLM · AI 층 (13건)

| # | 결함 | 영향 |
|---|---|---|
| 34 | RAG 색인이 `__main__.RagIndex` 피클 | **화면의 근거 인용이 죽어 있었다** |
| 35 | LLM 테스트가 주 키만 격리 | 예비 키 등록 즉시 4/4 → **2/4** |
| 36 | 뉴스 대상을 위험 상위만 선정 | 262건 중 **위험 이벤트 0건** ('오마이걸 미미' 등 동명이인) |
| 37 | 시의성 필터 부재 | 2012·2015년 기사가 '뉴스 경보'로 노출 |
| 38 | `--demo` 격리 붕괴 | 실제 RSS 호출 + git 추적 데이터 오염 |
| 39 | 심사메모 환각 검증 부재 | 프롬프트 지시만 있고 출력 검증 없음 |
| 40 | 분류 체계에 **'수요급감'** 자리 없음 | 탕후루 매출 92% 급감 기사를 양쪽 모두 '기타'로 흘림 |
| 41 | 뉴스 근거에 브랜드명 미포함 기사 인용 | 「더보이즈 영훈, 인생 첫 중국냉면」 |
| 42 | 브랜드 인식 오탐 | '가장'→`가장맛있는족발`, 'sk하이닉스'→`하이오커피` |
| 43 | Gemini `AQ.` 키 형식 오판정 | **멀쩡한 키를 우리가 차단** → 판정 철회 |
| 44 | `chat.py` 가 reason 자리에 intent 전달 | 원인과 무관한 안내문 |
| 45 | 429(한도)를 "모델 미연결"로 표기 | 사용자가 키 설정을 의심하며 엉뚱한 곳을 고침 |
| 46 | 비전 모델 선택 오류 | `gemini-2.5-flash` 가 판독 불가 → **검증이 전부 기각** |

### E. 화면 · 디자인 (15건)

| # | 결함 | 실측 |
|---|---|---|
| 47 | 아이콘 폰트 충돌 | 사이드바에 `keyboard_double_arrow_right` **글자**가 찍힘 |
| 48 | 사이드바를 닫으면 다시 못 엶 | 펼치기 버튼이 숨긴 툴바의 자식 (실측 0×0) |
| 49 | 카드 스타일 전체 사망 | `stVerticalBlockBorderWrapper` 가 DOM 에 **0개** |
| 50 | `st.cache_data` 밑줄 인자 | 파일 수정시각을 `_m` 으로 받아 **캐시가 영원히 무효화 안 됨** |
| 51 | `st.plotly_chart` 기본 테마 | **KB 템플릿을 통째로 덮어씀** (marker 색만 살아남아 먹은 줄 알았다) |
| 52 | 사이드바 비활성 메뉴 대비 **1.14** | 특이도 역전 — 사실상 비가시 |
| 53 | 의미색 4종 대비 미달 | 2.59 / 3.94 / 4.42 / 2.79 |
| 54 | 타이포 29종 난립 | 렌더 28종 → **8단계로 수렴** |
| 55 | 지표 이름 말줄임 | 1024px 에서 '주의 · 100점포 이상' 잘림 |
| 56 | 비율 서식 오류 | 45.3% 가 화면에 **"0.5%"** 로 표시 |
| 57 | `chat avatar="◆"` | 이모지가 아니어서 **AI 상담 화면 전체 크래시** |
| 58 | 부문 위치 표기 | 점포 성장률 **−46.1%**(하위 11%)가 "상위 89%" 로 **좋아 보임** |
| 59 | 동점 처리 없음 | 직영점 0개처럼 몰린 지표의 백분위가 0 |
| 60 | 등급 명칭 불일치 | 범례 'FS1 안정' vs 카드 '양호' |
| 61 | 지표 명칭 불일치 | 한 화면만 '브랜드 리스크', 나머지는 '악화 가능성' |

### F. 운영 · 배포 (8건)

| # | 결함 | 증상 |
|---|---|---|
| 62 | 진입 스크립트가 theme 상수 참조 | 배포본 `AttributeError: theme.FS_XS` — **앱 전체 정지** |
| 63 | `load_scores()` 가 **파일 수정시각**으로 데이터 선택 | 배포 `KeyError: 'deterioration_1y'` — **앱 전체 정지** (같은 자리 2번째 사고) |
| 64 | Windows 콘솔 cp949 | '—' 출력 실패 → 산출물은 다 만들어졌는데 **스케줄러에 실패로 기록** |
| 65 | **CI 시크릿 미등록** | 매일 success 인데 **축소 가동** 중 (`gh secret list` 공백) |
| 66 | `news_llm` 이 구 개발자센터 키만 조회 | HUB 키만 있는 환경에서 제목만으로 폴백 |
| 67 | `.env` 를 utf-8 로 읽어 BOM | **첫 키가 조용히 무시됨** |
| 68 | Python 버전 미지정 | Cloud 가 3.14 선택 → wheel 부재 → 무한 빌드 (**"Resolved 67 packages" 까지는 정상**) |
| 69 | pytest 진입 | 아무것도 안 깨졌는데 **10건 거짓 실패** |

### G. 문서 · 명칭 · 산출물 정합 (16건)

| # | 결함 | 방향 |
|---|---|---|
| 70 | `synthetic` 플래그 하드코딩 `True` | **과소 주장** — 실측을 쓰면서 "합성"이라고 알림 |
| 71 | **PD 명칭 모순** — 두 문서 정면 충돌 + 공개 컬럼명 `pd_*` | 우리가 이름으로 오독을 유도 |
| 72 | `check_doc_numbers` DOCS 에 4문서 누락 | 88/88 통과인데 그 안이 낡음 |
| 73 | OPERATIONS 등록만 하고 `need()` 0건 | **게이트 문서가 게이트를 안 받음** |
| 74 | 폐기값 탐지기 사각지대 | 옛 값이 함께 남아도 통과 |
| 75 | 문서 3곳이 쓰지 않는 모델명 기재 | 3.6 계열 이름 vs 실제 `gemini-2.5-flash` (폐기표기 스캔에 등록) |
| 76 | README 가 저장소 자신과 모순 | 좁은 CI 를 채택 → **주장 철회** |
| 77 | "개인정보 0" 오기재 | 대표자 성명 84,368행 존재 |
| 78 | 커버리지 단일 기준(15.5%)만 기재 | **과소 주장** — 점포 가중 73.6% |
| 79 | 익스포저 '합성' 표기 | **과소 주장** — 60건 전부 공시 실측 기반 |
| 80 | 낡은 상관 산출물 | UL 배수 5.31 → **7.30** (보정기 수정 후 미재산출) |
| 81 | 내가 새 절에 넣은 3.93배 | **되돌린 실험의 값** — 폐기값 스캔이 검출 |
| 82 | `portfolio_summary.json` 설명문 파손 | 개명 때 조사가 깨져 "부도확률(PD)이 사용한 것으로" |
| 83 | 데이터셋 종수 6종 기재 | 실제 7종 (창업비용 추가 후 미갱신) |
| 84 | `test_naming` 이 CSV **하나만** 검사 | 확장 트랙 옛 컬럼명 미검출 → 배포 사망 |
| 85 | 출처 없는 외부 사건 인용 3곳 | 규정 인용은 조문까지 정밀한데 시장 통계만 무출처인 **비대칭** |

### H. 진단 엔진 · 로고 (18건)

| # | 결함 | 실측 |
|---|---|---|
| 86 | `STARTUP_COST_HIGH` **원리적 발동 불가** | 대상연도 창업비용이 전 브랜드 결측 → 수정 후 **185건 발동** |
| 87 | 죽은 함수 3개 | `brand_search.suggest`, `diagnosis._churn_rate`, `naver.local_places` |
| 88 | 규칙 단순 합산 | 상관된 소견 3~5개가 겹쳐 **+126점** — 검증된 모델 성분(최대 100) 압도 |
| 89 | 조사 처리 없음 | "3,879만원**는**" · "1.6억원**로**" |
| 90 | 비율 소견 최소 분모 없음 | 점포 5개 브랜드가 "계약종료 40%" 로 최상위 |
| 91 | 노후 재무를 현재형 단정 | 2019년 감사보고서 |
| 92 | 면적당매출 아티팩트 | −85% 인데 매출 +62% (면적 기재 변경) |
| 93 | 데이터 공백에 위험 가점 | `HQ_NO_DATA` → info 방향, 가중치 0 |
| 94 | og:image 무검증 수용 | 배너·SNS버튼·카톡캡처 → **32건 철회**, 커버리지 43.2%→40.9% |
| 95 | 도메인 해석 오류 | 일미리금계찜닭→김해시청, 생어거스틴→카카오예약 (**224건 차단**) |
| 96 | `prune_shared_logos` 가 이미지검색 로고 삭제 | 출처 구분 추가 |
| 97 | 이중 검증으로 대량 폐기 | 도메인 1,326개 중 **1,078개(81%)** 가 JS 렌더 사이트에서 실패 |
| 98 | ICO 기본 프레임 | PIL 이 16~32px 로 열어 **256px 프레임을 '너무 작음'으로 탈락** |
| 99 | 로고 색인 경합 | **PNG 556장이 디스크에 있는데 색인은 0건** |
| 100 | **철회 로고가 화면에 계속 표시** | 화면은 색인이 아니라 파일 존재로 판단 (3건) |
| 101 | `collect_logos` 가 `verify=False` 미전달 | `brand_logo()` 만 고치고 캐시 경로를 놓침 |
| 102 | `official_domain` 이 창업 포털 채택 | 58개 도메인이 **545개 브랜드 오염** |
| 103 | `ifrmp_web` 컬럼 오프셋 | `재무제표작성여부` 빈 열이 페이지마다 있기도 없기도 → 값이 한 칸씩 밀림 |

---

## 44. 우리가 철회한 주장 — 7건

**결과가 나빠지는 방향의 수정**만 모았다. 이것이 정직성의 실제 증거다.

| # | 철회한 주장 | 왜 | 결과 |
|---|---|---|---|
| 1 | "LightGBM 이 단일변수 대비 **유의하게** 우위" | 좁은 부트스트랩 CI 채택은 정직하지 않음 | 우위 주장 **1개로 축소** (persistence 대비만) |
| 2 | "Brier 최우수" | 보정 불공정 비교 | 문구 철회 |
| 3 | "브랜드 상관으로 UL **290.6억**" | 층위 이중계상 | **60.6억으로 하향** |
| 4 | "BHC·교촌·한솥은 구조적 한계" | 맞는 수집원을 안 쓴 것 | 8건 전부 확보 |
| 5 | "AQ. 키는 OAuth 토큰이라 사용 불가" | 키가 무효였던 것 | 판정 철회, 형식은 순서 정렬에만 |
| 6 | "개인정보 0" | 대표자 성명 84,368행 존재 | 실태 표로 대체 |
| 7 | 로고 커버리지 **43.2%** | 철회분·실체 없는 색인 포함 | **42.4%** 로 하향 |

**#7 은 이 문서를 쓰기 직전에 일어났다.** 로고 색인과 디스크가 어긋나 있었고,
그 결과를 보고한 숫자가 실제보다 높았다. 불변식을 테스트로 못박고 숫자를 낮췄다.

---

## 45. 우리가 기각한 것 — 6건

**할 수 있었지만 하지 않은 것**들이다. 각각 근거가 있다.

### 45-1. 표본 게이트 완화 — 커버리지를 2배로 늘릴 수 있었다

커버리지 15.5% 는 이 프로젝트가 가장 크게 지적받는 한계다. 자격을
30개/3년 → 10개/2년으로 낮추면:

| 지표 | 현행 | 완화B |
|---|---|---|
| 라벨 | 3,635 | **7,163 (+97%)** |
| 사건 | 335 | +143% |
| 배포 코호트 | 1,442 | **3,063 (32.8%)** |
| ECE | 0.0354 | **0.0148** (개선) |
| HL p | 0.158 | **0.656** (개선) |
| AUC | 0.7105 | 0.7058 (사실상 불변) |

**실제로 config 를 바꿔 전 파이프라인을 끝까지 돌려 확인했다.** 커버리지도 늘고
보정도 좋아진다. 그런데 fold 간 편차가 커진다:

```
현행   +1.006 / +1.001 / +1.167          →  t-CI [+0.824, +1.292]   유의
완화B  +0.280 / +0.493 / +0.591 / +1.497 →  t-CI [-0.139, +1.570]   비유의
```

**이 도구의 유일한 통계적 우위 주장 — "모형이 전년 상태 유지를 유의하게 넘는다" —
이 사라진다.**

연도블록 부트스트랩으로 재면 완화B 도 유의하게 나온다(+0.725 [+0.358, +1.247]).
그러나 README 는 **두 산출물이 어긋날 때 좁은 쪽을 고르는 것은 정직하지 않다**고
이미 못박고 t-구간을 채택했다.

> **기준을 바꿔 결론을 살리지 않는다.**

전 파이프라인 재산출분을 **되돌리고** 현행 게이트를 유지했다.

**그리고 이 판정 자체가 한 번 틀렸다.** `tools/gate_sensitivity.py` 는 처음에
'평균 우위 ≥ 0.3' 하나만 보고 **두 완화안 모두 "채택 가능"** 을 냈다. 프로젝트
자신의 기준인 fold t-구간을 게이트에 넣으니 **둘 다 기각으로 뒤집혔다.**
그 경위를 파일 상단에 적었다.

### 45-2. 캡차 우회

공정위 웹 열람에서 세션당 5건 후 캡차. **rate limit 이 아니라 자동화 차단 장치**다.
우회하지 않았다 (§8-4).

### 45-3. 요주의 전용 모형

만들었으나 계약종료율 단일변수보다 못했다(AUC 0.6937 vs 0.7066). 기각하고
실현율 공표로 대체 (§17-4).

### 45-4. 순위 기반 보정

fold 척도 문제를 깨끗이 없애고 Spiegelhalter 도 통과한다. 그러나 **등급 컷이 다시
순위 컷이 된다** — 절대 등급으로 바꾼 이유가 무효화된다 (§18-3).

### 45-5. 부문 점수 합성

6개 부문 → 가중 합성 등급을 검토했으나 **가중치를 정당화할 근거가 없다.**
3개 부문만 남기고 점수를 매기지 않는다 (§22).

### 45-6. 가명·구간값 데이터

15143710/15143709 를 쓰면 커버리지가 늘지만 **변화율을 계산할 수 없다.**
이 모형의 핵심 신호는 수준이 아니라 변화다 (§8-2).

---

## 46. 되돌린 작업 — 전 파이프라인 재산출 후 원복

가장 비용이 큰 종류의 정직성이다.

| 되돌린 것 | 이유 |
|---|---|
| 게이트 완화 전 파이프라인 재산출 | t-CI 유의성 상실 (§45-1) |
| 로고 32건 (og:image 출처) | 배너·SNS버튼이 로고 자리에 |
| 로고 3건 (철회 잔존 파일) | 색인에서 뺐는데 화면에 남아 있었다 |
| UL 290.6억 → 60.6억 | 층위 이중계상 |

---
---

# 제8부 · 한계와 로드맵

## 47. 명시적 한계

### 47-1. 데이터

| 한계 | 크기 | 해소 경로 |
|---|---|---|
| **본부 재무 커버리지** | 브랜드 11.4% / 점포가중 39.7% | 정보공개서 **정식 API 키** (공공데이터포털) — 유일한 구조적 해법 |
| 표본 자격 | 브랜드 15.5% (점포가중 73.6%) | 완화는 판별력 주장을 잃는다 (§45-1) |
| 매출 결측 | 60~82% | 공시 특성 — 소규모 브랜드가 매출 미기재 |
| 검색수요 | 292개 브랜드 | 네이버 한도 내 일간 확장 중 |
| 뉴스 | 9개 브랜드 | 〃 |
| 로고 | 42.4% / 점포가중 67.7% | Gemini 할당량 (프로젝트 단위) |

### 47-2. 모형

- **라벨이 부도가 아니다.** 실제 여신 부도·연체 라벨을 한 건도 보지 못했다.
- **요주의 구간에 모델 성능 근거가 없다.** 실현율표로 대체했으나, 그 표는 순위를
  매기지 않는다.
- **단일변수·로지스틱 대비 우위가 통계적으로 동등하다.** LightGBM 유지 근거는
  성능이 아니라 SHAP 설명력이다.
- **절제 실험상 수준+변화 13개 피처만으로 Lift 2.500** — 피처를 늘린 것의
  한계효용이 작다.
- 대표성 PSI 적색 14건 — 개발표본과 적용모집단의 구조적 차이.

### 47-3. 제품

- 점검 큐 상태는 **클라우드 재배포 시 초기화**된다 (인증·이력·결재 연동 없음).
  공식 기록은 엑셀 반출로 넘겨야 한다.
- 여신 익스포저는 **공시 창업비용 × 두 가정**의 추정치다. 절대 금액이 아니라
  **구조를 보는 용도**다.
- 자동 여신 결정에 사용하지 않는다 (금지 용도).

### 47-4. 사람 개입이 필요한 항목

| 항목 | 현재 |
|---|---|
| 정보공개서 정식 API 키 | 공공데이터포털 신청 대기 |
| KOSIS API 키 | 미발급 |
| 네이버 데이터랩 scope | 권한 필요 |
| GitHub Actions 시크릿 | **미등록** — 배선은 완료, 등록만 남음 |

---

## 48. 로드맵

### 48-1. 즉시 (키만 있으면)

1. **정보공개서 정식 키** → 본부 재무 11.4% → 95%+ (파서·정합 이미 완성)
2. **CI 시크릿 등록** → 일간 배치 정상 가동 (배선 완료)
3. **Gemini 프로젝트 추가** → 로고 42.4% → 완주

### 48-2. 은행 내부 데이터 연결 시

| 붙이는 것 | 열리는 것 |
|---|---|
| 실여신 CSV (3컬럼) | 절대 금액이 실측이 됨. HHI·EL·UL 이 실제 포트폴리오 값 |
| 부도·연체 라벨 | **라벨을 부도로 교체 가능** — 그때는 PD 라 부를 수 있다 |
| 가맹점-브랜드 매핑 | 차주 단위 상관 모형의 분모가 실측이 됨 |

### 48-3. 추가 데이터 경로 (조사 완료, 미실행)

| 경로 | 상태 |
|---|---|
| `file.localdata.go.kr` 직접 다운로드 (일반음식점 694MB, 폐업일자 최신) | 조사에서 확인 — 후속 과제 |
| 공정위 보도자료 PDF (차액가맹금·세부업종 개폐점률) | 미착수 |
| DART 음차 매칭 확장 | 확정 3/7 — 효과 제한적 |

### 48-4. 기능

- **「판정 검토」 기능** — 최소 결정 집합 k 를 재예측으로 검증하는 설명 층.
  `Σ pred_contrib = logit(score_raw)` 최대 오차 **3.997e-15** 확인.
  SHAP 차감이 재예측과 **12.8%** 에서 불일치한다는 것도 측정 완료. 설계 완료·미구현.
- 개발 명세서 (평가기준·산출물·검증항목 정의) — 미착수

---
---

# 부록

## A. 주요 산출물 색인

| 파일 | 내용 |
|---|---|
| `outputs/scores_latest.csv` | 배포 코호트 점수·등급·SHAP 요인·상태 |
| `outputs/metrics.csv` | 단일분할 지표 (모형 4종 × 보정 전후) |
| `outputs/metrics_bootstrap_ci.csv` | 부트스트랩 신뢰구간 |
| `outputs/walkforward_metrics.csv` | 워크포워드 fold별·macro·풀링 |
| `outputs/walkforward_delta_ci.csv` | 모형 간 격차 CI |
| `outputs/walkforward_pool_bias.csv` | **집계 편향 진단표** |
| `outputs/grade_bands.json` | 등급 컷·실적표·절차 OOT·귀무검정 |
| `outputs/grade_validation.csv` | 연도별 등급 실현율 |
| `outputs/validation/summary.json` | 판별력·정확성·안정성 종합 |
| `outputs/validation/transition_matrix.csv` | 이행행렬 (이탈 열 포함) |
| `outputs/validation/psi_features.csv` | 대표성/안정성 분리 PSI |
| `outputs/validation/grade_binomial.csv` | 등급별 이항검정 |
| `outputs/validation/grade_system_comparison.json` | 신·구 등급체계 정량 비교 |
| `outputs/watch_base_rates.csv` | **요주의 구간 실현율표** |
| `outputs/brand_correlation.json` | ρ_W · ρ_B · 층화 결과 |
| `outputs/correlation_impact.json` | 손실 분포 · UL 배수 · Euler 배분 |
| `outputs/brand_ul_contribution.csv` | **브랜드별 꼬리 기여·쏠림 배수** |
| `outputs/portfolio.csv` / `portfolio_summary.json` | 여신 집중도·EL·근거 표기 |
| `outputs/label_composition.csv` | 라벨 구성 (자격 통과 표본) |
| `outputs/label_composition_pregate.csv` | 〃 (자격 이전) |
| `outputs/attrition_report.csv` / `attrition_summary.json` | 표본 이탈 회계 |
| `outputs/survival_report.csv` | 생존성 진단표 |
| `outputs/entity_resolution_report.csv` | 엔티티 정합 품질 |
| `outputs/dart_match_report.json` | DART 정합 + 재무 검산 |
| `outputs/llm_eval.json` | **규칙 vs LLM 정량 비교** |
| `outputs/rag_stats.json` | RAG 코퍼스 통계 |
| `outputs/gate_sensitivity.json` | 게이트 완화 민감도 (기각 근거) |
| `outputs/ablation.csv` | 피처 그룹 절제 실험 |
| `outputs/newbrand_split.csv` | 부분표본 성능 |
| `outputs/brand_diagnosis_meta.json` | 진단 소견 통계 |

## B. 문서 색인

| 문서 | 내용 |
|---|---|
| `README.md` | 진입점 — 문제 정의·핵심 결과·재현 절차 |
| **`docs/TECHNICAL_REPORT.md`** | **본 문서 — 전 과정 기술서** |
| `docs/METHODOLOGY.md` | 방법론 공개서 (평가사 형식) |
| `docs/MODEL_USE_SPEC.md` | 사용 범위·금지 용도 명세 |
| `docs/OPERATIONS.md` | 운영·거버넌스 설계서 |
| `docs/IMPLEMENTATION.md` | 명세 → 코드 전수 대조 |
| `docs/INTERFACES.md` | 모듈 계약 |
| `docs/LEAKAGE_CHECKLIST.md` | 누출 점검 체크리스트 |
| `docs/RATING_CONCEPT.md` | 등급 체계 설계 근거 |
| `docs/DESIGN_SYSTEM.md` | 디자인 토큰·접근성 기준 |
| `docs/AI_USAGE.md` | AI 활용 내역 |
| `DEPLOY.md` | 배포·폐쇄망 이식 |

## C. 재현 절차

```bash
# 1. 환경
python -m venv .venv && .venv/Scripts/activate     # Windows
pip install -r requirements.txt

# 2. 전 파이프라인 (data/raw 스냅샷이 있으면 API 키 불필요)
python run_pipeline.py --all

# 3. 검증
python -m pytest tests -q              # 30 passed
python tools/check_doc_numbers.py      # 142건 대조
python -m ruff check .                 # All checks passed

# 4. 화면
streamlit run src/app.py
```

## D. 개발 이력 요약

| 시기 | 주요 작업 |
|---|---|
| 2026-07-30 | M1~M5 전 모듈 구현 + 실데이터 백테스트 + 문서화 |
| 2026-07-31 | 적대적 리뷰 22건 수정 · Gemini 전환 · **상관 실증 신설** · 운영 설계서 · LLM 정량 평가 · 여신 익스포저 실측화 |
| 2026-08-01 | DART·정보공개서 층 · KB 화면 전면 개편 · 진단 엔진 · 네이버 연동 · 배포 · **절대 등급체계 전환** · 검증 산출물 |
| 2026-08-02 | 보정 재설계 · 보정기 결함 2건 · 명칭 모순 해소 · 요주의 실현율표 · **게이트 완화 측정 후 기각** · 심사 지적 반영 |
| 2026-08-03 | 로고 3층 재설계 · 데이터 보강 5경로 실측 · **등급 컷 순환성 제거** · Euler 배분 · 폐쇄망 문서 · pytest 진입 복구 · **본 문서** |

---

## 맺음 — 이 프로젝트가 지킨 것

1. **전제를 주장하지 않고 측정했다.** ρ_W = 0.416 을 재고 나서 브랜드 관리를 말한다.
2. **없는 근거를 만들지 않았다.** KB 내부 수치·부도 라벨·정보공개서 전량 수집 —
   전부 "없다"고 적고 그 자리를 비워 뒀다.
3. **불리한 결과를 먼저 적었다.** 과소예측·부분표본 순위·절제 실험·커버리지 —
   전부 우리에게 불리한 방향의 사실이다.
4. **결론을 살리려고 기준을 바꾸지 않았다.** 게이트 완화는 모든 지표가 좋아졌지만
   유일한 유의성 주장을 잃어 기각했다.
5. **넘지 않을 선을 넘지 않았다.** 캡차를 우회하지 않았고, 개인정보 이력을
   재작성하지 않았다.
6. **틀린 것을 찾으면 되돌렸다.** 결함 103건, 7건의 철회, 4건의 원복이 커밋
   이력에 그대로 남아 있다.

> 이 문서에 적힌 모든 숫자는 `python tools/check_doc_numbers.py` 로 확인할 수 있다.

