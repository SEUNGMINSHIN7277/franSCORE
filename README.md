# FranSCORE

**프랜차이즈 브랜드 여신 포트폴리오 집중·상관 리스크 관리 — KB 제8회 AI Challenge 프로토타입**

### ▶ 가동 중인 서비스 — **<https://franscore.streamlit.app>**

바로 눌러 보실 것: **FRANSCORE** 화면 상단 검색창에 브랜드명을 두 글자만 입력 → 후보에서
하나 선택 → **「진단 근거 보기」**. 그 브랜드가 왜 그 등급인지 공시 원수치까지 내려갑니다.
(2024년 공시 기준 1,442개 브랜드 · 매일 06:00 KST 자동 갱신)

### 📄 전 과정 기술서 — **[docs/TECHNICAL_REPORT.md](docs/TECHNICAL_REPORT.md)**

문제 정의·시장 조사부터 데이터·모형·AI 활용·운영까지 개발 전 과정을 한 문서에
담았습니다. **우리가 스스로 잡은 결함 103건 · 철회한 주장 7건 · 기각한 설계 6건**을
전수 기록한 제7부가 이 프로젝트의 실제 모습입니다.

---

## ① 무엇이 문제인가

**자영업자 대출은 1,095.5조원으로 전 금융권 가계·기업대출의 28.5%다.** 그 안에서
**숙박·음식업 사업자대출 연체율은 3.02%** 로, 도소매(1.77%)·제조(2.37%)보다 높고
부동산업(1.10%)의 약 3배 수준이다. 자영업자 차주는 307.8만명인데 그중 **취약차주가 43.7만명**이며,
차주 수는 줄어드는 동안 취약차주만 38.9만 → 43.7만명으로 늘었다.
(한국은행 『금융안정보고서』 2025년 12월)

외식 프랜차이즈는 정확히 이 구간의 한복판에 있다. 공정거래위원회 집계로
**가맹본부 9,960개 · 브랜드 13,725개 · 가맹점 379,739개**이고, 브랜드 수는 전년 대비
10.9%, 가맹점 수는 4.0% 늘었다 (공정거래위원회 「2025년 가맹사업 현황 통계」).

**그런데 이 시장의 위험은 차주 단위로 흩어져 있지 않다.**

| 우리가 실측한 것 | 값 |
|---|---|
| 브랜드 **내부** 자산상관 ρ_W (연도·업종 통제 후) | **0.416** [0.382, 0.438] |
| 브랜드 **간** 자산상관 ρ_B | **0.005** [0.000, 0.009] |
| 한 브랜드의 모든 지역이 동시에 감소한 실측 빈도 | **4.31%** (지역 독립 가정의 21.6배) |
| 차주를 독립으로 볼 때 비예상손실 과소추정 | **7.30배** |

같은 브랜드에 묶인 가맹점들은 함께 무너지고, 서로 다른 브랜드는 거의 무관하다.
**위험은 경기(거시)에 있지 않고 브랜드에 있다.** 그래서 차주를 아무리 잘 심사해도
브랜드 축을 안 보면 대수의 법칙이 작동하지 않는다.

### 왜 이 문제가 은행 것인가 — 그리고 왜 지금 비어 있나

가맹사업법상 **가맹점사업자의 파산·부도·영업정지는 즉시해지 사유**다. 차주가 부도나는
순간 계약이 끊기고 영업권이 소멸한다 — 담보로서의 점포 가치가 채무불이행과 **동시에**
증발한다. 회수율이 브랜드 상태에 직접 묶여 있다는 뜻이다.

그런데 개인사업자 CB·카드사·빅테크가 쓰는 대안정보는 **가맹점 매출·상권·업력·결제정보·
단골고객·부동산** 이다. 전부 **차주(점포) 단위**이고, 그 목록 어디에도 '브랜드 단위
위험도'가 없다(→ [RATING_CONCEPT §2](docs/RATING_CONCEPT.md)). FranSCORE 는 차주 심사를
대체하지 않는다. **차주 심사가 구조적으로 볼 수 없는 축을 하나 더 얹는다.**

> ⚠️ **KB 고유 수치는 여기에 쓰지 않았다.** SOHO 여신 잔액·가맹점 결제 점유 같은 내부
> 지표를 대입하면 위 배수(7.30배)가 곧바로 원화 자본 소요로 환산되지만, 그 값은 공개
> 통계로 확인할 수 없어 **추정치를 지어내지 않는다.** 은행 실여신 CSV 한 장을 붙이면
> 그 자리가 채워지도록 인터페이스를 계약 수준으로 정의해 두었다
> (→ [OPERATIONS §8](docs/OPERATIONS.md)).

**출처**
- 한국은행, 『금융안정보고서』 2025년 12월 — <https://www.bok.or.kr/portal/bbs/P0000593/view.do?menuNo=200068&nttId=10095308>
- 공정거래위원회, 「2025년 가맹사업 현황 통계 발표」 — <https://www.ftc.go.kr/www/selectBbsNttView.do?key=12&bordCd=3&nttSn=47332>

---

> 같은 프랜차이즈 브랜드에 묶인 여신은 '브랜드'라는 공통 요인으로 상관된다.
> **이 전제를 주장이 아니라 실데이터로 측정했다 — 브랜드 내부 자산상관 ρ=0.416(바젤 기업여신
> 가정의 1.7~3.5배)인 반면 브랜드 간은 0.005. 위험은 경기가 아니라 브랜드에 있다.
> 차주를 독립으로 보면 비예상손실을 7.30배 과소추정한다. (아래 ⓪)**
> FranSCORE는 공정위 공개 공시로 **브랜드의 구조악화 전환을 예측(LightGBM)·설명(SHAP)** 하고,
> 이를 은행의 **브랜드별 여신 집중도와 결합해 상관 손실 위험을 원화로 정량화**하는 2선 리스크 관리 도구다.

## 핵심 결과 (실데이터 백테스트)

**데이터:** 공정위 가맹정보 오픈API **7종** 실데이터 — 브랜드×연도 76,717행(실적연도 2016~2024, 20,626개 브랜드) + 브랜드×연도×지역 144만행
**정합:** 브랜드 관리번호(`brandMnno`) 기준 97.8% 확보 (명세 §2.2 준수), 독립 API 간 가맹점수 일치율 **98.9%** 교차검증
**표본(명세 기본):** 외식업 · 점포 30개+ · 3년 연속 → 1,809 브랜드 / 라벨 3,635행 (양성률 9.2%)
**피처:** 49개 (공시 35개 + **가맹본부 재무 14개**, 직영비중·지역분산·면적당매출 포함, **죽은 피처 0개**)
**검증:** 완전 시간분할(train ≤2021 / valid 2022 / test 2023) + **워크포워드 확장창** 3-fold, seed 고정, 라벨 규칙 학습 전 git 동결

### ⓪ 전제 검증 — "브랜드가 공통 위험요인"을 **주장하지 않고 측정**했다

이 도구는 *"같은 브랜드에 묶인 여신은 브랜드라는 공통 요인으로 상관된다"* 는 전제 위에 서 있다.
전제가 거짓이면 브랜드 단위 관리는 불필요하다. 그래서 먼저 측정했다 (`src/correlation.py`).

**식별 전략:** 한 브랜드의 점포는 여러 시도(市道)에 흩어져 있고, 시도마다 상권·지역경기·임대료가
다르다. 점포 감소가 지역별로 **독립**이면 입지 요인이고, 여러 지역에서 **동시에** 일어나면
브랜드 요인이다. 독립 가정을 넘어서는 동시성의 초과분이 곧 브랜드 공통요인이다.

| 통제 수준 | 자산상관 ρ | 무엇을 걷어냈나 |
|---|---|---|
| 통제 없음 | 0.418 | (거시+업종+브랜드 혼재) |
| 연도 통제 | 0.417 | 코로나 등 **거시충격** 제거 |
| **연도+업종 통제** | **0.416** | **브랜드 고유 상관** |

→ 거시·업종을 걷어내도 상관이 **거의 그대로**다. "그냥 코로나 아니냐"는 반론이 수치로 기각된다.
브랜드 자체가 공통 요인이다.

- **브랜드 고유 자산상관 ρ = 0.416** (95% CI **[0.382, 0.438]**, 브랜드 블록 부트스트랩)
  — 바젤 IRB가 기업여신에 **가정**하는 자산상관 12~24%의 **1.7~3.5배**
- 한 브랜드의 **모든 지역이 동시에** 감소한 사례: 실측 **4.31%** vs 지역 독립 가정 0.200% → **21.6배**
- 표본: 브랜드-연도 6,328 · 브랜드 1,606 · 지역쌍 349,089 (2017~2024)

**브랜드 *간*에는 상관이 거의 없다 — ρ_B = 0.005** [0.000, 0.009]

브랜드 단위 악화율의 연도 간 변동에서 역산한 값이다(부도율 시계열 적률법, 8개 연도).
연도별 악화율이 0.53~0.62로 매우 안정적이라 **거시 공통요인은 사실상 검출되지 않는다.**

> 이 대비가 핵심이다. 위험은 **경기(거시)에 있지 않고 브랜드에 있다.**
> 그래서 처방도 "경기 대응"이 아니라 **"브랜드 단위 집중 관리"** 가 정확히 옳다.

**브랜드를 무시한 대가** — 차주(가맹점) 단위 **2단계 요인 모형** 몬테카를로 20만 회

은행 여신은 브랜드 법인이 아니라 **개별 가맹점주**에게 나간다. 그래서 차주를 개별 노드로 두고
브랜드를 중간 계층으로 갖는 모형으로 계산한다 (예시 포트폴리오 = 60개 브랜드 **9,180 차주**):

  `X_ij = √ρ_B·Z(거시) + √(ρ_W−ρ_B)·Y_i(브랜드 i) + √(1−ρ_W)·ε_ij(가맹점)`

| 신뢰수준 | 차주 독립 가정 | 브랜드 상관 반영 | 과소추정 |
|---|---|---|---|
| 95% | 16.5억 | 22.9억 | +6.3억 (+38.3%) |
| **99%** | **17.0억** | **28.0억** | **+11.0억 (+64.3%)** |
| 99.9% | 17.6억 | 36.9억 | +19.3억 (+109.4%) |

> 차주 9,180명을 서로 무관한 소상공인으로 보면 비예상손실(자본 소요분)은 1.7억이지만,
> 브랜드로 묶여 있다는 사실을 반영하면 **12.7억 — 7.30배**다.
> 대수의 법칙이 작동하지 않는 이유가 바로 '브랜드'이고, 이것이 이 도구가 필요한 이유다.

<details><summary>왜 ρ_W 를 그대로 포트폴리오에 넣지 않았나 (층위 이중계상 방지)</summary>

ρ_W = 0.416은 **한 브랜드 안에서 지역끼리** 얼마나 함께 움직이는지다. 브랜드 1개를 채무자
1행으로 집계한 뒤 이 값을 브랜드 *사이* 상관으로 쓰면 이중계상이 된다 — 집계 과정에서
브랜드 내부 상관은 이미 ρ=1로 소진되기 때문이다. 그래서 ρ_W(브랜드 내부)와 ρ_B(브랜드 간)를
따로 측정해 2단계 모형의 제자리에 각각 넣었다. 이 수정으로 헤드라인 수치는 **줄었지만**
(수정 전 대비 크게 낮아졌지만) 층위가 맞다. 자체 감사에서 지적된 사항을 그대로 반영한 결과다.
</details>

<details><summary>이 수치를 읽을 때의 한계 (클릭)</summary>

- 관측 단위는 **'지역 내 가맹점 수 감소'** 이지 차주의 부도가 아니다. 따라서 ρ는 부도상관의
  직접 추정치가 아니라 **대리지표**다. 비교 대상인 바젤 자산상관도 관측값이 아니라 규제 가정값이다.
- 손실 시뮬레이션의 exposure는 **공정위 공시 창업비용에서 유도한 추정치**다(§④ 참조).
  절대 금액보다 **독립 가정 대비 배수**가 이 표의 메시지이며, 배수는 exposure 스케일에 불변이다.
- 추정기 자체의 정확성은 정답을 아는 합성 데이터로 검증한다
  (`tests/test_sanity.py::correlation_math` — 역산 왕복 오차 <1e-3, 독립 데이터에서 ρ≈0,
  심어둔 ρ=0.30 복원, 거시충격만 있는 데이터에서 층화가 0.142→0.003으로 제거).
</details>

### ① 명세 규정 평가 — 단일 시간분할 test 2023 (n=740, base rate 8.1%)

| 모형 | Lift@10% | Precision@10% | PR-AUC | ROC-AUC | Brier |
|---|---|---|---|---|---|
| ① 전년 상태 유지 (persistence) | 1.500 | 0.122 | 0.116 | 0.545 | 0.096 |
| ② 계약종료율 단일변수 | 2.167 | 0.176 | 0.141 | 0.637 | 0.232 |
| ③ 로지스틱 회귀 | 1.833 | 0.149 | 0.167 | 0.728 | 0.197 |
| **LightGBM (주모형)** | **2.667** | **0.216** | **0.171** | **0.723** | 0.115 |
| LightGBM (보정) | 2.667 | 0.216 | 0.171 | 0.723 | **0.075** |

→ **명세 M3 DoD 통과**: LightGBM Lift@10% **2.667 > 기준모형 최고치 2.167**.
단일 분할은 양성이 60건뿐이라 지표 변동이 크다 — 판단은 아래 워크포워드(②)를 기준으로 한다.

**Brier(보정품질)는 공정하게 비교해야 한다 — 자체 감사 지적 반영.** 위 표의 기준모형 Brier는
*미보정* 원점수 기준이라 LightGBM(보정)과 나란히 놓으면 불공정하다. 기준모형 3종에도 **완전히
동일한 isotonic 보정**(같은 valid, 같은 확률 상·하한)을 적용해 다시 비교한 결과:

| 모형 (동일 보정 적용) | Brier |
|---|---|
| **LightGBM** | **0.075** |
| 로지스틱 | 0.077 |
| persistence | 0.078 |
| 계약종료율 단일변수 | 0.078 |

LightGBM이 여전히 최우수지만 **차이는 0.002~0.003으로 미미하다.** 보정을 붙이면 어떤 점수든
Brier는 비슷해진다는 뜻이므로, "Brier 최우수"를 다변량 모형의 주된 실익으로 내세우지 않는다.
실익은 **순위 성능(Lift·PR-AUC)과 SHAP 요인 분해**에 있다. (`outputs/metrics.csv` 에 `*_calibrated` 행으로 전부 공개)

### ② 검정력 보강 — 워크포워드 확장창 3-fold (OOS n=2,063, 양성 191)

단일 연도 test는 양성이 60건뿐이라 통계적 검정력이 부족하다. 이를 **숨기지 않고** 확장창
워크포워드(2021·2022·2023 각각 자기 시점 이전만으로 재학습)로 표본을 3배로 늘려 재검정했다.

**집계는 fold 안에서 한다** (주 지표 `macro_avg_folds` = fold별 Lift@10%의 평균). fold마다
모형을 재학습하므로 출력 확률의 스케일이 fold마다 다르고, 원점수를 전역으로 한 줄 세워
상위 10%를 뽑으면 **재학습 모형이 부당하게 불리해진다** — 실측으로 LightGBM의 2023 fold는
행의 35.9%를 차지하는데 전역 상위 10%의 4.4%만 차지했다(편향배수 0.12).
운영 의미로도 "매년 상위 10%를 점검한다"가 맞는 정의다. 세 가지 집계를 모두 공개한다
(`outputs/walkforward_metrics.csv`, 편향 진단은 `walkforward_pool_bias.csv`).

| 모형 | fold 평균 Lift@10% | fold별 (2021 / 2022 / 2023) |
|---|---|---|
| ① persistence | 1.688 | 1.811 / 1.751 / 1.500 |
| ② 계약종료율 단일변수 | 2.403 | 2.415 / 2.627 / 2.167 |
| ③ 로지스틱 회귀 | 2.526 | 2.617 / 3.127 / 1.833 |
| **LightGBM** | **2.745** | 2.818 / 2.752 / 2.667 |

| 비교 | fold별 Lift 차이 (2020/2021/2022) | 평균 | 95% CI (t, n=3) | 판정 |
|---|---|---|---|---|
| LightGBM − persistence | +1.006 / +1.001 / +1.167 | **+1.058** | **[+0.824, +1.292]** | **유의하게 우위** |
| LightGBM − 단일변수 | +0.403 / +0.125 / +0.500 | +0.343 | [-0.141, +0.826] | 통계적 동등 |
| LightGBM − 로지스틱 | +0.201 / −0.375 / +0.833 | +0.220 | [-1.282, +1.721] | 통계적 동등 |

> **정직한 주장 범위:** LightGBM이 통계적으로 유의하게 넘어서는 것은 기획서가 우려한
> **자기상관(persistence) 함정 하나뿐**이다. 계약종료율 단일변수·로지스틱 회귀와는
> **통계적으로 동등**하며(두 CI 모두 0을 포함), "압도적 우위"를 주장하지 않는다.
>
> ⚠️ **이 표는 정정된 것이다.** 이전 판은 `outputs/walkforward_delta_ci.csv`
> (`bootstrap_unit=year_block`, 블록 3개)를 인용해 단일변수 대비 `[0.218, 0.468]
> 유의하게 우위`라고 적었다. 블록이 3개뿐인 백분위 부트스트랩은 t-구간의 약 1/3.4 폭밖에
> 나오지 않아 유의성을 만들어낸다. 같은 저장소의 `outputs/lift_delta_bootstrap.csv` 는
> 동일 비교를 **[-0.893, +1.589], 승률 0.66(비유의)** 로 낸다. 두 산출물이 모순될 때
> 좁은 쪽을 골라 싣는 것은 정직하지 않다 — 위 표는 fold 값을 그대로 공개하고 t-구간을 쓴다.
>
> **그래도 LightGBM을 쓰는 이유**는 성능 우위가 아니라 **SHAP 기반 요인 분해**다.
> 화면의 진단 소견이 "왜 이 브랜드인가"를 브랜드별 수치로 설명할 수 있는 것은 이 때문이며,
> 순위 성능만 놓고 보면 로지스틱으로 바꿔도 무방하다.
>
> **무엇이 이 차이를 만들었나 — 가맹본부 재무(§2.4).** 본부 재무 층을 넣기 전에는 단일변수
> 대비 +0.063 [-0.183, 0.290] 으로 **통계적 동등**이었다. DART 감사보고서에서 뽑은 본부 재무
> 피처 15종을 더하자 fold 평균 Lift 가 2.456 → **2.745** 로 올랐고, 단일변수 대비 판정이
> 동등 → **유의 우위**로 바뀌었다. 브랜드 지표(결과)만 보던 모형에 본부 재무(원인)를 넣은 것이
> 실제로 값을 했다는 뜻이다.
>
> **커버리지 — 모은 양이 아니라 모형이 본 양.** DART 감사보고서만으로는 자격 브랜드
> 가맹점 가중 39.7% 가 한계였다(외부감사 대상이 아니면 애초에 제출 의무가 없다).
> 그래서 공정위 정보공개서를 사람이 직접 열람해 받아 합쳤고, 현재 모형이 실제로 보는
> 본부 재무는 자격 브랜드 **13.8%** · 가맹점 가중 **48.1%** 다. 나머지 51.9%(가맹점 가중)
> 에 대해서는 이 이득이 없다는 점도 함께 밝힌다. 이 두 수치는 `features.parquet` 의
> `f_hq_has_financials` 에서 직접 재며(`tools/check_doc_numbers.py` §⑥-3), 수집량이
> 아니라 **입력량**이다 — 모아 두고 배선하지 않으면 커버리지가 아니다.

**불리한 결과도 함께 공개한다 (침묵에 의한 체리피킹 방지).**

| 산출물 | 결과 | 해석 |
|---|---|---|
| `newbrand_split.csv` | 기존 브랜드에서 로지스틱 3.014 > LGBM 1.884, 신규 브랜드에서 persistence 2.131 > LGBM 1.522 | **부분표본 양쪽 모두에서 LGBM이 1위가 아니다.** 전체에서 1위인 것은 두 부분표본의 기저율 차이(5.1% vs 15.6%) 때문이며, 부분표본별 최적 모형이 다르다는 뜻이다 |
| `ablation.csv` | 공시 피처 35개 단계 Lift@10 = 2.167 → 본부재무까지 49개에서 2.667 | 브랜드 공시 피처만 누적하는 구간(7→13→22→35개)에서는 Lift가 단조 증가하지 **않는다**. 단일 분할이라 변동이 크므로 이 표 자체를 근거로 삼지 않고, 검정력이 3배인 워크포워드(②)로 판단한다 |

두 결과 모두 이 프로젝트에 불리하지만 산출물에 그대로 남기고 여기에 적는다. 소표본(test 양성 60건)
에서 부분표본 지표는 변동이 크다는 점도 함께 밝힌다.

### ②-2 AI(LLM)가 규칙기반보다 나은가 — **측정했다**

"LLM을 붙였다"는 것만으로는 근거가 아니다. 정답셋을 만들어 같은 입력·같은 지표로 채점했다
(`data/eval/news_gold.jsonl`, 생성기 `tools/build_news_gold.py`, 실행 `--step eval_llm`).

| 구간 | 방식 | 정확도 | macro-F1 | 위험탐지 F1 | 위험 재현율 |
|---|---|---|---|---|---|
| 실수집 41건 | 규칙기반 | 0.488 | 0.381 | 0.500 | — |
| 실수집 41건 | **Gemini** | **1.000** | **1.000** | **1.000** | 1.000 |
| 프로브 18건 | 규칙기반 | 0.444 | 0.432 | 0.636 | — |
| 프로브 18건 | **Gemini** | **0.889** | **0.911** | **0.917** | **1.000** |
| 전체 59건 | 규칙기반 | 0.475 | 0.498 | 0.615 | — |
| 전체 59건 | **Gemini** | **0.966** | **0.945** | **0.933** | **1.000** |

- 정답셋은 **실제 수집된 기사 제목**에 사람이 판정 기준을 적용해 라벨을 붙인 것이다.
  규칙기반이 붙인 라벨을 정답으로 쓰면 규칙이 100점이 되는 순환논증이므로 그렇게 하지 않았다.
- 실수집분에는 본부분쟁·재무이슈·집단폐점 사례가 **0건**이라 그 클래스의 재현율을 잴 수 없다.
  그래서 합성 프로브 18건을 별도로 두고 **결과를 분리 보고**한다(합쳐서 뭉개지 않는다).
  프로브는 규칙이 실패하는 두 방향을 모두 담는다 — *키워드는 있으나 위험 아님*(예: "상생협약
  체결하며 갈등 봉합"), *위험이나 키워드 없음*(예: "전국 매장 절반이 간판을 내렸다").
- 이 평가 과정에서 **분류 체계의 공백**이 드러났다: 실측 기사 "탕후루 유행 꺾이더니 1년 새
  매출 92% 급감"을 규칙·LLM **양쪽 모두** '기타'로 흘려보내고 있었다. 프랜차이즈 여신에서
  가장 중요한 위험 중 하나인데 5분류에 자리가 없었던 것이다 → **'수요급감' 유형을 신설**했다.
  평가셋을 만들지 않았다면 발견하지 못했을 결함이다.
- 표본이 수십 건이므로 소수점을 유의미한 차이로 읽지 말 것. 오분류 31건 전수는
  `outputs/llm_eval_errors.csv` 에 남겨 사람이 직접 확인할 수 있게 했다.
- **측정한 모델을 그대로 운영에 쓴다** (`gemini-2.5-flash`).

**심사메모 환각 자동 검증.** "입력 근거만 인용하라"는 프롬프트 지시는 보증이 아니다.
생성된 메모의 모든 수치를 입력 context와 대조해(반올림·백분율 변환 허용) 근거 없는 수치가
있으면 **메모 본문에 경고를 붙인다**. 회귀 테스트로 지어낸 수치(412.7%, -18.3%)를 실제로
잡아내는 것을 확인한다(`tests/test_llm_paths.py`).

### ③ 표본 트랙 2종 — 둘 다 공개 (체리피킹 방지)

명세 §11 플랜B가 허용하는 확장 표본(전 업종·점포 20+)도 **사전 선언 후 함께** 실행했다.
결과가 좋은 쪽만 고르지 않는다.

| 트랙 | 라벨 표본 | 단일분할 LightGBM Lift | 워크포워드 fold평균 LightGBM Lift |
|---|---|---|---|
| **기본**(명세: 외식·30+) | 3,635 | **2.500** (최우수) | 2.456 (로지스틱 2.472와 동등) |
| 확장(플랜B: 전업종·20+) | 6,406 | 2.082 (로지스틱 2.263에 열위) | 1.993 (로지스틱 2.300에 **열위**) |

→ 업종 혼합은 트리 모형에 불리했다. 명세 표본(외식 특화)이 더 적합하다는 실측 근거.

### ④ 포트폴리오 상관 손실 (공시 창업비용 기반 추정 여신 — 합성 난수 아님)

> 총 여신 666억원 · LGD 45% 기준 1년 예상손실 **15.3억원**(2.30%) · 위험 상위 10% 동시 악화
> 스트레스 시 **16.8억원** · 상위 10개 브랜드 집중도 62.2% · HHI 0.068

PD는 여신 실무 관행대로 **하한 0.03%(바젤 IRB) · 상한 99%** 로 클립한다. isotonic 보정이 계단함수라
양 끝이 검증표본 경험률로 포화되어 실측에서 "1년 악화확률 100%"가 나왔기 때문이다(단조 변환이라
순위 기반 지표는 불변, 과신만 억제).

## 빠른 시작 (재현 절차)

```bash
git clone <repo> franscore && cd franscore
pip install -r requirements.txt          # 버전 고정 (Python 3.13 기준)

# 전체 파이프라인 (수집→패널→피처→라벨→학습→평가→포트폴리오→뉴스)
python run_pipeline.py --step all

# 두 표본 트랙 + 워크포워드 검증 전부 (제출 근거 재생성)
python run_both_tracks.py

# RAG 코퍼스·색인 구축 (심사메모 근거 검색)
python -m src.rag

# 브랜드별 진단 소견 (공시·본부재무·검색수요·보도 → 문장 소견 + 감시점수)
python run_pipeline.py --step diagnose

# 서비스 화면
streamlit run src/app.py

# 테스트
python tests/test_sanity.py        # 6/6  라벨규칙·시점누출·시간분할·스키마·포트폴리오·상관
python tests/test_diagnosis.py     # 9/9  계단보간·조사·규칙성분·소견 사실성·브랜드 변별
python tests/test_llm_paths.py     # 4/4  LLM 클라이언트·실경로·RAG (모의 HTTP, 키 불필요)
python tests/test_calibration.py   # 6/6  계단 밖 출력·기저율 대체·단조성·등급 척도 일치
python tests/test_naming.py        # 4/4  산출물을 PD(부도확률)라 부르지 않는지
python tests/test_theme_contrast.py #     명도대비 35종·타이포 스케일·진입 스크립트 규칙
python tools/check_doc_numbers.py  # 114건 문서에 쓴 숫자가 산출물과 일치하는지

# 린트 (규칙·버전 고정 — ruff.toml / requirements-dev.txt)
pip install -r requirements-dev.txt && python -m ruff check .    # 기대: All checks passed!
```

**재현성 실측 확인:** 새 클론에서 `data/raw` 스냅샷만으로 API 호출 없이 실행 → 라벨 표본 3,635행,
양성률 9.2%, LightGBM Lift 2.500이 **정확히 동일 재현됨**.
문서에 적힌 수치가 실제 산출물과 일치하는지는 `python tools/check_doc_numbers.py` 가 매번 대조한다.

- `data/raw/` 스냅샷 포함(대용량은 gzip) → **API 키 없이 전체 지표 재현 가능**.
  수집 재실행 시에만 `DATA_GO_KR_KEY` 필요 (data.go.kr 개발계정 자동승인).
- LLM 기능(뉴스 신호 구조화·심사메모)은 `GEMINI_API_KEY` 설정 시
  **Google Gemini(`gemini-2.5-flash`)** 로 동작 — `python check_keys.py --llm` 으로 즉시 진단 가능.
  없으면 규칙기반 폴백으로 자동 전환(화면·로그에 `llm_used=false`, 폴백 건수까지 명시).
  **키 없이도 LLM 실제 코드 경로는 모의 HTTP 테스트로 검증됨**(`tests/test_llm_paths.py`).
- 합성 데이터 스모크: `python run_pipeline.py --step all --demo` (산출물 `outputs/_smoke/` 격리)

## 디렉토리

```
franscore/
├─ config.yaml            # 모든 파라미터 (라벨 임계값·분할·모델 복잡도 후보·시나리오) — 학습 전 동결
├─ run_pipeline.py        # 파이프라인 러너 (--step ... --scope primary|extended --demo)
├─ run_both_tracks.py     # 두 표본 트랙 + 워크포워드 일괄 실행 → track_comparison.csv
├─ data/raw/              # 공정위 API 원본 스냅샷 (재현성 — 수정 금지, 대용량 gzip)
├─ data/processed/        # 패널·피처·라벨·예측·RAG 코퍼스 parquet
├─ outputs/               # 지표·보정·SHAP·부트스트랩CI·포트폴리오·뉴스·RAG색인 (제출 근거)
│  └─ extended/           # 확장 표본 트랙 산출물 (격리)
├─ src/
│  ├─ collect.py entity.py panel.py        # M1 수집·관리번호 정합·패널
│  ├─ dart.py ifrmp.py naver.py            # M1.5 본부재무(DART)·정보공개서·네이버(수요/뉴스)
│  ├─ features.py labels.py                # M2 피처(49)·라벨 (시점누출 자동검증)
│  ├─ model.py evaluate.py backtest.py     # M3 학습·기준모형3종·지표·보정·SHAP·워크포워드
│  ├─ score.py diagnosis.py                # M3.5 최신 코호트 점수 + 브랜드별 진단 소견(규칙 34종)
│  ├─ news_llm.py portfolio.py memo_llm.py rag.py chat.py  # M4 뉴스·원화손실·심사메모·RAG·상담
│  ├─ theme.py                             # KB 디자인 시스템 (색·타이포·차트 템플릿)
│  ├─ app.py                               # M5 서비스 셸 (테마 + 좌측 내비 + 라우팅)
│  └─ views/                               # 화면 5종
│     ├─ overview.py                       #   한눈에 보기 — 현황·우선 점검 대상
│     ├─ brand.py                          #   브랜드 조회 — 자동완성·진단소견·추이·본부재무·수요
│     ├─ queue.py                          #   점검 큐 — 배정·처리상태·확인 메모
│     ├─ portfolio.py                      #   여신 포트폴리오 — 실행/회수 반영 즉시 재계산
│     └─ assistant.py                      #   AI 상담 — 자연어 질의 + 근거 인용
├─ tests/                 # sanity 6종 + 진단 7종 + LLM/RAG 4종
├─ docs/                  # INTERFACES · IMPLEMENTATION · LEAKAGE_CHECKLIST · AI_USAGE
└─ DEPLOY.md              # 웹 배포 절차 (Streamlit Cloud / HF Spaces / 사내망)
```

## 서비스 화면

심사역이 실제로 쓰는 화면입니다. **개발 근거(산출물 존재 여부·하이퍼파라미터·백테스트
지표표)는 이 화면에 두지 않습니다** — 그것은 이 README 와 `docs/`, `outputs/` 의 몫입니다.

| 화면 | 하는 일 |
|---|---|
| 한눈에 보기 | 전체 현황과 **지금 봐야 할 브랜드 8개**. 각 브랜드에 그 브랜드의 수치로 쓴 진단 문장이 붙는다 |
| 브랜드 조회 | 한 글자만 쳐도 후보가 뜨는 검색. 진단 소견 · 공시 추이 · 가맹본부 재무 · 검색 수요를 탭으로 |
| 점검 큐 | 담당자 배정 → 처리상태 → 확인 메모. 엑셀·CSV 반출 |
| 여신 포트폴리오 | 여신 실행·회수를 입력하면 총여신·쏠림(HHI)·집중도·예상손실이 **즉시 재계산**. 브랜드·업종 한도 초과 경고 |
| AI 상담 | "인생냉면 창업 고민 중인데 분석해줘" 같은 자연어 질의 → 공시·감사보고서·뉴스에서 근거를 찾아 답변 |

디자인은 KB국민은행 운영 사이트에서 **실측한 값**을 씁니다 — 시그니처 `#FFCC00`,
웜 그레이 텍스트 `#4E473F`·보더 `#EAE5DF`. 차트는 전부 같은 팔레트의 Plotly 템플릿입니다.

### 브랜드별 진단 소견 (`src/diagnosis.py`)

구버전 화면은 위험 근거를 SHAP 피처명 3개로 보여줬습니다. 피처명이 18종뿐이라
**상위 브랜드의 설명이 전부 같았고**, 숫자가 없어 심사 소견으로 쓸 수 없었습니다.

지금은 규칙 34종이 공시 이력·본부 재무·검색수요·보도를 훑어 그 브랜드의 실제 수치로
문장을 만듭니다. 업종 벤치마크(같은 연도·업종그룹 분위수)를 함께 넣어 비교 가능하게 합니다.

> 2024년에 계약이 끝난 가맹점이 82개로, 연초 점포의 58.6%입니다.
> 한식 업종 상위 8%에 해당합니다. 업종 중간값은 4.6%입니다.

실측: **1,442개 브랜드 · 소견 8,763건 · 브랜드당 6.1건 · 서로 다른 소견조합 908종.**

감시점수는 `det_component`(백테스트로 검증된 모델 순위)와 `rule_component`(규칙 정책)를
**산출물에 분리 기록**합니다. 어디까지가 검증된 부분인지 드러나야 하기 때문입니다.
규칙 성분은 카테고리별로 체감 합산합니다 — 단순 합산은 '계약종료율 높음'·'중도해지 많음'·
'순유출' 처럼 한 사실을 세 각도에서 말한 소견이 겹쳐 +126점까지 치솟아
검증된 모델 성분(최대 100)을 압도했습니다.

### 확률 계단 제거

isotonic 보정은 계단 함수라 **2,510개 중 216개가 42.86%(=3/7)로 같은 확률**을 달고
있었습니다. 화면에서 이것은 "모델이 브랜드를 구분하지 못한다"로 읽힙니다.
계단 내부를 원점수 순위로 **대칭 분산**해 폈습니다 — 계단 평균을 정확히 보존하므로
보정 품질이 유지됩니다.

| test 분할 (n=740) | 서로 다른 값 | 평균확률 | ECE | Lift@10 | 순위상관 |
|---|---|---|---|---|---|
| isotonic 계단 | 17 | 0.10071 | 0.03612 | 2.667 | — |
| **보간 후** | **511** | **0.10071** | **0.03612** | **2.667** | **1.000000** |

## 데이터 출처 (전부 공개·무료)

**개인정보 취급 고지 — 정정.** 이 절의 제목은 오랫동안 "개인정보 0"이었으나 **사실이 아니다.**
공정위 15125467 응답에는 `jnghdqrtrsRprsvNm`(가맹본부 대표자 성명)이 포함돼 있고,
`data/raw/brand_master_*.json` 스냅샷 **84,368행**에 그대로 남아 있다.

| 항목 | 실태 |
|---|---|
| 대표자 성명 | 원본 스냅샷에 존재(84,368행). **코드 참조 0건 · 파생 산출물 전파 0건** — 패널·피처·점수 어디에도 들어가지 않는다 |
| 사업자등록번호·법인등록번호 | 존재. **법인 식별자**이며 엔티티 정합 키로 실제 사용 |
| 개인 차주 정보 | **없음.** 이 도구는 브랜드 단위이며 개인 차주 데이터를 취급하지 않는다 |

은행 도입 시에는 수집 단계에서 대표자 성명을 드롭하면 되고(참조가 없으므로 산출물 영향 0),
그 전까지 **저장소를 비공개로 유지**한다. 제목이 사실과 달랐던 것을 그대로 적어 둔다 —
검증자가 확인할 때 문서가 먼저 인정하고 있어야 한다.


| 데이터셋 | 서비스 | 내용 | 용도 |
|---|---|---|---|
| 15110241 | `FftcBrandFrcsStatsService` | 브랜드×연도 가맹점수·신규등록·계약종료/해지·명의변경·평균매출·면적당매출 | 패널 본체·라벨 |
| 15109828 | `FftcBrandBrandStatsService` | 가맹사업개시일·연차·임직원수 | 업력·본부규모 피처 |
| **15125467** | `FftcBrandRlsInfo2_Service` | **브랜드관리번호·본부관리번호·사업자등록번호** | **엔티티 정합 키(명세 §2.2)** |
| **15125490** | `FftcBrandFrcsDropInfo3_Service` | **브랜드×연도×지역 가맹점수·직영점수** | **직영비중 + 지역분산 피처** |
| **15125518** | `FftcBrandRtrcnInfo2_Service` | 브랜드 등록취소(자진/직권)·사유 | 하드 실패 신호 |
| 15157660 | `FftcIndutyFrcsCntOpclStatsService` | 업종×연도 개폐점률 | 업종 기준선 |
| **OPEN DART** | `opendart.fss.or.kr` | **가맹본부 감사보고서·사업보고서 재무제표, 감사의견** | **본부 재무 피처 15종 + RAG 1차 출처** |
| 정보공개서 원문 | `franchise.ftc.go.kr` | 본부 재무·점포 유출입·법 위반·소송·계약해지 사유 (110개 표준 섹션) | 파서 검증 완료 · **정식 키 대기** |
| Google News RSS | — | 브랜드 뉴스(제목·출처·발행일) | LLM 실시간 신호 (**점수 미투입**) |
| 네이버 검색 API | `openapi.naver.com` | 브랜드 뉴스(제목 + **본문 요약**) | 뉴스 신호 품질 향상 (선택 · 무료 25,000회/일) |

주의: 공시 기준연도(yr)의 수치는 직전 회계연도 실적이므로 **패널 연도 = yr − 1**로 정렬했다
(`acntgYr` 필드로 교차 확인).

**필요한 인증키** (전부 무료, 없으면 해당 층만 자동으로 비활성화된다):

| 환경변수 | 발급처 | 없을 때 |
|---|---|---|
| `DATA_GO_KR_KEY` | data.go.kr | 공정위 공개 데모키로 대체 |
| `DART_API_KEY` | opendart.fss.or.kr (즉시 발급) | `f_hq_*` 피처 미생성, 나머지는 정상 |
| `IFRMP_SERVICE_KEY` | franchise.ftc.go.kr | 데모 고정응답을 자동 탐지해 브랜드별 수집 중단 |
| `NAVER_CLIENT_ID` / `_SECRET` | developers.naver.com (즉시 발급) | Google News RSS 단독 사용(제목만) |
| `GEMINI_API_KEY` | aistudio.google.com | 규칙기반 폴백 (`llm_used: false` 표기) |

## 정직성·재현성 장치

1. **시점 누출 금지** — 연도 t 피처는 t 이하만 사용. 윈저라이즈·업종분위수·보정기·복잡도 선택 모두
   해당 시점 또는 valid에서만. t+1 이후 전체 교란 후 피처 bit-exact 불변을 자동 검증.
   전수 점검표: **[docs/LEAKAGE_CHECKLIST.md](docs/LEAKAGE_CHECKLIST.md)** (명세 §4.2)
2. **실시간 표본 선별** — 표본 편입도 t까지의 누적 정보만 사용(초기 구현의 룩어헤드 결함을
   적대적 리뷰로 발견해 수정, 이력 문서화).
3. **라벨 동결** — 규칙·임계값을 `config.yaml`에 학습 전 고정, 최초 git 커밋으로 증빙.
4. **과튜닝 금지** — 그리드서치 없음. 3개 사전 선언 복잡도 후보를 **valid AUC로만** 선택, test 미사용.
5. **체리피킹 금지** — 두 표본 트랙을 사전 선언하고 **둘 다 공개**. 부트스트랩 CI로 우위 주장의
   불확실성까지 공개(유의하지 않은 비교는 "동등"으로 명시).
6. **익스포저 출처 명시** — 여신 익스포저는 공정위 공시 창업비용(15110265) **실측값** 기반이며
   (가맹점수·창업비용은 실측, 대출조달비율 60%·은행점유율 15%만 가정), 화면·산출물·메모에
   산출식과 가정을 상시 표기한다. 실측 원천을 못 쓰는 경우에만 합성 폴백으로 내려가며
   그때는 `synthetic: true` 로 뒤집힌다(플래그가 실제 경로를 따른다).
7. **LLM 정직 표기** — 실제 LLM 추출 건수와 규칙 폴백 건수를 분리 로깅(의도가 아닌 결과 기준).

## 알려진 한계

1. 여신 익스포저는 **공시 창업비용 기반 추정치**다. 가맹점수·창업비용은 실측이지만
   대출조달비율·은행점유율 두 비율은 가정이며, 실제 여신 잔액은 은행 내부 자료라 공개
   데이터에 존재하지 않는다. `config.portfolio.exposure_source` 에 CSV 경로만 넣으면
   은행 실여신으로 즉시 교체된다.
2. 브랜드 악화 → 개별 차주 부도 연계는 미검증 (KB 내부데이터 PoC 항목).
3. 정보공개서 **원문**은 파서까지 완성·검증했으나 전량 수집은 공정위 정식 키 대기 중이다.
   공개 데모키는 본문을 **고정 1건**으로만 반환하며(서로 다른 `jngIfrmpSn` 5건의 SHA-256 동일),
   코드가 이를 스스로 탐지해 브랜드별 산출물을 만들지 않는다. `outputs/ifrmp_status.json` 참조.
4. 엔티티 정합 2.2%는 관리번호 미확보로 명칭 기반 ID 사용(출처를 `id_source` 컬럼에 표기).
5. 2021년 공시 등록 급증(직영점 요건 완화)으로 2020↔2021 표본 단절 존재.
6. LightGBM의 우위는 persistence 대비 유의, 강한 기준모형 대비 동등 (위 ② 참조).

## 라이선스·이용 고지

공정위 오픈API 데이터는 공공데이터로 자유 이용 가능. 수집 시 사용한 키는 공정위가 자체 미리보기
페이지에 공개 배포한 데모키이며, **제출·운영 시에는 본인 `DATA_GO_KR_KEY`로 교체**를 권장한다
(코드가 환경변수를 우선 사용). Google News RSS는 개발 단계 보조 신호로만 사용했으며 상용 배포 시
계약 기반 피드(예: 네이버 오픈API)로 대체를 권장한다.
